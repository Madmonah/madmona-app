# 🐞 سجل الباجات — مضمونة

> قاعدة: أي باج يتصلّح يتسجّل هنا (التاريخ · العرض · السبب الجذري · الإصلاح · الدرس)
> علشان ميتكررش ويبقى عندنا ذاكرة. الأحدث فوق.

---

## 2026-07-22 — مسح المنيوهات «الاسترشادية» المؤقتة (مطاعم الدليل)
- **العرض:** 37 مطعم directory (مسحوبين من دليل جوجل) كان عندهم منيوهات استرشادية
  (كل صنف مكتوب عليه «سعر استرشادي — بيتأكّد من المطعم عند الطلب») — حالة مؤقتة
  بتعمل كونفلكت في الداتا. المطلوب: تتشال نهائياً وميرجعش أي أداة تعملها.
- **الفحص:** الـ231 صنف اتعملوا كلهم دفعة واحدة يوم 2026-06-04 08:12:57 (سييد مرة
  واحدة، مش أداة مستمرة). مفيش أي كود في الريبو بيولّد الجملة دي — بحثت عن النص
  المخزّن «سعر استرشادي / بيتأكّد من المطعم» = صفر نتائج. يعني مفيش جينيريتور شغّال.
- **الإصلاح:** (1) backup لكل الصفوف في `_backup_20260722_indicative_menu_items`
  (+ الأحجام + أوردر لاينز في `_backup_20260722_indicative_order_items`). (2) مسح
  228 صنف مش مرتبطين بأوردر. (3) الـ3 أصناف المرتبطة بـ`marketplace_order_items`:
  أول الأمر خبّيتهم بس (FK)، وبعد ما أكّد محمد إن كل الأوردرات دي تجريبية → مسحت
  الأوردر لاينز (backup الأول) وبعدين الأصناف = **0 صنف استرشادي فاضل**. (4) رجّعت
  كل مطاعم الـdirectory لـpaused. (5) revert لكوميت عرض المنيو الاسترشادي (dbb8af0).
- **الدرس:** قبل أي DELETE في restaurant_menu_items لازم تتأكد من FK بتاع
  marketplace_order_items — أي صنف اتطلب قبل كده ماينفعش يتمسح. الطريقة الآمنة:
  backup → امسح غير المرتبط بأوردر → اخفِ (is_available=false) المرتبط بأوردر.
  وسييد الداتا المؤقت لازم يتعلّم عليه (description ثابت) عشان يبقى قابل للمسح لاحقاً.

## 2026-07-21 — منيوهات مطاعم مش ظاهرة (تصنيف «مأكولات بدوية»)
- **العرض:** 4 مطاعم منشورة وبمنيوهات كبيرة (مشوي ومندي 272 صنف، السدة 172،
  قصر المندي، خليها مشاوي = 465 صنف إجمالي) مش بتبيّن أي منيو في صفحة الإعلان.
- **السبب الجذري:** صفحة العميل `marketplace/[slug]/page.tsx` بتجيب المنيو وتعرضه
  بشرط `category.track === 'restaurants'`. تصنيف «مأكولات بدوية 🏜️» (food-bedouin)
  كان `track = NULL` بدل `'restaurants'` — فبوابة العرض بتفشل والمنيو مايتحمّلش أصلاً.
  كل تصنيفات الأكل التانية كانت مظبوطة؛ ده الوحيد الناقص.
- **الإصلاح:** `UPDATE categories SET track='restaurants'` على food-bedouin. اتأكد
  live إن مشوي ومندي بقى بيعرض الـ272 صنف بكل الأقسام.
- **الدرس:** بوابات العرض المعتمدة على `category.track` هشّة قدام أي تصنيف track ناقص.
  أي تصنيف بيقدّم أكل لازم track='restaurants'. لو ظهر بج «منيو مش بيبان» راجع الأول
  الـtrack بتاع تصنيف الإعلان قبل أي حاجة تانية.

## 2026-07-21 — ناس كتير مش عارفة تدخل بالواتساب («مبيسجلش دخول»)
- **العرض:** الدخول بالواتساب بيعلّق على «مستنيين رسالتك…» ومبيكمّلش.
- **السبب الجذري:** الدخول معتمد على إن الويبهوك (whatsapp-webhook) يأكّد الكود
  في `wa_inbound_verifications`. لو حصل خلل في المعالجة اللحظية (لبس LID/رقم مثلاً)
  الكود مايتأكدش — رغم إن رسالة العميل **وصلت واتسجّلت** في `whatsapp_messages`.
  الاعتماد على الويبهوك اللحظي هشّ.
- **الإصلاح:** GET /api/auth/wa بقى «صامد للحظر»: لو الكود لسه مش verified، بيدوّر
  على رسالة inbound فيها الكود في `whatsapp_messages` (الوارد بيوصل ويتسجّل حتى
  تحت الحظر — الحظر بيمنع الإرسال بس)، وياخد رقم صاحبها ويأكّده.
- **الدرس:** أي تحقق معتمد على معالجة لحظية خلّي وراه fallback بيقرا من مصدر البيانات
  الثابت (الرسالة في الداتابيز) — الوارد أوثق من أي هوك. الشرط الوحيد: البوت متصل
  (يستقبل)؛ لو مفصول لازم يرجع يتوصّل الأول.

## 2026-07-21 — الرجوع من صفحة المنتج بيوديك على تاب «الكل»
- **العرض:** تفتح مطعم/منتج من تاب معيّن (مطاعم مثلاً) → باك → يرجّعك على «الكل».
- **السبب الجذري:** زر الرجوع في `marketplace/[slug]/page.tsx` كان `<Link href="/marketplace">`
  ثابت — بيروح للديفولت (الكل) بدل ما يرجّع لنفس المكان بالتاب.
- **الإصلاح:** الزر بقى `window.history.back()` (مع fallback لـ/marketplace لو مفيش تاريخ)
  — بيرجّع لنفس الـURL بالتاب (?track=) اللي محفوظ بالفعل.
- **الدرس:** أزرار الرجوع في صفحات التفاصيل تستخدم history.back() مش رابط ثابت،
  علشان تحافظ على حالة التصفح (التاب/الفلتر/السكرول).

## 2026-07-21 — الماركت بليس بيبان فاضي على الموبايل / أول فتحة
- **العرض:** فتح مضمونة من كروم موبايل أول مرة → الماركت بليس مفيهوش ولا إيتم.
- **السبب الجذري:** في `MarketplaceClient.tsx` كان `const { data } = await query`
  بيتجاهل `error` تمامًا، وعند أي فشل (نت موبايل ضعيف / أول تحميل) `data=null`
  فيتعمل `setListings([])` = سوق فاضي بصمت. + الليستنجز بتتحمّل client-side بس
  (الـSSR shell فيه الكاتيجوريز بس مش المنتجات).
- **الإصلاح (commit 2a6058f):** مسك `error` + إعادة محاولة أوتوماتيك ٣ مرات (backoff)
  + عدم تفضية القائمة عند الفشل + زر «جرّب تاني» بدل «مفيش نتايج» المضلّلة.
- **الدرس:** أي كويري بيغذّي شاشة أساسية لازم يمسك `error` ويكون عنده retry + حالة
  خطأ واضحة. الأفضل مستقبلًا: SSR لأول صفحة ليستنجز.

## 2026-07-21 — إشعار رد المارد مكانش بيتبعت
- **العرض:** المارد بيرد بس مفيش صف بيتكتب في `notification_queue`.
- **السبب الجذري (٢ مع بعض):**
  1. الاستدعاء كان `void enqueueReplyPush(...)` (fire-and-forget) قبل الـresponse،
     والـserverless بيقفل الـfunction بعد ما يرجّع الرد فيقتل الـpromise قبل الإدخال.
  2. تريجر `notification_queue_dedupe` بيرمي (RETURN NULL بصمت) أي إشعار بنفس
     `title` لنفس الشخص خلال ٦٠ دقيقة — فالاختبارات المتكررة كانت بتتشال.
- **الإصلاح:** `await enqueueReplyPush(...)` + مطابقة رقم بـ`.in([010..,2010..,+2010..])`.
- **الدرس:** (أ) في الـserverless لازم await لأي شغل مهم قبل الـresponse — مفيش
  background promises. (ب) BEFORE INSERT trigger اللي بيرجّع NULL بيمنع الصف من
  غير error — لو insert «نجح» ومفيش صف، دوّر على تريجر dedupe.

## 2026-07-21 — أيقونات maskable بترجع 404
- **العرض:** أيقونات PWA الـmaskable مش بتظهر.
- **السبب:** `manifest.json` بيشاور على `/icon-maskable-192.png` بينما الملفات
  اسمها `/icon-192-maskable.png` (ترتيب غلط).
- **الإصلاح:** تصحيح المسارات في manifest.json.
- **الدرس:** أي مسار أصول في المانيفست لازم يتأكد إنه موجود فعلًا (404 = أيقونة ضايعة).

## 2026-07-21 — endpoint تشخيص رجع 404 دايمًا (Next.js gotcha)
- **العرض:** عملت route على `/api/_diag-push` وكان بيرجع 404 دايمًا.
- **السبب:** في Next.js App Router، أي فولدر بيبدأ بـ`_` بيبقى «private folder»
  ومبيتعملش له route.
- **الدرس:** مفيش شرطة سفلية في بداية أسماء فولدرات الـroutes.
