# موديول شات مضمونة — الحالة والمتبقّي

> ٢١ يوليو ٢٠٢٦ · قناة شات مملوكة بالكامل (بعيد عن واتساب/ميتا والبان)

## ليه الموديول ده

واتساب (Baileys) بيتحجب: الرقم بيتصل ويستقبل، بس رسايله مابتتسلّمش (shadowban).
اتأكد ده بتجارب كتير (رقمين مختلفين + جلسة جديدة + إرسال مباشر — كله فشل).
فبنينا شات مملوك: نفس **مخ المارد**، رد مباشر في المتصفح، صفر اعتماد على ميتا.

## المعمارية والملفات

- `src/lib/marid-brain.ts` — `callMaridWithTools()` (منقولة من راوت واتساب، نفس الأدوات والبرومبت).
- `src/lib/marid-media.ts` — `saveMedia` (Supabase Storage) + `transcribeAudio` (Whisper) + `processIncomingMedia`.
- `src/app/api/chat/route.ts` — الراوت: يستقبل {phone,name,message,media}، يدير المحادثة في `whatsapp_conversations` (session_id='web')، يشغّل المارد، يرجّع {reply}. وبينشئ أكونت مضمونة للعميل الجديد (`ensureAccount`، نفس نمط `/api/auth/wa`).
- `src/app/chat/page.tsx` — واجهة بشكل واتساب (RTL، أخضر).
- كرت "كلّم المارد" في `src/app/account/page.tsx`.
- التخزين: صور → bucket `content-images`، الباقي → `project-media`.

## ✅ خلص واتنشر (٢١ يوليو)

- الشات الأساسي: نفس مخ المارد، رد مباشر، ذاكرة بالرقم. (متأكّد)
- ميديا: صور (المارد بيشوفها — متأكّد) · صوت (Whisper) · ملفات/PDF · فيديو.
- ضغط الصور client-side (1280px / JPEG 0.7) قبل الرفع.
- هوية بالأكونت (دخول تلقائي للمسجّلين من `profiles`).
- إنشاء أكونت للجداد بالاسم والرقم (auth user + profile). (متأكّد)
- لينكات الردود قابلة للضغط.
- إيموجيز · مشاركة الموقع (geolocation) · اختيار ميعاد (datetime) · ردود سريعة · زرار مايك/إرسال متبدّل.
- اتشال زرار تليجرام العايم (`TelegramBotFab`).

## 🔲 المتبقّي (Backlog — بالترتيب المقترح)

1. **الكاليندر ↔ حجز المواعيد** — يربط اختيار الميعاد بأداة `manage_meeting` (يعرض المواعيد المتاحة من الشات، أو مانيوال). دلوقتي بيبعت نص بس.
2. **إعداد عرض المنتجات في الأكونت** — المورد يختار إعلاناته تتعرض: من الماركت بليس بس، أو من المشاريع (البورصة) بس، أو الاتنين. محتاج حقل على المورد + فلترة في الماركت/المشاريع.
3. **تاب/سايد بار "فريق العمل"** — مود شغل (مش "أصدقاء"): محادثات الفريق/الزملاء جوّه الحساب. تصميم جديد.
4. **ضغط الفيديو** — محتاج عامل/ffmpeg (مش serverless). دلوقتي الفيديو بيترفع زي ما هو (حد ٣٠ ميجا).
5. **التوجيه الذكي للميديا** — لو الميديا لمورد/إعلان، ترفع في مكانها الصح (بورصة/ماركت) عبر أدوات المارد (`create_listing_draft`).

## ملاحظات مهمة

- كل ملفات الشات معزولة — **ماتلمسش مسار الواتساب الشغّال** (`/api/whatsapp/baileys`).
- إنشاء الأكونت best-effort (مايوقفش الرد) — نفس النمط الآمن في `/api/auth/wa` و`cron/publish-drafts`.
- محادثات الشات كلها `session_id='web'` في `whatsapp_conversations` (سهل تمييزها).
