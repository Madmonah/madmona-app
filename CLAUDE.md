# ⛔ اقرا الجزء ده الأول — أوامر محمد الثابتة

> كل سطر هنا محمد قاله بلسانه. **ماينفعش يتسأل عليه تاني.**
> الباقي بعد الخط الطويل تحت أرشيف وتفاصيل — ارجعله عند اللزوم بس.

## ١) الفلوس — الرصيد بيتحرق قدام عينيه
«انا بشوف الرصيد بيتاكل قدام عيني» · «متتعبنيش طول اليوم»

كل نداء أداة بيبعت المحادثة كلها من أولها. الجلسة الطويلة تكلفتها **تربيعي**.

- **متطبعش خرج كامل لأي أمر** (`tsc` · `npm` · `git log`). وجّه لملف
  وبعدين `findstr` على اللي يهمك بس.
- **متعملش `select *`** على جدول عريض.
- **متلفّش على الديبلوي كل شوية** — استنى مرة واحدة طويلة.
- **متجرّبش نفس الحاجة أكتر من مرتين.**
- شغّل الحاجات المستقلة مع بعض في نداء واحد.
- المتصفح: `channel: 'chrome'` على جهازه. النسخة المدمجة بايظة،
  والسحابة مالهاش وصول للموقع.

## ٢) خطوط حمرا
- **💳 رصيد الـAPI (٢٧/٨ — قاعدة ثابتة):** محمد: «أي حاجة هتسحب من الرصيد
  بإنها تستخدم API لازم أكون على علم وأوافق قبل ما تتنفذ — لازم يتعرض عليا
  التكلفة الأول». يعني: قبل أي سكريبت/جوب/أداة بتنادي Anthropic بمفتاحه —
  اعرض: إيه اللي هيتعمل + الموديل + عدد النداءات/التوكن المتوقع + التكلفة
  بالدولار، **واستنى موافقته نصًا**. مفيش تشغيل «تجريبي» ولا «دفعة صغيرة»
  من غير إذن. الترجمة والكتابة والتلخيص بتتعمل في الشات وتترفع بملفات
  (`scripts/apply-listing-i18n.mjs` نموذج). ده بيسري على كل الجلسات
  والأدوات (كو-وورك · شات · Code).
- **ممنوع تولّد باسورد وتخزّنه plaintext.** محمد بيكتبه بإيده من اللوحة.
- **ممنوع تخترع سعر أو نسبة أو إحصائية** في أي حاجة بتروح لعميل.
- **💰 العمولة — اقرا ده كامل قبل ما تكتب أي نص فيه كلمة «عمولة».**
  محمد (٤/٩/٢٠٢٦): «العميل ملوش دعوة بالعمولة — السعر اللي بيحطه
  لمضمونة هو اللي بيحصله». وقال كده بعد ما جلسة غيّرت نص في صفحة
  استلام المعرض من «مفيش عمولة على مبيعاتك» لـ«عمولة ١٠٪ بعد البيع».

  **القاعدة الواحدة:** البايع بياخد **سعره كامل**. مضمونة بتحط نصيبها
  **فوقه** في الباكاند — مابيتخصمش منه. فأي نص بيقول للمورد إنه
  «هيتخصم منه X٪» **غلط**، وأي خانة أو معاينة عمولة في أي شاشة
  مورد **ممنوعة** (اتنضّفت كلها ٢٤/٨).

  ⚠️ **«١٠٪» ليها تلات معاني مختلفة في الملف ده — ماتخلطهمش:**
  | فين | معناها | بتتخصم من مين |
  |---|---|---|
  | نموذج العمل (٢٠ يوليو) | نصيب مضمونة من الصفقة | **بتتحط فوق سعر البايع** — مش منه |
  | المرتبات (٤.٣) | حصة الموظف = ١٠٪ **من ربح مضمونة** | من ربح مضمونة، مالهاش علاقة بالمورد |
  | العربيات (٢٤/٨) | `cars_commission_pct` = **١٪** | نفس المبدأ — فوق السعر |

  **اللي بيتقال للمورد:** «مجاني · والسعر اللي بتطلبه هو اللي بتاخده».
  **اللي ماينفعش يتقال:** أي رقم عمولة، أو «هناخد منك»، أو «بعد خصم».
- **ممنوع أي إرسال واتساب جماعي من السيرفر** — ده اللي ضرب أرقامه.
  الطابور يفضل واقف (`queue_send_enabled='0'`)، وهو بيبعت بإيده من `/crm`.
- التنبيهات الداخلية **نوتيفيكيشن مش واتساب**.
- **المارد (ردود واتساب بالـAI) عالي التكلفة** — سويتشات:
  `marid_reply_mode='off'` = ماحدش يرد، `'template'` = قالب واحد
  ثابت من `marid_reply_template`، غير كده = المارد يشتغل بـClaude.
  الأدمن مستثنى دايمًا. الحل الدائم: تمبليتس مبنية على نيّة الرسالة.

## ٣) الفريق — متسألش عليهم تاني
محمد ناصف (owner) · **أحمد سامي** · **سامية ايهاب** · نورا · عبير ·
شهد · مديحة.

⚠️ **أحمد وسامية موظفين معاه وليهم صلاحيات كاملة.** خلاص.

> «افتح صلاحيات لفلان» = **بابين مش باب واحد**:
> 1. `business_employees.permissions` (التطبيق) ← `{all:true}`
> 2. `platform_admins` (لوحة `/admin`) ← سطر بحساب شخصي
> افتح الاتنين مع بعض. البنت اتحطّت في الأول بس مرتين قبل كده — والنتيجة
> إنها بتفتح التطبيق وتتقفل في وشها اللوحة، فمحمد يرجع يقولّي تاني.

**بابين دخول مختلفين — اللخبطة بينهم كلّفت يوم كامل:**

| الباب | الجدول | بيفتح |
|---|---|---|
| `/admin-entry` (إيميل+باسورد) | `platform_admins` → كوكي | لوحة `/admin` |
| دخول التطبيق العادي | `auth.users` + `profiles` | التطبيق |

صفحة جوّه `/admin` **مالهاش حق تطلب جلسة Supabase** — تستخدم
`adminPanelStage()` من `src/lib/platform-staff.ts`.
موظفين مضمونة دورهم `business_ops` **مش** `admin` عن قصد (لأن
`role='admin'` بيشغّل `is_admin()` في RLS في كل المنصة).
الصلاحيات التفصيلية في `business_employees.permissions` (`all:true`).

## ٤) توزيع أرقام الـCRM
مطاعم + تجميل → **نورا** · طبي + مصانع → **مديحة وعبير** ·
عقارات (الأغلبية) → **عبير** · الباقي → **مديحة**.

الموزّعين (`crm_staff_settings.is_dispatcher=true, receives_leads=false`):
**محمد ناصف · أحمد سامي · سامية**. الموزّع مايبقاش شايل أرقام
(receives_leads=false)، بس بيشوف تاب «الفريق كله» في الموبايل
ويقدر يفتح قايمة أي مندوب (`p_as`).

## ٤.١) التاسكات
**كل واحد يشوف تاسكاته هو بس.** `flow_tasks` بتتفلتر لموظف محدد
(owner_id أو employee_id أو assignee_email أو assignee_name).
الشات (`/chat/tasks` ← `get_my_tasks`) والموبايل (`/account/work`
← `get_my_work_home.my_tasks`) بيقروا نفس المصدر — لو أضفت حاجة
في واحد ضيفها في التاني.

## ٤.٣) 💼 نظام المرتبات والعمولة (٢٧/٨ — كله في الداتابيز)
⚠️ **مفيش كود للنظام ده في جيت** — كله ميجريشنز مطبّقة لايف.
متدوّرش عليه في `src/` — راجع الدوال والجداول في Supabase.

**الجداول:** `payroll_rules` (قواعد دينامِك لكل قسم) ·
`commission_entries` (دفتر العمولات).
**الأعمدة:** `listings.created_by_employee_id` ·
`listing_drafts.created_by_employee_id` · `marketplace_orders.sold_by_employee_id`
· `bz_advances.employee_id` · `daily_tasks.review_status/evidence`.
**الدوال:** `preview_payroll()` · `accrue_order_commission()` ·
`reverse_order_commission()` · `auto_review_tasks()` · `employee_day_hours()` ·
`net_daily_hours()`. **الويوز:** `v_employee_attribution` · `v_commission_split`.
**الشاشات:** `/admin/payroll` · `/admin/task-review` · `/admin/payments`.

**القواعد اللي محمد حددها:**
- العمولة = **١٠٪ من ربح مضمونة** (`commission_amount`) مش من سعر البيع.
- بتتقسم **٥٠/٥٠** بين اللي ضاف واللي باع؛ اللي عمل الاتنين ياخدها كاملة.
- **بعد التحصيل بس** (`earned`)؛ بتترد لو الأوردر اترجع (`reversed`).
- 🏛️ **المورد ضاف لوحده أو العميل اشترى لوحده → الحصة لمضمونة**
  (`employee_id = NULL`، role = `house_added`/`house_sold`). الدفتر بيقفل
  على ١٠٠٪ دايمًا — مفيش حصة بتضيع.
- الشغل **بالساعات مش بالمواعيد**: ٩ ساعات فيهم ساعة راحة = ٨ صافي.
- التاسكات: **المتحقّق منه بس** بيتحسب — مش اللي الموظف قفله.
- المرتب **اقتراح للمراجعة مش صرف تلقائي**.

⚠️ **لسه معلّق:** الخصم على الحضور معطّل عمليًا (حد أدنى ٥٠٪)
لحد ما بيانات البصمة تستقر — الفريق مسجّل ١٠٪ من ساعاته بس
(الجلسات كانت بتفصل — اتصلحت بالحساب بالمدى).
و**العمولة مش بتتحسب على أي إعلان اتضاف قبل ٢٧/٨** — الإسناد ماكانش موجود.

## ٤.٤) 📞 معمل المكالمات — ربط تليفون عادي بأستريسك (٢٨/٨)
⚠️ **تجربة معملية — مش جزء من مضمونة ومش في جيت.**
كل حاجة على لاب محمد في `D:\voice\`.

**النتيجة: نجحت.** مكالمة حقيقية → شبكة المحمول → إيتل A70
→ بلوتوث → لينكس → أستريسك يرد ويتكلم → المتصل سمعه.
من غير فلاشة ولا روت ولا مساس بالويندوز.

**الإعداد:** VirtualBox VM `madmona-voice` (أوبونتو سيرفر ٢٤.٠٤) ·
كارت بلوتوث Intel AX200 ممرَّر بـUSB filter · أستريسك ٢٠.٢١
**مبني من المصدر** (الحزمة مفيهاش chan_mobile) · SSH على بورت 2222.

**٥ إصلاحات جوهرية متلزمة (لو المعمل اتعاد بناؤه):**
1. `MENUSELECT_ADDONS` = قايمة **المستبعَد** مش المفعَّل — `--enable`
   بيستبعد! الصح: شيله من السطر خالص.
2. الموديول لازم يكون **نفس نسخة** أستريسك المشغّلة.
3. BlueZ لازم `--compat` (وإلا: *Failed to connect sdp*).
4. 🔑 **`disable_esco=1`** — ده اللي حل مشكلة *corrupted SCO packet*
   وخلّى صوت أستريسك يوصل للمتصل. eSCO محتاج توقيت دقيق
   بيتكسر عبر USB الافتراضي. (`/etc/modprobe.d/bt-sco-fix.conf`)
5. **الاقتران لازم يبدأ من التليفون** — من لينكس مابيثبتش.

**ربط المارد:** `agi-bin/marid_call.py` بيوصّل المكالمة بـ`/api/chat`
(نفس المارد بتاع الشات والواتساب — إرشاد وترشيح بس).
🔒 **الوضع الافتراضي `MARID_MODE=offline`** — رد ثابت بدون أي
استهلاك API. التشغيل الحي (تفريغ صوت + رد صوتي) **بيستهلك رصيد**
وممنوع يتفعّل إلا بموافقة محمد على التكلفة (خط أحمر ٢٧/٨).

⚠️ **قيود معروفة:** مكالمة واحدة في المرة لكل تليفون · اللاب لازم
يفضل شغّال · البلوتوث بيفصل عن ويندوز وقت ما الماكينة شغالة.
**للتشغيل الفعلي:** جهاز مخصص ٢٤ ساعة، أو GSM Gateway
(مفيش بلوتوث + خطوط متوازية).
⚠️ **الحملات الخارجة:** ممكنة تقنيًا بس **بتحرق الخط** — زي اللي
حصل في واتساب. وقانون حماية البيانات بيلزم بموافقة مسبقة.

### ✅ القائمة الصوتية (IVR) — نجحت بالكامل (٢٨/٨)
`scripts/voice-lab-clinic-demo.conf` · `scripts/voice-lab-log_callback.sh`
**مثبَت بمكالمتين حقيقيتين:** الرقم اتعرف (`01026068340`)
وضغطة «١» وصلت وودت لقسم العيادات. يعني **DTMF شغّال** —
ودي كانت أكتر نقطة مشكوك فيها (النغمة بتعدي على ضغط البلوتوث).

💰 **تكلفة القائمة الصوتية = صفر** (ملفات مسجّلة، مفيش نداء API)
والرد **فوري بدون تأخير** — عكس المارد الحي (٣–٥ ثواني سكوت
وـ١.١ جنيه/دقيقة). **القائمة أفضل لـ٨٠٪ من المكالمات.**

🐞 **فخ متكرر:** أي سكريبت بيترفع من ويندوز لازم يتشال منه `\r`
(`sed -i 's|\r$||'`) وإلا أستريسك بيقول *Unable to execute*.

⚠️ **الواقع اللي الاسكريبت مبني عليه:** قسم الخدمات الطبية
(`medical-clinics`) = **صفر إعلانات منشورة**. الموجود ٨ إعلان
**عيادات كعقار** للبيع. فالاسكريبت **مابيوعدش بحجز دكتور**
لأنه مش موجود. ✅ أول ما تنزل عيادات شغالة، عدّل الخيار ١.

🎙️ **لسه ناقص:** ٨ ملفات صوتية عربي (محمد هيسجلها بصوته).
النموذج دلوقتي بأصوات أستريسك الإنجليزية المجانية — الخريطة
بين المؤقت والنهائي مكتوبة جوّه الملف.

## ٤.٥) 🩺 صحة الوكلاء — اتوصلت ببيانات حقيقية (٢٨/٨)
محمد: «هيلث ألرت ده يا يجيب بيانات حقيقية يا تقفله».

🐞 **السبب:** `/admin/agent-health` كانت بتقرا من `v_agent_health`
والـview **ماكانش موجود في الداتابيز أصلاً** — فالصفحة بتعرض فاضي.
✅ اتبنى من `agent_runs` الحقيقي (١٣٬٨٤٠ تشغيلة مسجّلة).

🚨 **اللي اتكشف أول ما ربطناه — كان مستخبي بسبب الصفحة المعطلة:**
- `quality-control`: **٩٬٢١٨ فشل من ٩٬٤٧٤** (٢.٧٪ نجاح فقط!)
- ٩ وكلاء تانيين عند صفر٪ نجاح: olx-scraper · draft-followup-nudger ·
  agent-extras · cold-lead-outreach · anthropic-credit-watchdog · agent-slow
- **٥٣ وكيل مشتغلوش من أكتر من شهر** (أغلبهم آخر تشغيل يونيو)

**منطق التقييم:** الفشل قبل التوقف — وكيل بيفشل ٩٧٪ أخطر
من واحد نايم. critical <٥٠٪ نجاح · warning <٨٠٪ أو أسبوع+ ·
disabled ٣٠ يوم+.

⚠️ **قيد:** مفيش جدول تسجيل وكلاء — الفريق بيتحدد من اسم
الوكيل، و`enabled` دايمًا true. لو عايز تعطيل حقيقي محتاج
جدول `agent_registry`.

## ٤.٦) 🚨 درس مهم: GRANT مش نفس حاجة RLS (٢٨/٨)
محمد: «تيكوود لما بيجي يعدّل إعلان بيظهرله مش مورد على مضمونة».

🐞 **السبب الجذري:** `SELECT` على `marketplace_suppliers` كان **مسحوب**
من `authenticated` و`anon` — بينما `UPDATE` مسموح!
يعني المورد يقدر يعدّل بس مايقدرش **يقرا** بياناته، فكل شاشة
بتسأل «مين أنا؟» بترجع فاضي → «مش مورد على مضمونة».

⚠️ **ليه اتأخّر اكتشافه:** الفحص من لوحة Supabase بيتم بدور
`postgres`، وهو بيتخطى الـGRANTs. و`can_edit_supplier_listings()`
كانت بترجّع `true` لأنها SECURITY DEFINER (بتتخطى الصلاحيات
برضه). فكل الفحوص كانت تقول «سليم» والمورد متقفل فعلًا.

✅ **القاعدة:** لما مورد/مستخدم يقول «مش لاقي بياناتي»، افحص
`has_table_privilege('authenticated', ...)` **قبل** ما تدوّر في الكود
أو الـRLS. الـGRANT طبقة منفصلة فوق الـRLS — لو الـGRANT مرفوض،
سياسات RLS مابتتقراش أصلاً.

🛡️ الأمان محفوظ: RLS مفعّل على الجدول، فالـGRANT بيسمح بالقراءة
**تحت السياسات بس** — كل مورد يشوف صفّه هو.
🔍 اتفحصت كل جداول public: مفيش جدول تاني بنفس العطل.

## ٤.٧) 🛡️ حمايات دائمة ضد تكرار الموردين (٢٨/٨)
محمد: «في ٢ تيكوود… المفروض يبقوا حساب واحد… وأكّدلي إن
المشكلة دي مش هتتكرر مع مستخدمين تانيين».

⚠️ **جدولين موردين بنفس الـid:** `suppliers` و`marketplace_suppliers`.
الحذف من واحد كان بيسيب صف يتيم في التاني ← «مورد تاني من غير
صلاحيات». ✅ تريجر `sync_delete_to_*` بيمسح من الجدولين مع بعض.

⚠️ **تكرار الرقم بصيغتين:** `profiles.phone` عمود unique بس
`01104496225` و`+201104496225` صيغتين مختلفتين للنظام ← حسابين
لنفس الشخص، كل واحد مربوط بمورد مختلف.
✅ `phone_core()` + تريجر `block_dup_phone` بيرفض أي حساب جديد
   برقم موجود بصيغة تانية (مجرّب وشغّال).
   🤖 حسابات `agent-*` المؤقتة للسماسرة مستثناة (متعمدة).

📊 الفحص لقى **٢٧ تكرار** مش تيكوود بس. اتمسح **٦ فاضيين تمامًا**
(صفر موردين/أوردرات/رسايل + عمره ما سجّل دخول). الباقي عليه
بيانات فمايتمسحش — التريجر بيمنع الجديد وبيسيب القديم.

## ٤.٨) 🔗 دمج حسابات السكرابر المؤقتة (٢٨/٨)
🔍 **مصدر المشكلة:** السكرابر بيعمل حساب مؤقت `agent-01xxx`
لكل سمسار. وبعدين السمسار يسجّل بنفسه بـ`+201xxx` ←
**حسابين وموردين مختلفين وإعلانات متفرقة** — وقع فيها ١٤ مورد.

✅ اتدمجوا: الإعلانات والحجوزات والأوردرات راحت للمورد الحقيقي،
والمورد المؤقت اتعلّم `rejected` (مااتمسحش — أثر محفوظ).

💡 **درس:** ٤ إعلانات كانت مكررة **حرفيًا** عند الطرفين.
النظام فيه آلية جاهزة للحالة دي: `listings.dup_quarantine_of`
(حجر المكرر مربوط بالأصل) — استخدمها بدل الحذف.
وتريجر `prevent_duplicate_listing` بيمنع أي إعلانين بنفس
العنوان لنفس المورد — وهو اللي وقف الدمج وكشف المكرر.

🛡️ **منع التكرار:**
- `resolve_profile_for_phone()` — بترجّع البروفايل الصح لأي رقم
  (الحقيقي قبل المؤقت). أي سكرابر أو كود جديد يناديها قبل
  ما يعمل حساب.
- `v_duplicate_accounts` — ويو تنبيه: **لازم يفضل فاضي**.
  أي صف فيه = مورد ممكن يشوف «مش مورد على مضمونة».

⚠️ مصدر حسابات `agent-*` **مش في الكود** — سكريبت خارجي أو
إدخال يدوي. عشان كده الحماية اتحطت في **الداتابيز** — بتشتغل
مهما كان المصدر.

## ٤.٩) 🔴 إغلاق أمني عاجل — ٧٩ جدول مكشوف (٢٨/٨)
محمد: «شوف الجداول اللي عندنا مكشوفة ولا لا علشان ده غلط دايمًا بيحصل».

🚨 **الفحص كشف ٧٩ جدول** الدور `anon` يقدر **يقراهم ويعدّلهم**
من غير أي RLS. ومفتاح anon **موجود في كود الموقع** — يعني أي حد.

الأخطر اللي كان مكشوف:
- `app_secrets` — **مفاتيح وأسرار النظام**
- `madmona_otp_codes` · `owner_otp_codes` · `password_reset_tokens`
  — **أكواد الدخول وتغيير الباسورد** (دخول بأي حساب)
- `platform_admins` · `platform_admin_sessions` — حسابات وجلسات الأدمن
- `payroll_items` · `commission_entries` — مرتبات وعمولات الموظفين
- `ai_billing_invoices` · `anthropic_requests` — فواتير واستهلاك API
- `clinic_leads` · `customer_email_outbox` — بيانات عملاء

✅ اتقفلوا كلهم (revoke + enable RLS). **صفر مكشوف دلوقتي.**
الاستثناء الوحيد: `districts` (٤٧ حي) — مرجعية عامة للقراءة فقط.

💡 **ليه التطبيق ماتأثرش:** السيرفر بيستخدم `service_role`
اللي بيتخطى RLS والصلاحيات. اتأكد بعد القفل: كل الصفحات
بترد 200 والإعلانات ظاهرة.

⚠️ **قاعدة دائمة:** أي جدول جديد → `enable row level security`
فورًا وماتديهوش لـanon إلا لو فعلًا عام. وافحص دوريًا بـ:
`has_table_privilege('anon', 'public.X', 'SELECT')`

## ٥) رقم استقبال ملفات العملاء
**٠١٠٠٢٢٢٩٩٨٢** (`INTAKE_WA` في `src/lib/crmScripts.ts`).
في الاسكريبتات: **متبدأش ببيزنس المطعم**.

## ٦) نماذج العروض
الوحيد الباقي: `/s/demo-clinic` (عيادة = موديل حجز خدمات فعلاً).
باقي النماذج (سيارات/مصنع/متجر/عقارات) **اتمسحت يوم ٢٤/٨** — صفحة
النموذج شاشة خدمات وأقسام وعرضت العربية «بمدة ٦٠ دقيقة» كأنها جلسة
سبا. محمد: «بيانات لا تصلح لموديل السيارات». متعملهاش تاني —
النشاطات دي بتتعرض في الماركتبليس نفسه.

## ٧) حاجات بتكسر على ويندوز
- **PowerShell بيبوّظ العربي في الأوامر** → اكتب الرسالة في
  `.commitmsg.txt` واستخدم `git commit -F`.
- **`move` مابيعرفش الأسماء العربية** → سكريبت Node.
- **`cmd /c "... & echo END"` بيرجع فورًا** → وجّه لملف، استنى، بعدين اقرا.

## ٧.٥) معرض ولا فرد — seller_kind
كل إعلان ليه `listings.seller_kind`: `business` (معرض/نشاط ← تعامل
B2B) أو `individual` (فرد). تريجر `trg_listings_seller_kind` بيملاها
تلقائي من `derive_seller_kind()` لأي مسار إدخال. **أي فيتشر جديد
يتعامل مع إعلانات لازم يفرّق بينهم.**

> ⚠️ قاعدة محمد: «التعديل يتعمل على مستوى قاعدة البيانات كاملة مش
> جدول و١٠ لأ» — أي عمود/مفهوم جديد لازم يتحط في: الجدول الأساسي +
> المسودات (listing_drafts + instant_listing_drafts) + الـRPCs +
> التريجرز + الـUI في نفس التعديل. مفيش نص شغل.
> وكمان: «أي تعديل = كوميت + بوش + ديبلوي فورًا» — مش في آخر الجلسة.
> و«أي تعديل لازم يكون **موثّق** بحيث مايتعارضش مع أي جلسة تانية،
> والتاريخ معروف لكل الجلسات» — يعني: رسالة كوميت بتشرح الجذر،
> وتعليق في الكود بتاريخ وكلام محمد نصًا، وسطر هنا لو قاعدة جديدة.

## ٤.٢) قواعد اتقفلت خلاص — متفتحهاش تاني
- **📋 «دروب ليست» = السبسيفيكيشن مش التصنيفات (٢٧/٨):** محمد: «شيل موضوع
  اختار نشاطك من القايمة اللي في الإضافة — لما قلت دروب ليست كان قصدي
  السبسيفيكيشن والتفاصيل اللي جوّه إضافة المنتج زي الماركة والموديل، بس
  على مستوى الأبليكيشن». القايمة المنسدلة للتصنيفات في `/add-listing`
  **اتشالت خالص** والاختيار بقى من الكروت بس — **متضفهاش تاني**. المطلوب
  بدلها: كل حقول التفاصيل (ماركة · موديل · حالة · خامة …) تبقى قوايم
  منسدلة مش كتابة حرة، مع «أخرى…» اللي بتفتح خانة كتابة. الماركة اتعملت
  (`BRANDS_BY_FAMILY` + `brandFamilyFor`)؛ باقي الحقول تتحوّل بالتدريج
  عبر `attributes.options` في الداتابيز.
- **🧞 المارد = توجيه وترشيح فقط، ومية بيضا (٢٧/٨)**: محمد «المارد عاكك
  الدنيا معايا… ميبدأش كلام من نفسه ويقتصر شغله على التوجيه والترشيح فقط لا
  غير. شيل كل الداتا القديمة وأسلوب الشغل القديم — لو احتجت حاجة هبنيها من
  الأول». اللي اتعمل:
  · البرومبت اتكتب من الصفر (`customer-concierge.ts` — ٧٦ سطر بدل ٦٢٦):
    يرد بس لما حد يكلّمه، يرشّح إعلانات، يحوّل للمورد، يوجّه للينكات.
  · الأدوات المفتوحة **٦ بس**: search_catalog · list_categories · who_is_this
    · get_my_orders · forward_to_supplier · record_unmet_demand. الباقي
    (١٨ أداة) مقفول في `marid_tool_settings` بتعليمة توجيه بديلة.
  · كل جوبز المارد اللي بتبدأ كلام أو تنشئ محتوى وقفت: sync_bourse_to_ads ·
    rescue-missed-listings · listing_draft_nudges · meeting-reminders ·
    marid_listing_completion_request · request_missing_project_info ·
    marid-restaurant-agent · marid-blog-writer · bourse_cover_cards ·
    marid_project_change_digest. الشغّال: الحرّاس والبنية بس.
  · الداتا القديمة (marid_notifications · agent_insights · agent_directives)
    اتمسحت — نسخة في `_backup_marid_reset_20260827`.
  **ممنوع** أي جلسة تفتح أداة أو جوب من دول أو تزوّد البرومبت — أي قدرة
  جديدة للمارد تتبني من الأول بأمر محمد نصًا.
- **🗂️ ترجمة داتا الداتابيز (٢٧/٨)**: `categories.name_i18n` /
  `group_name_i18n` {en,uk,ru,ja} (٤٠٠/٤٠٠ مترجمين — المصدر
  `sql/2026-08-27_category_i18n.cjs` + `scripts/apply-category-i18n.mjs`)
  و`listings.i18n` {lang:{title,description}} بتتولد بـ
  `scripts/translate-listings.mjs` (هايكو) وبتتمسح بتريجر لو الأصل اتغيّر.
  الفرونت يقرا عبر `src/lib/i18n/catName.ts` (`catNameFor` · `groupNameFor` ·
  `listingTitleFor` · `listingDescriptionFor`) — ممنوع `name_ar`/`title`
  مباشرة في العرض. ⚠️ **الترجمة بتتعمل في الشات مش بالـAPI** (٢٧/٨ — محمد:
  «مش عايز آخد من رصيد الأنثروبك»): أي إعلان جديد ترجمه بنفسك هنا واكتبه في
  ملف JSON وارفعه بـ`node scripts/apply-listing-i18n.mjs ملف.json` (صفر نداءات
  AI). سكريبتات translate-listings / translate-categories-lang بتستهلك API
  credits — **ممنوع** تشغيلها من غير إذن محمد نصًا.
- **🌍 ٧ لغات في الفرونت (٢٧/٨)**: مصري (الافتراضي) · خليجي · English ·
  Українська · Русский · 日本語 · 中文 (zh). لغة جديدة = ٤ خطوات: `Locale` +
  `LOCALE_META` في dictionary.ts · `locales/xx.ts` · خريطة `H` في
  layout.tsx · `scripts/translate-categories-lang.mjs --lang xx` +
  `scripts/translate-listings.mjs --langs xx`. النظام هو `LanguageProvider` الموجود
  (كوكي/localStorage `madmona_lang`) — **مفيش** `[locale]` في الراوتات ولا
  middleware لغات. القواميس: `src/lib/i18n/dictionary.ts` (مصري + إنجليزي
  كاملين) + `locales/{ar-gulf,uk,ru,ja}.ts`. أي نص جديد في الواجهة =
  مفتاح `t()` في الستة، **ممنوع** `en ? '..' : '..'` ثنائي. سلسلة الرجوع:
  اللغة → العائلة (ar/en) → المصري → المفتاح.
- **🧹 الكووركينج اتشال من ملفات الأبليكيشن نهائيًا (٢٧/٨)**: محمد «المشروع
  مبقاش كووركينج». `/coworking` و `/reserve/*` بقوا redirects في
  next.config بس. تصنيفات `*-coworking-suite` و`workspaces-hot-desk`
  اتقفلت (is_active=false). «مساحات عمل» (قاعات اجتماعات/تدريب/مكتب)
  فضلت كفئة تأجير عادية — مش كووركينج. متضيفش أي محتوى كووركينج تاني.
- **اسطمبات البيزنس** في `src/lib/erpModules.ts` (مصدر واحد):
  صالون/سبا · عيادة · مطعم · مقاولات · معارض عربيات · **عقارات
  (real_estate — اتضافت ٢٤/٨)**: الوحدات (صفحة جديدة) + المشاريع
  والتحصيل والجدول الزمني والربحية والحجوزات (إعادة استخدام صفحات
  المقاولات — مفيش نسخ موازية). نموذج العرض: «رويال إستيت (نموذج)»
  `/admin/business-finance/2ad0cab4-7c50-4968-8277-00952c935388`
  — theme فاضي عن قصد (محمد: تصميم الديسكتوب من غير ألوان متخزنة).
- **تاسكات المبيعات اليومية**: `generate_sales_daily_tasks()` عبر
  orchestrator job `sales-daily-tasks` (state-based: بعد ٩ صباحًا +
  فيه مندوبة من غير تاسكات). ٤ تاسكات: مكالمات (بعدد قايمتها الفعلي)
  + متابعة المستحق + تسجيل النتايج + **تاسك ماركتنج بيلف بأيام
  الأسبوع**. الموزّعين مستثنيين.
- **تريجر `trg_daily_tasks_dedupe`**: نفس الموظف+اليوم+العنوان =
  مفيش إدخال تاني حتى لو الأصلي مقفول — التاسك المقفول مايتولدش تاني.
- **جدول فريق مضمونة اليومي (٢٥/٨)**: ٩ ص ← ٦ م، الأحد–الخميس،
  راحة ١–٢. مصدره **`recurring_task_templates`** (سطر لكل تاسك
  بميعاده) وبيتولّد كل يوم من جوب `generate-recurring-tasks`
  (اتفك قفلها من قفل «فئة work» ٣/٨).
  🕘 **المرساة (٢٥/٨ مساءً)**: محمد: «بلان بالوقت على حسب معاد تسجيل
  الدخول». `anchor_mode='clockin'` (الافتراضي لفريق مضمونة) = التاسك
  بتتولد لحظة أول بصمة عن طريق `trg_tasks_on_clock_in` وميعادها =
  الحضور + `offset_minutes` بسقف ١٧:٤٥. `'fixed'` = ميعاد حائط
  (الراحة ١:٠٠). الجوب اليومي بيولّد الثابتة **بس**. الأقسام في
  `business_employees.metadata->team` (مبيعات · دعم فني · أوفيس ·
  إشراف · الإدارة) — الجدول والمونيتور بيقروا منها.
  📺 **المونيتور** (`/admin/business-finance/[id]/monitor`): صورة
  اليوم الحية من `get_team_monitor()` — حاضر/غايب · إنجاز · المتأخر ·
  التاسك الحالية والجاية. بيتحدث كل دقيقة.
  🧰 **أي شاشة جديدة في اللوحة تنادي الداتابيز عن طريق
  `src/lib/financeRpc.ts`** — fetch مباشر (مفيش supabase-js بسبب
  قفل navigator.locks في الـPWA) + p_token تلقائي.
  📋 **التاسكات تفصيلية حية (٢٥/٨ مساءً)**: `task_dynamic_detail()`
  بتتحقن في description وقت البصمة — ☎️ أول ١٢ رقم من قايمة المندوبة
  الفعلية · 📣 اسكريبت بوست جاهز من أحدث إعلان حقيقي (النماذج
  مستبعدة) · 🔍 المستني مراجعة بأسمائه. الفاصل `⸻` بيفصل الثابت عن
  الحي — `refresh_task_details_today()` بتستبدل الحي بس. شاشة «شغلي»
  فيها زرار «التفاصيل» (get_my_work_home بقت بترجّع description).
- **🔔 التاسكات مسموعة (٢٥/٨ مساءً)**: محمد: «خانة الاسكدجوال هي اللي
  مسمعة + تاسك من مكالمة محتاجينه يسمع». ٣ مصادر پوش (notification_queue
  → كرون البوش كل دقيقة): تاسك يومية جديدة (`notify_employee_new_task`
  اتوسعت) · تاسك من مكالمة (`trg_notify_new_flow_task` على flow_tasks —
  أول إشعار ليها) · ميعاد التاسك وصل (`notify_due_tasks()` عبر job
  `task-due-notify`، نافذة ١٥ دقيقة + dedupe بـtask_id في
  notification_queue). اتجرب بمسار حقيقي: تاسك اختبار لمحمد → الپوش
  اتبعت فعلًا. ⚠️ ٤ موظفين لسه مافعّلوش الإشعارات على موبايلاتهم
  (سامية · نورا · شهد · الأوفيس بوي) — مايوصلهمش صوت لحد ما يفعّلوا.
- **🔁 دورة السوشيال مرتين يوميًا (٢٥/٨)**: سامية بتنشر (بوست الصبح +
  بعد الضهر بالاسكريبت المعتمد من social_packs)، **وكل** الباقيين —
  الأوفيس والإدارة كمان — عندهم تاسك شير ١٤:١٥ و١٧:١٥ (بروفايل شخصي +
  جروب على الأقل + رياكشن).
- **🚪 دخول الفريق للوحة /admin (٢٥/٨)**: «الحساب ده مش موجود» عند شهد
  = مفيش صف في `platform_admins` (باب اللوحة بكوكي + باسورد، منفصل عن
  جلسة الأبليكيشن). الستة اتسجلوا بدور **`staff`** — دخول بس، من غير
  قوة `is_admin()` (القوة من صلاحيات كل واحد). الباسورد = نفس باسورد
  الأبليكيشن (التريجر `sync_employee_password_to_platform_admin`
  بيزامنه؛ الصف لازم يكون موجود الأول عشان المزامنة تلاقيه). اللي لسه
  مالوش باسورد صفّه فيه `LOCKED-…` — بيتفك أول ما الباسورد يتظبط من
  شاشة حسابات الموظفين. الإيميل الداخلي `<رقم>@staff.madmonacairo.com`
  قيد جدول بس — الدخول بالتليفون.
- **🔒 شات الاستفسار: الأسماء والأرقام مخفية لحد الاتفاق (٢٧/٨)**:
  محمد: «مش هنعرض الاسم ورقم التليفون إلا لما يتم الاتفاق في حضور
  مضمونة». ده **مكمّل** لـ`partyPhoneHidden` تحت (اللي بيحمي صفحة
  الإعلان) — ده بيحمي **جوّه الشات نفسه**:
  · `chat_rooms.masked_identities` = true لكل روم استفسار ثلاثي.
  · `chat_room_members.role`: `admin` (فريق مضمونة) · `owner_party`
    (صاحب الإعلان) · `customer` (العميل) — الفرونت بيحوّلهم لألقاب.
  · `chat/team` بيعرض اللقب بدل الاسم في اسم المرسل واقتباس الرد،
    والرسايل الخارجة بتتكتب باللقب.
  · بيانات الطرفين الحقيقية للفريق بس (روم الفريق + `listing_inquiries`).
  • فك الإخفاء بعد الاتفاق = `masked_identities = false` (زرار الواجهة
    للفريق جوّه `chat/team` — مش كومبوننت منفصل).
  • فلتر الأرقام جوّه نص الرسالة: `src/lib/mask-contacts.ts`
    (موبايل · أرضي · إيميل · معرّفات سوشيال · أرقام عربية-هندية).
    ⚠️ حاجز مش قفل محكم — حد مصمّم يقدر يكتب رقمه بالحروف.
  ⚠️ **أي شات جديد بين طرفين في قسم بيع لازم يحترم نفس الفلاج.**
- **🔒 بيع العقارات والعربيات: مفيش أرقام موردين للعميل (٢٥/٨)**:
  محمد: «التواصل يفتح في شات مضمونة مع الطرفين — مش دايركت».
  `partyPhoneHidden` في صفحة الإعلان (sale-properties · sale-vehicles ·
  sale-tourism): كل أزرار الواتساب/التليفون بتفتح على رقم مضمونة
  (INTAKE ٠١٠٠٢٢٢٩٩٨٢) ورقم الفرع مخفي. **أي CTA تواصل جديد في
  القسمين دول يحترم نفس الفلاج.** الأقسام: مبيعات · دعم فني ·
  إدخال بيانات/تشغيل · إشراف وتوزيع · المنصة. المحتوى والسوشيال
  مكتوب ومتقفول في `sql/2026-08-25_madmona_daily_schedule_9_6.sql`
  لحد ما يتحدد مين شايله. **محمد عبدالجابر أوفيس بوي** (مش إدخال
  بيانات) — قسم إدخال البيانات والتشغيل لسه من غير حد.
  التعديل بيتعمل من شاشة **«جدول التاسكات اليومي»**
  (`/admin/business-finance/[supplierId]/schedule`) مش من SQL:
  ميعاد · عنوان · شرح · أولوية · أيام · إيقاف بدل المسح · وزرار
  «ولّد تاسكات النهارده». تاب «المهام» بيعرض تاسكات يوم بعينه، ده
  بيعرض القالب اللي بيتولّد منه.
  🔐 كل دوال `recurring_task_*` بتفحص `can_manage_business_team()` —
  كانت SECURITY DEFINER مفتوحة لأي حساب مسجّل بأي `supplier_id`.
  ⚠️ درس (٢٥/٨): الحارس ده بيمرّ لـ`platform_admins` أو لمين عنده
  `can_manage_team` — فأول ما اتحط، الشاشة قفلت في وش كل الفريق
  وحساب محمد الشخصي (`moh91arabco@gmail.com` دوره `supplier` مش
  `admin`؛ الأدمن هو `madmona@madmonacairo.com`). **أي حارس جديد
  على شاشة بيستخدمها الفريق، جرّبه بجلسة موظف حقيقي قبل ما تقول
  خلصت** — `set_config('request.jwt.claims', ...)` وبعدين نادي الدالة.
  ⚠️⚠️ **الدرس الأكبر (٢٥/٨ — بعد ٣ محاولات إصلاح):** لوحة
  business-finance ليها **نظامين دخول**: جلسة Supabase (auth.uid)
  **وتوكن واتساب** `madmona_token` (جدول `madmona_sessions`) — وده
  اللي محمد وأصحاب البيزنس بيدخلوا بيه، من غير أي جلسة Supabase.
  أي RPC جديدة لشاشة جوّه اللوحة **لازم** تقبل `p_token uuid` وتفحص
  بـ`schedule_access_ok()` / `schedule_edit_ok()` (بيقبلوا النظامين؛
  التوكن بيتفحص بمنطق `admin_check_finance_access` نفسه). حارس
  بيفحص auth.uid() بس = الشاشة شغالة لموظفي مضمونة ومقفولة في وش
  محمد وكل أصحاب البيزنس.
- **صلاحيات فريق مضمونة (٢٥/٨)**: نورا · مديحة · عبير · شهد · ايمان ·
  سامية عندهم صلاحيات الأدمن الكاملة **ما عدا الفلوس والتسعير**
  (`can_view_finance` / `can_manage_pricing` = false لحد أمر صريح).
  محمد: «عايز صلاحيات للتيم إنه يكون أدمن ويقدر يضيف إعلان لأي مورد
  أو بيزنس». ده بيشتغل عن طريق `is_madmona_staff()` جوّه
  `can_edit_supplier_listings()` — **مش** بتغيير `profiles.role` لـ
  `admin` (ده بيفتح `is_admin()` على المنصة كلها بما فيها الفلوس).
  الأوفيس بوي مستثنى. نورا كانت `permissions = {}` فاضية تمامًا.
- **ليه الإعلان بيفضل درافت**: `ListingForm` بيعمل الإعلان
  `status='draft'` بالتصميم، والنشر خطوة تانية بتعدّي على تلات
  تريجرز: صورة واحدة على الأقل · سعر (ما عدا العقارات) · **إثبات
  رقم الواتساب** (`phone_verified_at` أو رسالة واردة من نفس الرقم أو
  `publish_exception_until` ساري). قياس ٢٥/٨: من ١٩٦ درافت، ١٨٣
  من غير أي صورة و١٨٢ رقمهم مش متأكد — واحد بس كان هينشر لو دُس نشر.
- **⚡ أداء `listings` (٢٥/٨)**: بوليسي القراءة بتنادي ٦ دوال لكل صف.
  `can_edit_supplier_listings()` بتخرج فورًا لو `auth.uid()` فاضي
  (الزائر مالوش أي فرع يعدّي منه) وكوستها 500 عشان المخطّط يأجّلها:
  صفحة ٢٤ إعلان من 438ms/26,470 بافر → 46ms/6,513. **أي دالة
  جديدة في بوليسي على جدول كبير لازم يكون ليها خروج مبكر للزائر.**
- **🕘 الحضور مايتقفلش بالسكوت (٢٥/٨)**: محمد: «السيستم بيخلي الناس
  تنصرف وهما لسه في الشغل». الداتا: **١٠٠٪** من الانصرافات في ٤ أيام
  كانت `auto_offline` ولا واحدة `auto_app_exit` — يعني مسار «خرج من
  الفرع» عمره ما اشتغل، السكوت دايمًا بيسبقه. نبضة صفحة ويب على
  موبايل في جيب مستحيل تفضل شغالة. القواعد دلوقتي:
  `auto_clockout_offline_sessions` بتقفل عند **نهاية الوردية**
  (`supplier_branches.closes_at` + ٣٠ دقيقة سماح) أو بعد **٤ ساعات**
  سكوت — مش ٣٠ دقيقة؛ و`employee_auto_attendance` **بتفتح نفس
  الجلسة تاني** لو الرجوع في نفس اليوم وخلال ٤ ساعات من قفل
  أوتوماتيك، بدل ما تعمل صف جديد (نورا ٢٤/٨ كان عندها ٧ جلسات
  مقطّعة = ٣.٥ ساعة في يوم كامل). ورسالة الواتساب بقت للجلسة
  المنسية بس، مش لكل قفل يومي.
- **🖼️ الصور الواقعة (٢٥/٨)**: ٦٠٨ ملف في `content-images` محتواهم
  **SVG** (كروت بلايس-هولدر بعنوان الإعلان، من مسارات الـrehost
  والبوستات لما الصورة الأصلية تفشل) متسمّيين `.jpg`. الـoptimizer
  بيرفض SVG افتراضيًا بـ400 — ٢٤ إعلان منشور صورهم كانت مكسورة
  والملف نفسه 200. الحل: `dangerouslyAllowSVG` + CSP معقّمة
  (`script-src 'none'; sandbox`) في next.config.mjs. اتأكد: ٢٥/٢٥
  رابط صورة من الصفحة الجديدة = 200 (كانوا ٧/٢٥).
  الروابط القديمة بـ`dpl` قديم هتفضل 400 لحد ما الصفحة تتحمّل تاني —
  ده طبيعي، الـHTML مش متخزّن (SW v8+).
  + صور S3 القديم وحساب كلاودينري `dqott4uei` **ماتوا نهائيًا** —
  صفوفهم اتشالت والتلات شقق اللي كانت كل صورهم من هناك اتوقفوا
  (paused) لحد ما صور حقيقية تترفع. حساب `duxfgqioc` (أبيكس/Elite) حي.
- **📡 SW v9 (٢٥/٨)**: النت يفصل → `/offline.html` (ثابتة، بترجع
  لوحدها مع حدث `online`) بدل «This site can't be reached».
  ضمانات v8 زي ما هي: HTML عمره ما بيتخزّن. أي تعديل في سلوك
  الكاش يفضل محافظ على الاتنين: لا نسخة عالقة، ولا صفحة خطأ خام.
- **مورّد المنصة مستثنى من مولّدي التاسكات العامة** في المسارين:
  `generate_daily_tasks_pulse()` (قوالب الأدوار) و
  `/api/agents/generate-tasks` (وكيل الـAI) — القوالب دي مكتوبة
  لمحلات وصالونات (مخزون · تنضيف محطة) مش لفريق منصة. عملاء B2B
  زي ما هما. أي تعديل على جدول الفريق يتعمل في القوالب مش في
  `daily_tasks` مباشرة.
- **حالة قفل التاسك اليومي = `completed` مش `done`** (`done` دي
  حالة `flow_tasks`). عدّاد `tasks_done_today` في `get_my_work_home`
  كان بيعدّ `done` فكان دايمًا صفر — اتصلح ٢٥/٨ ليقبل الاتنين.
  أي عدّاد جديد على `daily_tasks` يستعمل `in ('completed','done')`.
- **`wa_inbound_watchdog` بيقرا حالة الجلسات من OpenWA مباشرة**
  (pg_net على `/api/sessions` — المفاتيح في whatsapp_config:
  openwa_url/openwa_api_key). قاعدة محمد: «التقارير تقرا من داتا
  حية مش جداول ميتة».
- **مبدأ UI**: أي اختيار نوع بيزنس/تصنيف في أي شاشة إضافة يبقى
  **دروب ليست** مش جريد طويل (لسه ناقص تطبيقه في ويزارد /add-listing).
- **قنبلة completed_by اتفكت (٢٤/٨ بليل)**: daily_tasks.completed_by
  FK على auth.users مش business_employees — الدالة كانت بتكتب صف
  الموظف فكل قفل تاسك يومي كان بينفجر FK من يوم ٢٢/٨. الصح:
  completed_by = auth.uid(). اتجرب بجلسة عبير على تاسك حقيقي واتقفل.
  درس: جرب بجلسة المستخدم الحقيقية قبل ما تقول اتصلح.
- **ملكية المهمة = `is_my_flow_task()` بس** (عرض وقفل). أي تعديل
  على «مين المهمة بتاعته» يتعمل جوّاها هي — مش نسخة جديدة.
- **المارد بيقرا آخر ٣ رسايل بس** (منّنا أو من العميل) — بريف ١٨٠ حرف.
- **الميديا بتتفرّغ أول بأول**: صوت عبر Groq، صورة/PDF عبر نداء هايكو
  صغير بيكتب الوصف جوّه سجل الرسالة (`enrichMediaTranscript`).
- **سامية**: أدمن ماعدا الإداري والحسابات (فلوس/تقارير/تسعير/فريق/
  فروع = false صراحةً في business_employees.permissions).
- **إشعارات الحضور/الانصراف**: تريجر trg_attendance_notify على attendance_logs — أي مسار تسجيل بيبعت push. المستلمين في whatsapp_config: attendance_notify_profile_ids (مفصولين بفاصلة).
- **زوم iOS مقفول** (maximumScale:1 + خانات الموبايل 16px) — أي
  input جديد ماينزلش تحت 16px على الموبايل.

## ٧.٩) قاعدة قفل المشاكل — أمر مباشر من محمد (٢٤/٨)
> «لما أقولك على مشكلة، اقفلها من **كل** الاحتمالات — متسيبش احتمال
> واحد ينفع يكون هو السبب.»
يعني: (١) احصر كل المسارات اللي بتعمل نفس الفعل (دوال، راوتات،
شاشات، تريجرز). (٢) جرّب كل مسار بجلسة المستخدم الحقيقية مش
بالقراءة. (٣) اللي فيه نفس العيب اصلحه حتى لو محدش اشتكى منه.
(٤) وثّق نتيجة المسح.
أول تطبيق: مسح الـ١٣ دالة قفل تاسكات — طلع قنبلة FK تانية في
employee_complete_task_by_pin (مسار البصمة) واتفكت، والباقي
اتفحص واتجرب: complete_my_task (daily+flow) ✓ بالبصمة ✓
crm_task_update ✓ flow_task_set_status ✓ التوجلز ✓.

## ٨) طريقة الشغل
- **اشتغل، متسألش كتير.** لقيت مشكلة؟ صلّحها.
- **متخلّيهوش يجرّب مرة ورا مرة** — شخّص وأصلح وقوله اتعمل إيه.
- **اتأكد من الديبلوي بنفسك** — هات `/_next/static/chunks/*.js`
  ودوّر على كلمة من التعديل.
- **غلطت؟ قول غلطت وصلّحها — ومتكرّرهاش.**
- بعد الخلاص: سطرين. مش تقرير.

---
---

# قواعد العمل على مضمونة

> ## 🔒 حالة تشغيل قائمة — ٣ أغسطس ٢٠٢٦
>
> **٤١ وظيفة في فئة `work` واقفة ومقفولة بأمر محمد.** مايتشغّلوش إلا بأمر صريح منه.
> القفل بترايجر `trg_block_locked_job_enable` + جدول `orchestrator_job_locks` —
> أي محاولة `update orchestrator_jobs set enabled = true` هتفشل.
>
> **ممنوع تشغيلهم من نفسك مهما بدا السبب وجيه.** لو شفت وظيفة واقفة وانت شايف
> إنها لازم تشتغل، اسأل محمد — ماتفكّهاش.
>
> التشغيل: `select public.unlock_orchestrator_job('job_key');`
> التفاصيل: `docs/sessions/2026-08-03-work-jobs-locked.md` ·
> `system_runbook` topic = `orchestrator-work-jobs-locked`
>
> آثار مقصودة: `meeting-reminders` واقف (تذكيرات المواعيد مش بتتبعت) ·
> `snapshot-daily-kpis` واقف (`daily_kpis` مش بيتسجّل) · `olx-scrape-d1..d5` واقفين.
>
> شغّال عادي: الواتساب بيوصّل والمارد بيرد على العملاء.

> اتفاق مع محمد — ١٩/٢٠ يوليو ٢٠٢٦

## 📌 سجل الاتفاقات

**القاعدة صفر: أي حاجة نتفق عليها في الشات تتكتب هنا فورًا.**
مش في آخر الشغل ولا لما أفتكر — **وقتها**. الاتفاق اللي مش مكتوب
بيتنسى، وبنغلط فيه تاني، ومحمد بيضطر يكرّره. ده حصل فعلًا:
اتفقنا نسجّل الاتفاقات ومانفذتش، فمحمد اضطر ينبّهني.

| الاتفاق | التاريخ |
|---|---|
| أي داتا تتبعت للمارد (نص · صورة · PDF · **لينك**) تتفرّغ وتتفهم ويترد عليها | ٢٠ يوليو |
| المارد يعمل الشغل بنفسه — مايقولش «سجّل من اللينك ده» | ٢٠ يوليو |
| العمولة **١٠٪** بس، مفيش اشتراك ولا رسوم تسجيل — ⚠️ **بتتحط فوق سعر البايع مش منه** (شوف قاعدة العمولة فوق) | ٢٠ يوليو |
| المطاعم: نظام طلبات · بنرشّح المسجّلين · داش بورد من تاب حسابي · عرض ربط السيستم | ٢٠ يوليو |
| الرد يخرج من **نفس الرقم** اللي جت عليه الرسالة | ٢٠ يوليو |
| ممنوع بدء محادثات جديدة — `MARID_REPLY_ONLY` | ٢٠ يوليو |
| أي حركة غير طبيعية تتعالج **فورًا** مش بعد ساعات | ٢٠ يوليو |
| الشريك/المورّد المعتمد مايتعرّفش بمضمونة تاني | ٢٠ يوليو |
| لينك بورصة مضمونة العقارية إجباري لأي مطوّر أو سؤال عن عقار | ٢٠ يوليو |
| كل الأعضاء أدمن في جروب المورّد | ٢٠ يوليو |
| الناس تحس إنها بتكلّم بني آدم — ممنوع «أنا مساعد ذكي» | ٢٠ يوليو |
| **ممنوع نطلب داتا مرتين** — الطلب التاني معناه إننا ضيّعناها | ٢٠ يوليو |
| **ماتقولش «اتعمل» غير بعد إثبات** — الملف اتحفظ + الكوميت في `git log` + اتعمل push. التعديل المحلي لوحده مايكفيش | ٢١ يوليو |
| **المشروع واحد — دايمًا بأفضل شكل** — مفيش «ده مش من تعديلي» ولا «ده خطأ قديم». أي حاجة تشوفها بتضر تجربة المستخدم، اعتبرها مسؤوليتك وصلّحها أو نبّه عليها | ٢١ يوليو |
| **توثيق الرقم = الوارد بس** — مضمونة مابتبعتش أي كود بارد (حماية من الحظر). العميل يبعت `MADxxxxx` للمارد والرقم يتأكّد من مصدر الرسالة. دخول جوجل ياخد إيميل+اسم بس ويوثّق الرقم بالوارد. القرار المقفول كامل في `DECISIONS.md` | ٢٣ يوليو |
| **📁 مكان الريبو المحلي على جهاز محمد: `E:\madmona-app`** (على درايف E — **مش** على C). أي شغل محلي أو Desktop Commander يشتغل هنا | ٢٣ يوليو |

### 🎯 المشروع واحد — ملكية كاملة للتجربة

محمد: «أنا بتكلم عن تجربة المستخدم بشكل عام. مفيش حاجة اسمها ملهاش علاقة بتعديلي».

ممنوع التفكير المجزّأ. لو لقيت خطأ TypeScript قديم، لينك ميّت، شكل مش مظبوط،
أو أي احتكاك في التجربة — **مش «بره نطاقي»**. المشروع كله كيان واحد لازم يفضل
بأفضل شكل. صلّحها لو تقدر، أو على الأقل نبّه عليها بوضوح — بس ماتتجاهلهاش
وتقول «مش من شغلي».

### ✅ التحقق قبل ما تقول «اتعمل»

**«اتعمل» من غير دليل = ماتعملش.** حصل فعلًا: اتفقنا نشيل قسم كأس
العالم من الهوم بيدج، و`git log` أثبت إن `page.tsx` ماتلمسش خالص
من ٩ يوليو لحد ٢١ يوليو — الكلام اتقال والتعديل ماوصلش للـ disk.

**قبل ما تقول لمحمد إن حاجة خلصت، لازم:**
1. الملف اتحفظ فعلًا (مش بس ظهر في الشات).
2. `git log` بيوري الكوميت بتاعه.
3. اللي المفروض يبقى لايف اتعمله `push` — Vercel بينشر تلقائي،
   والتعديل المحلي لوحده **مايظهرش** على الموقع.

**⚠️ الـ Desktop Commander ممكن يفشل يتصل** وقت فتح الجلسة (timeout
٦٠ ثانية بسبب بطء إقلاعه — مؤكّد من اللوج). لو فشل، أي تعديل على
الملفات المحلية إما مايتنفّذش أو مايوصلش، والشات يبان كإنه اتعمل.
**لو DC مش راد، أعد فتح تطبيق Claude واتأكد إنه اتصل قبل أي تعديل.**
(اتطفت الـ telemetry بتاعته في ٢١ يوليو عشان يسرّع الإقلاع.)

### ⛔ «ابعتهالي تاني» = اعتراف بإننا ضيّعنا الداتا

الداتا اللي وصلتنا **ملكنا**. لو مش قادرين نقراها، المشكلة عندنا
مش عنده. نتصرف: نحوّل الـPDF لصور ونقراها بصريًا، ندوّر على
الملف في التخزين، نفتح اللينك، نستخرج من المحادثة نفسها.

الطلب التاني بيوصّل رسالتين: إننا مش منظّمين، وإن وقته مش مهم.
وكتير من الناس مابيردّش على الطلب التاني أصلاً.

## 🖼️ جودة الإعلان — واجهة المشروع

**الإعلان المكسور بيضرب الثقة في المنصة كلها، مش في نفسه بس.**
محمد: «مش شامم ريحة الجودة خالص ومش هعرف ابعت لحد جديد بالوضع ده».

| ⛔ ممنوع يتنشر | ليه |
|---|---|
| صورة تصنيف نمطية | ٨ مطاعم طلعوا بنفس الصورة بالظبط |
| بيزنس كارد كصورة إعلان | مش صورة المنتج ولا المكان |
| صورة مش ظاهرة/مكسورة | العميل بيفتح ويلاقي فراغ |
| صورة مكررة عبر إعلانات مختلفة | بيبان مزيّف |
| تصنيف غلط | معدات مطاعم كانت في «منتجات متنوعة» |

**القاعدة: العدد مالوش قيمة من غير جودة.** ٢٧ مطعم حقيقي
بصورهم أحسن من ٧٤ نصهم بصور نمطية.

### ⛔ بس الحساب مايتقفلش عشان الصورة

محمد: «مش معنى إن الصورة مش حلوة أو مش موجودة إننا نقفل
حساب مورد أو مطعم».

أنا عملت حارس بيمنع النشر لو الصورة ضعيفة — وده كان **غلط**.
المورّد اللي بعتلنا شغله يستاهل يظهر.

اللي بيضر: **الصورة النمطية اللي بتتكرر عبر إعلانات مختلفة**
(٨ مطاعم بنفس الصورة = شكل مزيّف). مش غياب الصورة.

الصح: الإعلان يظهر، والصورة الضعيفة تتعلّم (`quality_flag`)
عشان نستبدلها، ونجيب صورة من أي مصدر حقيقي — لوجوهم، موقعهم،
صورهم على الواتساب.

## ⛔ وعود المارد مايقدرش ينفّذها

**ممنوع يوعد بحاجة مش في إيده.** الوعد اللي مايتنفذش أسوأ من
«مش هينفع».

- ❌ **«هكلّمك على التليفون»** — المارد مابيتصلش. حصل مع مدير
  حاتي المجنون ٢٠ يوليو الساعة ١١:٠٦، والراجل استنى.
- ❌ **«هسجّل الأصناف من اللينك ده»** — لو مفيش أداة تفتح لينك.
  حصل مع Rino's Kitchen ٢٠ يوليو.
- ❌ **«تم التسجيل»** والصور ناقصة — الإعلان بيقف ومحدش يعرف.

## القاعدة الأساسية

**محمد بياخد قراراته بناءً على المعلومة اللي بديهاله. المعلومة الغلط = قرار غلط = ضرر حقيقي.**

فمسؤوليتي مش إني أرد بسرعة — مسؤوليتي إن اللي أقوله يكون **صح ومتحقق منه**.

## قبل ما أقول أي معلومة

**افصل بين "شفت" و"استنتجت".**

- ✅ «الرقم `NOT_VERIFIED` — جبتها من الـ API دلوقتي»
- ❌ «الـ API ماشتغلش ولا مرة» ← ده كان استنتاج من دليلين ضعيفين، وطلع غلط، وكلّفنا الرقم

لو مااختبرتش حاجة بنفسي، أقول: **«ده استنتاج، محتاج أتأكد»**. مش أقولها كأنها حقيقة.

## قبل ما أكتب كود

**اقرا الواجهة الفعلية — ماتفترضش.**

غلطة ٢٠ يوليو: كتبت `api/whatsapp/baileys/route.ts` بالتخمين من ملف تاني. أربع توقيعات غلط (`upsertConversation`, `logInboundMessage`, `getConversationHistory`, `callClaude`) → الويبهوك بيرمي 500 → رسالة أميرة ماردّش عليها.

**القاعدة:** أي دالة هستدعيها، أقرا تعريفها الأول. كل مرة.

## قبل ما أبني حاجة جديدة

**افحص اللي موجود.**

غلطة ٢٠ يوليو: بدأت مشروع Railway جديد من غير ما أبص على الـ ٤ مشاريع الموجودة. محمد هو اللي نبّهني.

## قبل أي إجراء غير رجعي

الحذف، إلغاء التسجيل، مسح داتا، تغيير على الإنتاج:

1. اتأكد من **السبب الحقيقي** — مش أول تفسير معقول
2. اعرض **الخسائر بالتحديد** — مش «ممكن تخسر حاجات»
3. لو الأدلة ضعيفة، **قول كده صراحة قبل التنفيذ**

## لما محمد يقول «اتصرف»

**يعني نفّذ فعلاً** — مش ترجع تسأل تاني. هو واثق في القرار التقني.

بس ده **مش تفويض إني أتصرف من غير ما أتأكد**. الثقة دي معناها إن عبء التحقق كله عليّا أنا.

## الاستثناءات — دايمًا أوقف وأسأل

- باسوردات، كروت دفع، أكواد OTP
- CAPTCHA / «Verify you are human»
- حذف بيانات مش راجعة
- إرسال رسايل لعملاء (النص لازم يتراجع الأول)

---

## دروس تقنية من ٢٠ يوليو

**١. `.vercelignore` كان بيحجب `/api/whatsapp/*`**
سطر `WHATSAPP` (المقصود بيه فولدر التوثيق) كان بيطابق `src/app/api/whatsapp/` كمان. الويبهوك كان **404 لشهور**. اتصلح بـ `/WHATSAPP`.
**الدرس:** لما مسار واحد بس يفشل والباقي شغال، شوف ملفات الاستبعاد قبل ما تشك في الكود.

**٢. الرقم إما على واتساب العادي أو على Cloud API — مستحيل الاتنين**
مفيش تطبيق ولا use case ولا مزود بيغيّر ده. لو عايز الاتنين → **رقمين**.

**٣. `callClaude` بتاخد نص بس**
`{systemPrompt, userMessage}` — مافيهاش `messages` array ولا بلوكات صور. الصور والـ PDF محتاجين تعديل عليها، مش شغالين تلقائي.

**٤. Claude مابيسمعش صوت**
التفريغ لازم مزود خارجي (Groq/Whisper).

**٥. Baileys محتاج بروسيس دايم**
Vercel serverless مايقدرش. عشان كده الخدمة على Railway بـ volume على `/data`.

**٦. واتساب بيبعت مُعرّف مخفي (LID) بدل الرقم**
`23889212117111@lid` — ده **مش رقم تليفون**. Baileys 6.7.9 مافيهاش أي طريقة
ترجّع الرقم منه. لو عيدت تركيبه لـ `@s.whatsapp.net` هتبعت لرقم مش موجود،
وBaileys هيقبله ويدّيك `wa_message_id` والرسالة تروح في الفراغ.
**القاعدة: رُد على نفس الـ JID اللي جت منه الرسالة — ماتعيدش تركيب أي مُعرّف.**

**٧. `wa_message_id` موجود ≠ الرسالة وصلت**
ده أخطر إشارة كاذبة قابلناها. لما محمد يقول «مارّدش» والداتابيز بتقول
«اتبعت» — الداتابيز مش دليل على الوصول. شوف الـ JID الأصلي.

**٨. أي سكريبت codemod لازم يحترم `'use client'`**
سكريبت `fix-localstorage.mjs` حط `import` فوق `'use client'` في ١٢ ملف.
الديركتيف لازم يبقى **أول سطر فعلي** — غير كده الملف يبقى مكوّن خادم
بيستخدم hooks، وnext build يقع. النتيجة: **٥ نشرات إنتاج فاشلة ورا بعض**،
وكل إصلاحات المارد فضلت مش منشورة، ومحمد بيقول «لسة مشتغلش» وأنا بختبر
كود قديم. اتصلح بـ `scripts/fix-use-client.mjs`.
**الدرس:** بعد أي سكريبت بيعدّل ملفات بالجملة — **اجري `npm run build`
واتأكد إن النشر نجح**، مش بس إن الكوميت اتدفع.

**٩. الكود القديم بيرد وأنا فاكره الجديد**
لما اختبار يرجّع نفس النتيجة القديمة بعد إصلاح — **مش دايمًا الإصلاح غلط**.
حط علامة تشخيصية في النص (زي `[صيغ:… صفوف:…]`)؛ لو ماظهرتش يبقى النشر
هو المشكلة مش الكود. `npx vercel ls` بيوري حالة البناء.

**١٠. `/health` بيعرض الكوميت الشغال**
`curl .../health` → `version.commit`. قبل ما تقول «اتصلح»، اتأكد إن النسخة
اللي فيها الإصلاح هي اللي شغالة فعلاً.


---

## 🧭 قواعد معمارية ثابتة — المارد (غير قابلة للتغيير / LOCKED)

> متفق عليها ومقفولة مع المالك. أي شغل في المشروع لازم يمشي عليها. أي أوتوميشن جديد يتبنى بالقواعد دي. آخر تحديث: 28 يوليو 2026.

**1) كل الأجينتس تحت المارد.**
مفيش أي أجينت أو كرون يشتغل لوحده أو بره تنسيق المارد (orchestrator). نقطة تحكّم واحدة، نبضة واحدة، ووتش-دوج يحميها ويرجّعها لو وقعت. ممنوع الكرونات المبعثرة أو الأنظمة الموازية.

**2) مش شغّال دوري — شغّال بالنبضة لما يكون فيه شغل.**
كل أجينت عنده «شرط شغل» (`orchestrator_jobs.work_check` = استعلام EXISTS). نبضة المارد (كل دقيقة) بتبصّ: «فيه شغل للأجينت ده؟» → تشغّله؛ «مفيش؟» → تعدّي. ممنوع أي جدول أعمى (`policy_cron`) يشغّل أجينت شغل على الفاضي. الكرون يفضل بس للبنية التحتية نفسها (النبضة، الطابور، مراقبة الصحة).

**3) البيانات الناقصة تتطلب أوتوماتيك.**
أي منتج/مشروع ببيانات ناقصة → المارد يطلب الناقص من مصدره (السمسار/صاحب الليستنج) مرة واحدة بس (dedup)، عبر قناة OpenWA/القوالب المعتمدة، وأرقام مصرية صح بس.

**4) النشر والتنظيف.**
المكتمل يتنشر تلقائي · الناقص يفضل ويتطلب · الناقص القديم يترحّل (`status=expired`, قابل للرجوع) — **مش يتمسح نهائي**. أي حاجة متعلّم عليها «ممنوع النشر / مستني تفويض» ما تتلمسش.

**5) النطاق.**
القواعد دي على **كل المنتجات** (مش بس العقارات)، وثابتة على مستوى مشروع مضمونة، وعلى مستوى أي شغل مع كلود (كو-ورك أو شات).

**المثال المعتمد (البورصة/المشاريع):** السمسار يبعت مشروع على الواتساب → النبضة تلاقي شغل → تستخرج المشروع بـClaude → تنشره → تطلب الناقص → تعمل صورة غلاف. كله بالنبضة، لما يكون فيه شغل — من غير جدولة ومن غير تدخّل يدوي.


---

## 🔒 نظام الواتساب — مقفول (LOCKED / غير قابل للتغيير)

> متفق عليه ومقفول مع المالك. **ممنوع أي جلسة تغيّره** — لا على مستوى المشروع ولا الأكونت. آخر تحديث: 28 يوليو 2026.

**القناة الرسمية = OpenWA** (whatsapp-web.js). التلات أرقام كلها `transport='openwa'`:
- `201002229982` — الرقم الأساسي
- `201026222337` — محمد ناصف (بيزنس موثّق)
- `201114621551` — الرقم المساعد

(جلسة `web` = شات الموقع، `transport=baileys` — قناة منفصلة تمامًا، مالهاش علاقة بأرقام الواتساب.)

**الممنوعات (مقفولة — ما تتلمسش):**
1. ممنوع إعادة تفعيل **ميتا كلاود** (`whatsapp-webhook` مقفول كـstub، `meta_cloud:disabled`). سبب القفل: كان بيعمل «مخّين» بيردوا على العميل سوا.
2. كل الإرسال يعدّي على **الجيت-واي** `/api/internal/wa-send` → يوجّه لـOpenWA حسب `wa_number_configs.transport`. **ممنوع** الإرسال يروح لبريدج بايليز الميت (`WA_SERVICE_URL` القديم على Railway) — ده كان بيرجّع «مفيش جلسة متصلة» ويحرق الليدز.
3. الطابور الخارجي (`fire_whatsapp_outbound_send`) والسويبر والتنبيهات → كلهم على الجيت-واي، مش على البريدج مباشرة.
4. ممنوع أي جلسة تغيّر `transport` أي رقم، أو توجيه الإرسال، أو ترجّع بايليز، أو تفتح ميتا كلاود. لو محتاج تغيير، ارجع للمالك الأول.

**الخلاصة:** الواتساب = OpenWA عبر الجيت-واي. ثابت ومقفول.


---

## 🖼️ حارس صور الإعلانات/البورصة — `image_text_sentinel` (تحت المارد)

أجينت تحت المارد بيمنع إن صورة إعلان أو غلاف بورصة يبقى فيه **كتابة محروقة** (منيو، برايس ليست، فلاير، خريطة، كارت، لوجو منافس، واترمارك). اتبنى بنفس القواعد الثابتة (بالنبضة لما يكون فيه شغل، بيطلب/يصلّح مش يمسح).

- **`work_check`:** فيه صورة رئيسية (real — مش برندنا `cloudinary` ولا `is_placeholder`) لسه `quality_flag = null`؟ أو غلاف بورصة منشور `cover_checked_at = null`؟
- **الفحص = Claude vision:** الصورة أساسًا كلام/جرافيك → يصلّح. رندر احترافي فيه لوجو صغير أو صورة حقيقية → يسيبها ويعلّمها `clean`.
- **التصليح (مش مسح):** يرقّي أنضف صورة تانية للإعلان؛ لو مفيش → يحط صورة القسم كواجهة (`is_placeholder`, `caption=cat-fallback-auto`) والأصلية تفضل في المعرض. البورصة: يفضّي الغلاف → كارت مبرند يتولّد (`bourse_cover_cards`).
- **بيعدّي على:** صورنا المبرندة (`cloudinary`) والـ placeholders — ما يتلمسوش أبدًا.
- **Edge function:** `image-text-sentinel`. التتبّع: `listing_photos.quality_flag` (null=لسه · clean · has_text · graphic · logo · broken) و`property_market_items.cover_checked_at`.
- **نسخ احتياطية (تنظيف 28 يوليو):** `_backup_primary_swap_20260728` (ترقيات الصور اليدوية). التنظيف اليدوي والحارس كله قابل للرجوع.

## 🔁 مزامنة البورصة للإعلانات — `sync_bourse_to_ads` (تحت المارد)

أي مشروع بورصة منشور (`property_market_items`, جاي من السماسرة) لسه من غير إعلان (`listings.project_id`) → المارد يعمله إعلان تلقائي بالنبضة: كاتيجوري `sale-properties-*` حسب `property_type`/المنطقة، سعر من `price_from`، مورد MADMONA، رقم التواصل الرسمي، وصورة الإعلان = **غلاف المشروع** (كارت المطور المبرند). دالة SQL `public.sync_bourse_to_ads(p_max)`. `work_check` = فيه مشروع بورصة منشور من غير إعلان. نسخة احتياطية `_backup_bourse_sync_listings_20260728` + وسم `directory_source='bourse-sync'`.

## 🏷️ كروت الإعلانات المبرندة — `listing-brand-cards` (تحت المارد)

الإعلان اللي واجهته صورة قسم مؤقتة (`caption='cat-fallback%'` — يعني كان فيه كتابة ومفيش صورة حقيقية) → المارد يستبدلها بـ**كارت مضمونة مبرند باسم النشاط** (لوجو المطعم/المحل) عبر `post-render` (template `value`). اختيار الاسم: اسم المورد الحقيقي لو نضيف (مش «مضمونة …» ولا «حساب …» ولا رقم) → وإلا عنوان الإعلان → وإلا اسم القسم. الكارت بيبقى `caption='brand-card'`. `work_check` = فيه صورة رئيسية `caption like 'cat-fallback%'`.

**السلسلة الكاملة لصور الإعلانات:** `image_text_sentinel` يكشف الكتابة → مربوط بمشروع؟ غلاف المشروع (لوجو المطور) · مفيش؟ صورة قسم مؤقتة → `listing-brand-cards` يحوّلها لكارت باسم النشاط. كله بالنبضة، ومايلمسش صورنا المبرندة (`cloudinary`) ولا الصور الحقيقية.

## 🖼️ عرض الصور — قايمة الدومينات + إعادة الاستضافة (مهم)

الموقع بيعرض الصور عبر **Next.js image optimizer**، اللي بيرفض أي دومين مش مسجّل في `next.config.mjs` → `images.remotePatterns` (بيرجّع **400** والصورة ماتظهرش). لو ضفت صور من مصدر جديد، لازم تضيف الدومين هناك. المسجّل حاليًا: supabase، `res.cloudinary.com` (كروتنا)، `media.canva.com`، `images.unsplash.com`، + مصادر سمسارة/مطاعم خارجية (yallamenu، graph.facebook.com، tripadvisor، `*.lovable.app`، sharkawy، wikilist، deliveryhero، linktree).

**الحل الجذري — `rehost-external-media` (أجينت تحت المارد):** أي صورة من مصدر خارجي (مش supabase ولا `res.cloudinary.com`) → المارد ينزّلها ويرفعها على `content-images/rehosted/` ويغيّر الرابط لتخزيننا. فكل الصور بتبقى من عندنا (ماتكسرش لو الدومين مش مسجّل، والصور الخارجية اللي بتنتهي متضيعش). `work_check` = فيه صورة رابطها خارجي ولسه ماتنقلتش. سجل `_rehost_log` (orig_url/new_url/status). الفاشلة (المصدر ميت) تتعلّم `failed` وتتعدّى. **بعد ما ده يشتغل، الدومينات الخارجية في `next.config` بتفضل بس كشبكة أمان للصور الجديدة قبل ما الأجينت ينقلها.**


## 🗂️ تنظيف صور الأقسام (categories) — تنفيذ 28 يوليو (لقطة واحدة، مش أجينت دائم)

مسح شامل لكل صور الأقسام (316 قسم / 59 صورة فريدة) بالـOCR (tesseract ara+eng) + معاينة بصرية. اتصلّح **8 صور** كان فيها كتابة محروقة أو مربعات خضرا فاضية (بتأثّر **38 قسم**):

- **كتابة/جرافيك:** `real/education-courses.jpg` (كتاب رياضة فنلندي، 12 قسم) → `learning-courses.jpg` · `real/printing.jpg` (بوستر إعدام تشيكي/ألماني قديم في الخلفية، 9 أقسام) → `printing-services.jpg` · `real/food-egyptian.jpg` (بادج MyPlate.gov) → `food-egyptian-v2.jpg` · `pro/cat-final/shop-motorcycle-parts.jpg` (واترمارك مصوّر) → `motorcycle-parts.jpg`
- **مربعات خضرا (placeholder قديم كان لايف):** `properties-v1.jpg` (properties + 11 شاليه ساحل، 12 قسم) → `properties-chalet.jpg` · `equipment-v1.jpg` (gaming-consoles) → `gaming-consoles.jpg` · `services-v1.jpg` (كان مشترك بين lawyers + recreation-kayak — **اتفصل لصورتين**) → `legal-services.jpg` + `recreation-kayak.jpg`
- كل الصور البديلة نضيفة/بدون كتابة (اتفحصت OCR + بصريًا)، اترفعت على bucket **`ads/categories/`** بأسماء جديدة (كسر الكاش)، واتأكد إنها بتظهر لايف عبر الـNext.js optimizer (200 image/jpeg). التحديث بمطابقة الـURL القديم بالظبط + فصل `services-v1` بالـslug.
- **`cat-img-upload`** edge function كان أداة رفع لمرة واحدة (service-role جوّه، anon JWT للنداء) → **اتقفل كـstub بيرجّع 403** بعد الاستخدام عشان الـanon key ما يقدرش يكتب في التخزين. الأقسام مجموعة ثابتة (مش user-generated زي الإعلانات/البورصة)، فمفيش أجينت دائم؛ لو احتجنا منع تلقائي لأي صورة قسم جديدة فيها كتابة، ممكن نضيف `work_check` على `categories` بنفس فكرة `image_text_sentinel`.


## 🧩 توحيد صور الأقسام: الإضافة (بيع/خدمات) = العرض (إيجار) — تنفيذ 28 يوليو

كان تراك **الإيجار (rentals) كله بصور (137/137)**، بس تراك **البيع (products) فيه 61 قسم نشط بدون صورة** والخدمات 32 — فاللي بيضيف إعلان بيع/خدمة كان بيشوف أقسام فاضية. الإجمالي **93 قسم نشط بدون صورة**. الإصلاح (مباشر، مش أجينت — الأقسام ثابتة):
- **56 قسم** اترقّعوا من **التوأم بنفس الاسم** (نفس القسم في تراك تاني عنده صورة، مثال `sale-properties-apartment` ← صورة `properties-apartment`).
- **35 قسم** اترقّعوا من **صورة القسم الأب** (أبناء تجميل → تجميل، أبناء الخدمات المنزلية → الخدمات المنزلية، أبناء الإلكترونيات → الإلكترونيات).
- **2 يتامى** (مفيش توأم ولا أب بصورة) اترفعلهم صور جديدة: `sale-kitchens` (مطابخ) و`equipment-restaurant` (معدات/مطابخ مطاعم).
- **النتيجة:** كل الأقسام النشطة (393/393) بقى عندها صورة — صفر فاضي. نسخة احتياطية `_backup_category_img_fill_20260728` (id/slug/old_image_url) للرجوع.

**التكرار:** مفيش أقسام مكررة حقيقية — كل التكرار في الأسماء هو **بيع (products) ↔ إيجار (rentals)** مقصود (سلاق `sale-` منفصل + تراك مختلف). الزيادة الوحيدة `sale-vehicles-used-car` («سيارة» مستعملة) **مقفولة أصلاً** (`is_active=false`).


---

## 🩺 قواعد الداتابيز — فخّين كلّفونا شغل ضايع (٢٢ أغسطس ٢٠٢٦)

محمد: «بلاقي حاجات بتقع بعد ما بنقفل الجلسة». اتكشف السبب، وده مش تخمين:

### ⛔ فخ ١: `CREATE FUNCTION` + `rollback` في نفس النداء = الدالة بتضيع

الـDDL في بوستجرس **جوّه ترانزاكشن**. فلو عملت:

```sql
create or replace function foo() ...;
begin; set local role authenticated; select foo(); rollback;   -- ❌
```

الـ`rollback` **بيلغي إنشاء الدالة نفسها**. التجربة بتطلع ناجحة، وإنت بتقول
«اتعمل واتجرّب»، **والدالة مش موجودة أصلًا**.

كده ضاعت `madmona_team_accounts` **مرتين** — وشاشة «إدارة الموظفين» فضلت
مكسورة في البرودكشن ومحدش واخد باله. و`crm_health` نفسها ضاعت أول مرة اتعملت.

✅ **القاعدة:** الإنشاء في نداء **لوحده**. التجربة في نداء **تاني**. وبعدها
تأكيد إن الدالة موجودة فعلًا:
```sql
select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace
 where n.nspname='public' and proname = 'اسم_الدالة';
```

### ⛔ فخ ٢: زيادة بارامتر بـ`DEFAULT` بتعمل دالة **تانية** مش بتستبدل القديمة

`create or replace` بيطابق على **التوقيع** مش على الاسم. فلو زوّدت بارامتر،
بتبقى عندك نسختين، والنداء بالاسم من PostgREST ممكن يقع على القديمة.

حصل مع `crm_log_call` (١١ → ١٤ بارامتر) و`crm_my_queue` (١ → ٢).

✅ **القاعدة:** بعد أي زيادة بارامتر، امسح القديمة:
```sql
drop function if exists public.اسم_الدالة(الأنواع_القديمة);
```
وفيه **حارس في الداتابيز** (`trg_block_crm_overloads`) بيرفض أي `CREATE
FUNCTION` ينتج نسختين من نفس دالة `crm_*` — ماتشيلوش.

---

## 📇 نظام الـCRM — من فين تبدأ في أي جلسة جديدة

**الشاشات:** `/admin/crm` (الأدمن) · `/crm` (الموظف على الموبايل، جوّه
«شغلي» = `/account/work`) · `/admin/staff` (الفريق وحساباته)

**أول حاجة تعملها:**
```sql
select jsonb_pretty(crm_health());
```
بترجّع `ok:true` لو النظام سليم، ولو فيه دالة/جدول/عمود/بوليسي ناقص بتقولك
اسمه بالظبط. البانر ده بيبان كمان فوق في `/admin/crm`.

**لو فيه حاجة ناقصة:** شغّل `sql/2026-08-22_crm_functions_rebuild.sql` كامل.

**بعد أي تعديل على الداتابيز:**
```bash
node scripts/dump-crm-sql.mjs   # بيولّد ملف إعادة البناء من السيرفر نفسه
git add sql/ && git commit
```
لو ماعملتش كده، التعديل بيعيش في السيرفر بس — ودي بالظبط المشكلة اللي فوق.

**العقد:** جدول `crm_contract` فيه كل حاجة الشاشات معتمدة عليها. أي دالة
جديدة الشاشات هتناديها **لازم تتضاف للعقد** وإلا الفحص مش هيحميها.

**التوثيق والقرارات:** `sql/2026-08-21_crm_madmona.sql` — ليه كل حاجة اتعملت
كده، وإيه اللي **ماتعملش عن قصد**.



## البيزنس كلاود للمطورين (٢٤/٨/٢٠٢٦)
- كل مطور في بورصة العقارات (property_market_items.developer) ليه صفحة كلاود `/s/dev-*` — ٦٨ مطور = ٦٨ صفحة (اتعمل provision لـ Al Qamzi / J communities / Qatari Diar اللي كانوا ناقصين).
- `public_storefront_listings(p_slug)` بترجّع الوحدات المنشورة من listings **+ مشاريع البورصة** النشطة غير المحظورة للمطور (مطابقة بـ business_name = developer)، وكل عنصر فيه `href` جاهز — الفرونت في `/s/[slug]` يستخدم `u.href` وما يبنيش اللينك بنفسه.
- الهوية: ٢٩ مطور عندهم لوجو، الباقي (~٣٦) محتاجين ملفات لوجو حقيقية من محمد. مفيش ثيمات ألوان متخزنة للمطورين عن قصد (قاعدة «تصميم الديسكتوب من غير ألوان متخزنة»).
- درس git (٢٤/٨): `git add -A` شفط ١٨ ألف ملف (بروفايلات كروم بتاعت سكريبتات الريلز) — اتضافوا لـ .gitignore. **دايمًا add بالأسماء، مش -A.**


## مسميات «إنت مين؟» + دروب ليست الإضافة (٢٤/٨/٢٠٢٦)
- سؤال «إنت مين؟» في الإضافة بيتكلم بلغة القسم (sellerOptions في AddListingClient): الطب = دكتور حر / عيادة / مجمع عيادات (المجمع = business + فروع)، التعليم = مدرس / سنتر، العربيات = فرد / معرض-تاجر، العقارات = مالك / شركة-مطور-مكتب… المتسجّل دايمًا individual/business → seller_kind. أي قسم جديد ياخد الديفولت «فرد / نشاط تجاري».
- دروب ليست اختيار النشاط بتعرض **كل** التصنيفات مش تصنيفات التاب المفتوح بس، وكل مجموعة معنون عليها نوعها (— بيع/إيجار/خدمات/مطاعم).
- شجرة التصنيفات في fetch صفحة الإضافة بتجيب الأحفاد (مستوى تالت) وتضمهم ورا أبوهم — «سيارة زيرو» و«سيارة مستعملة» كانوا مش بيوصلوا للويزارد خالص قبلها.
- جدول attributes: كل «الماركة» في المركبات بقت select (٤٦ ماركة عربيات / ١٧ موتوسيكل + «أخرى») و«سنة الصنع» select من 2027 لـ1990 — اتعمل على مستوى الجدول كله مش تصنيف واحد. وفيه ٩ خانات options كانت متخزنة نصوص بدل {key,label_ar} (الأزرار كانت بتطلع فاضية) — اتصلحوا. sale-vehicles-luxury كان فيها خانتين «الماركة» — التوأم اتشال.
- درس SQL: `unnest(...) with ordinality as t(b,i)` مع CTE alias بالمقلوب `brands(i,b)` قلب الأسماء أرقام — دايمًا سمّي أعمدة الـCTE بنفس ترتيب الإخراج.


## مفاتيح أدوات المارد + قاعدة السكوت (٢٤/٨/٢٠٢٦)
- محمد: «المارد مش نافع إنه يضيف إعلانات ويستدعي أدوات فألغيها — الإضافة تكون
  عن طريق صاحب الإعلان». **الأداة اتقفلت من الداتابيز، ماتحذفتش من الكود.**
  الجدول `marid_tool_settings` · الشاشة `/admin/marid` → كارت «🔌 أدوات المارد».
  المطفي دلوقتي: `create_listing_draft` · `add_menu_items`.
- **قفل أداة = ٣ حراس مع بعض** (`src/lib/marid-tool-settings.ts`)، وواحد لوحده
  مايكفيش: (١) الأداة ماتتبعتش لكلود، (٢) تعليمة بديلة تتحقن في **آخر** البرومبت
  كله، (٣) `runMaridTool` بيرفض النداء ويسجّله في `marid_tool_blocked_log`.
  التعليمة لازم تبقى آخر حاجة لأن `customer-concierge.ts` فيها ١٥ تعليمة بتأمر
  بنداء `create_listing_draft`، وبلوك الميديا في `marid-brain.ts` كمان.
- حارس الوعد الكاذب في `marid-brain.ts` بقى بياخد `listingToolOff`: لما الأداة
  مقفولة التصحيح بيقول «قوله يضيف بنفسه» بدل «نادِ الأداة» — من غير كده كان
  هيبعت المارد يدوّر على أداة مش موجودة.
- **قاعدة السكوت:** `marid_should_skip(phone)` هي اللي بتقرر مين المارد مايردّش
  عليه — الفريق · رقم مارد · رقم كان مارد قبل كده · مسكّت يدويًا. المطابقة على
  آخر ١٠ أرقام. الحارس في `/api/whatsapp/baileys` (و`openwa` بيحوّل عليه فحارس
  واحد بيكفي الاتنين).
- ⚠️ **أوامر الأدمن من واتساب وقفت** — استثناء `!isAdmin(phone)` اتشال بناءً على
  تعليمة محمد. الرجوع من غير نشر: صف في `marid_skip_exceptions` بالرقم.
- التوثيق الكامل والقرارات: `sql/2026-08-24_marid_tool_switches_and_skip.sql`.

- (٢٥/٨/٢٠٢٦) /admin/listings هي الشاشة الوحيدة لكل الإعلانات: فيها لوحة «واردة الويزارد» (جدول listing_drafts) جنب الإعلانات الفعلية (جدول listings). المسار القديم /admin/listing-drafts بقى redirect لـ /admin/listings?stage=wizard — أي شاشة إعلانات جديدة تتضاف جوّه /admin/listings مش كصفحة منفصلة. محمد: «انا عايز مكان واحد يعرض الاعلانات كلها وهو ده الي يكون ظاهر عند الموظفين».

- (٢٥/٨/٢٠٢٦) إعلانات الويزارد مسموعة: تريجر notify_wizard_draft_submitted على listing_drafts بيبعت پوش لكل موظفي can_manage_listings لما الإعلان يبقى submitted، وكارت الإعلانات في شغلي فيه زرار «واردة الويزارد» بعدّاد من wizard_drafts_pending_count(). ⚠️ درس مهم: notification_queue_dedupe بيرفض نفس (recipient + title) خلال ٦٠ دقيقة — أي إشعار جديد لازم عنوانه يحتوي حاجة مميزة للحدث نفسه (اسم الإعلان/التاسك) مش عنوان ثابت، وإلا التاني في نفس الساعة هيتبلع بصمت.

- (٢٥/٨/٢٠٢٦) دروب ليست «اختار نشاطك» في الإضافة بتتبني بنفس نظام مجموعات كروت الموبايل (group_slug → roots → subs) مع dedupe عام بالـslug — أي تعديل على التصنيفات لازم يتشاف في الاتنين. توحيد التاسكات: get_my_tasks (الشات) و get_my_work_home.my_tasks (شغلي) لازم يرجّعوا نفس المجموعة بالظبط — اختبار التطابق: عدّ الـids المشتركة للموظف نفسه. ⚠️ رقم مضمونة العام 01002229982 معمول له is_platform_public_phone(): ممنوع يدخل في أي منطق «الملكية بالرقم» (asset_owners, auto-claim) — الرقم ده بيتحط مكان أرقام الموردين فأي ربط بيه بيرمي أصول المنصة كلها على حساب محمد الشخصي.

- (٢٥/٨/٢٠٢٦) نظام التجميل والمطاعم الواقعي: بيزنس واحد = إعلان واحد + أصنافه جوّاه. المطاعم → restaurant_menu_items، أي حاجة تانية (تجميل/خدمات/منتجات) → mart_products. ممنوع «إعلان لكل خدمة/صنف» وممنوع جدول أصناف جديد موازي — الاتنين دول هما النظام. موظفي مضمونة ليهم RLS policies (mart_products_madmona_staff / rmi_madmona_staff) وإدارة الأصناف من زرار 🍽️/🧾 في /admin/listings. عنوان الكتالوج في صفحة الإعلان بيتغير حسب التصنيف (تجميل/خدمات → «الخدمات والأسعار») عبر defaultCatLabel.

- (٢٥/٨/٢٠٢٦) /login شاشة واحدة موحّدة: فورم (رقم/إيميل + باسورد أو PIN) عبر login_with_password — ممنوع أي شاشة «أنا موظف» منفصلة على مستوى المشروع؛ الواتساب زرار بديل جوه نفس الشاشة. ⚠️ درس صور الإعلانات: listing_photos ليها policy لموظفي مضمونة (listing_photos_madmona_staff) — أي جدول تابع للإعلانات (صور/منيو/كتالوج/أسعار) لازم يكون فيه is_admin_or_listings_staff() في سياساته، لأن الموظفين مش is_admin() عمدًا، والفشل بيكون صامت (الرفع للـstorage بينجح والصف بيترفض).

- (٢٥/٨/٢٠٢٦) سلسلة النشر فيها تريجرين على listings لازم يتراعوا مع بعض: enforce_listing_publish_requirements (صورة واحدة + توثيق رقم بواتساب — موظف مضمونة بجلسته الحقيقية مستثنى من شرط الرقم، ومضمونة الحقيقية c8b7b9d7-6178-… والوصيّة 267e0655-… في الاستثناءات) و enforce_listing_content_minimums (رسالة «النشر متوقف: …» — بيرفض لو كل الصور placeholder من /ads/categories/ أو بوستر cloudinary posts/test، أو مفيش سعر لغير العقارات). الصورة اللي شكلها موجود في الثمبنيل ممكن تكون placeholder تصنيف مش صورة حقيقية — دايمًا اتأكد بعدّ الصور الحقيقية مش بوجود أي صف في listing_photos.

- (٢٥/٨/٢٠٢٦) قاعدة محمد: «الإصلاحات تشمل المشروع مش شاشة». طبقة الحقول الموحّدة للإعلانات هي جدول attributes (بالتصنيف) + listing_values — الويزارد (عبر /api/listing-drafts/attributes) وفورم التعديل (ListingForm) الاتنين بيقروا منها، وclaim_listing_draft بقى بينقل كمان product_details بتاعة الويزارد (brand→make …) لنفس القيم. وحدات التسعير مصدرها categories.allowed_pricing_periods/default_pricing_period في كل الشاشات. شجرة بيع المركبات اتفردت: «سيارة زيرو/مستعملة» مباشرة تحت «مركبات ونقل» والمستوى الوسيط «مركبات زيرو/مستعملة» اتقفل — أي تصنيف وسيط مكرر بيتقفل مش بيتمسح.

- (٢٥/٨/٢٠٢٦) الدخول الموحّد: /login → POST /api/login — بيجرّب platform_admins الأول (bcrypt + scrypt القديم، نفس منطق admin-entry) ولو نجح بيفتح كوكي اللوحة + madmona_token مع بعض، وإلا business_employees (باسورد/PIN) عبر login_with_password. أي مسار دخول جديد يتبني على /api/login دي — ممنوع RPC دخول جديدة تتنادى من المتصفح مباشرة. issue_madmona_token_for_admin للـservice_role بس.

- (٢٥/٨/٢٠٢٦) قاعدة الحقول: أي سؤال عن مواصفات (ماركة/موديل/سنة/…) يتسأل من جدول attributes بس — ممنوع خانات مكررة hardcoded في الويزارد أو فورم التعديل. كل select فيه «أخرى…» = كتابة حرة (SelectWithOther في ListingForm، وAttributeFieldRenderer في الويزارد). كل تصنيفات المركبات (بيع + بحرية) عندها make/model/year. خانات brand/model_name في listings بتتملى أوتوماتيك من قيم make/model عند الحفظ (للعرض) — مش من إدخال منفصل.

- (٢٥/٨/٢٠٢٦) طابور البان الحقيقي هو whatsapp_campaign_messages (مش whatsapp_outbound_queue) — الرسايل المتأخرة بتنطلق دفعة واحدة أول ما البوت يتوصل. الحماية: pg_cron job «wa-campaign-stale-cancel» (كل ساعة، jobid 211) بيلغي أي queued عدّى معادها ٢٤ ساعة، وشاشة /admin/send فيها كارت الطابور بزرار «🧹 امسح الطابور» (DELETE /api/admin/send، ?session= اختياري). ملحوظة: cron.schedule جوه migration ممكن ميتسجلش بصمت — دايمًا اتأكد بـ select من cron.job بعدها.

- (٢٥/٨/٢٠٢٦) عمود «النشر» في /admin/listings = published_at (تاريخ أول نشر) و«— لسه» = مفيش تاريخ. تريجر trg_set_published_at بيسجله أوتوماتيك عند أي نشر من أي مسار. تنضيف الأصناف: أي إعلان-صنف (أكل/تجميل) محتواه موجود جوه منيو/كتالوج بيزنس اتنقل للـrecycle bin (١٢٤ يوم ٢٥/٨) — الاسترجاع بـ restore_listing(id). لسه فيه ~١٨٠ درافت صنف من غير بيزنس أم (بلا رقم) — مستنية قرار محمد: مسح ولا مراجعة.

- (٢٥/٨/٢٠٢٦) نبض الأداء: notify_employee_performance() بيقرا من daily_tasks الحقيقية ويبعت لكل موظف مضمونة پوش + إشعار داخلي (نسبة الإنجاز، المتأخر، أهم ٣ مهام مطلوبة). مجدول pg_cron مرتين يوميًا: employee-performance-pulse-midday (١ الضهر القاهرة) و-eod (٥:٣٠). العنوان فيه النسبة والتاريخ عشان dedupe الساعة ما يبلعوش.


## 📋 المهام في مكان واحد + لغز الكرونات المختفية (٢٥ أغسطس ٢٠٢٦)
- محمد: «عايز تاسكات تكون في مكان واحد وبتاب التفاصيل وعايز اعادة صياغة لتاب شغلي». `/chat/tasks` بقت ريدايركت على `/account/work` — أي شاشة مهام جديدة ممنوعة، «شغلي» هي الوحيدة. المهام متجمّعة (متأخر 🔥 / بمعاد / من غير معاد) وزرار «ضيف مهمة» بينادي `add_my_task`.
- 🕵️ **قاعدة حرجة**: الكرونات كانت بتختفي لأن `marid_cron_guard` (بيتنده من نبضة المارد) بيمسح أي جوب مش مسجّلة في `public.cron_allowlist` (عمود reason إجباري). **أي `cron.schedule` جديد لازم يتسبقه insert في `cron_allowlist`** — وبعدها اتأكد بتشغيل `select public.marid_cron_guard()` يدوي وشوف إن الجوب لسه عايشة في `cron.job`.


## 🧑‍💼 موظفين الأبليكيشن في شاشات /admin (٢٥ أغسطس ٢٠٢٦)
- محمد: «اعلانات شهد لسة مش بتنزل مع انها ضايفاها من تاب شغلي». السبب: middleware بيحرس /admin و/api/admin بكوكي اللوحة بس، وجلسة الأبليكيشن في localStorage (الـedge مايشوفهاش) — فأي موظف داخل من شغلي كان بياخد 401/ريدايركت وكل adminRpc بيفشل.
- **القاعدة**: أي شاشة أدمن المفروض موظفين الأبليكيشن يستخدموها لازم: (١) استثناء pass-through في middleware للصفحة، (٢) قبول توكن Supabase في مسارات الـAPI بتاعتها عبر `isListingsStaffRequest` (adminGate — edge-safe، بيتأكد بـ`is_listings_staff_uid`)، (٣) الفرونت يبعت التوكن بـ`staffAuthHeaders()` من adminRpc. الدوال المسموحة للـstaff في /api/admin/rpc محصورة في `LISTINGS_STAFF_ALLOWED` — متضفش دالة فلوس/صلاحيات هناك أبدًا.


## 🔌 النشر الأوتوماتيكي لدرافتات الويزارد (٢٥ أغسطس ٢٠٢٦)
- محمد: «الاعلان من ضيف ويزارد مش بيضاف عايز اعرف ليه مش بيتنشر». السبب: دالة `auto_publish_submitted_draft()` كانت موجودة من غير تريجر متركب على `listing_drafts` — اتركب `trg_auto_publish_submitted_draft` (AFTER INSERT OR UPDATE OF status). الدالة بترفع نفسها service جوّه الترانزاكشن قبل `publish_unclaimed_draft` عشان ترجرات النشر على listings ماترفضش لو الإدخال جه من سياق من غير هوية (كرون/ويبهوك).
- **قاعدة**: أي دالة تريجر بتتكتب لازم نتأكد إن التريجر نفسه متركب فعلًا (`select tgname from pg_trigger`) — دي تالت حاجة «مكتوبة بس مش موصّلة» نكتشفها (كرونات pg_cron مرتين + التريجر ده).


## 🚀 النشر الواضح + تفاصيل المهام الحية (٢٦ أغسطس ٢٠٢٦)
- محمد: «مفيش تاب نشر» + «تفاصيل التاسك مش بتسمع». القاعدة: أي إعلان مش منشور لازم يبان قدامه فعل نشر صريح (زرار 🚀 في صف الإعلانات + «احفظ وانشر» في التعديل)، وزرار اسمه «حفظ» عمره ما ينشر بالساكت.
- تفاصيل المهام بتتولد **حية وقت العرض** من `task_dynamic_detail` في `get_my_work_home` و`get_my_tasks` (النسخة الثابتة في daily_tasks.description فولباك بس) — متعتمدش على السنابشوت اللي بيتكتب وقت التوليد. وكل قالب في `recurring_task_templates` لازم يكون ليه description — مفيش مهمة من غير تفاصيل.


## 🧩 أتربيوتات «ضيف إعلان» + إشعارات المهام والرسايل (٢٦ أغسطس ٢٠٢٦)
- مودال «ضيف إعلان» بياخد حقول التصنيف من `/api/listing-drafts/attributes` (بقى يقبل `category_id` أو `slug`) والقيم بتتكتب في `listing_values` عبر `admin_add_listing(p_attributes)` — نفس مصدر الويزارد والتعديل، ممنوع فورم أتربيوتات موازي.
- إشعارات المهام مصدرها **تريجرات داتابيز بس**: `trg_notify_new_daily_task` (توليد أوتوماتيك = ملخص واحد بديدوب العنوان الثابت · يدوي = باسم المهمة · `madmona.skip_task_notify=1` بيسكّته للإضافة الذاتية) و`trg_notify_new_flow_task` — متضيفش إشعار مهمة من كود التطبيق تاني.
- مراقبة الواتساب: `trg_notify_owner_every_wa_message` بيبعت لمحمد (profile 147cd904) كل رسالة صادرة/واردة — الحملات (template_name أو agent_name فيه campaign) مستثناة عمدًا.


## 📞 التحكم في تليفون المكالمات من اللوحة (٣٠ أغسطس ٢٠٢٦)
- محمد: «تفعيل التليفون madmona-voice عايزين له مكان في الأدمن بانل». المعمارية: اللوحة بتكتب أمر في `voice_commands` وبتقرا `voice_status` (RPCs: voice_status_get / voice_command_request — كوكي اللوحة بس)؛ عميل ويندوز `D:\voice\voice-agent.ps1` (Scheduled Task «MadmonaVoiceAgent» كل دقيقة) بينفذ بـVBoxManage ويرفع الحالة (الماكينة + بلوتوث الأيتل + جاهزية أستريسك عبر plink على 2222، باسورد الجيست في السكريبت).
- **درس ٣٠/٨**: الماكينة بتقع أحيانًا في `gurumeditation` — التشغيل لازم يسبقه `controlvm poweroff` في الحالة دي (العميل بيعملها لوحده). وssh.exe بتاع ويندوز مبيشتغلش من شِل من غير كونسول (بيموت صامت) — استخدم `D:\voice\plink.exe` دايمًا.

## ?? ???????? ????Service Worker (? ?????? ????)
- **?? ??? ??? ????? ?? ??????? = ??? ??? ?????? ?? public/sw.js** (madmona-vN ? vN+1)
- ???? ?? ???????? ???? ??? ???SW ?????? ???????? ?? ????? � ??? ?????: ?? ????? ?? ??? ????????? ????????
- ??????: SW ???? ? SW_UPDATED ? ?????? ???? reload ??????

## ????? ????????? ????????? (? ?????? ????)
- **?? ????? ?? ??????? = ????? ?? ????????** � ????: �?? ????? ????? ????? ?? ????????? ????????? ??????�
- ???????: MobileHome.tsx (??????) + CategoryTrackTabs.tsx (???????) � ???? ????????
- ???????: MobileHome + TopNav � ??????? ????????? WorkspaceMenu (????)
- ??? ?????: ???????? ?????? ? ???? ?????????? ??? ??? �???? ?????� ?? ?? ?????

## ?? PowerShell inline node -e ????? ???? (? ?????? ????)
- **?? ????? ??? ??? = ?????? .cjs ??? Windows-MCP:FileSystem** � ?? node -e ?? PowerShell
- ??? ?????: ??? sw.js ?? v14 ??v18 (? ????) ???? ????? ???? � ????? ??? v13 ????????? ?? ???????
- ?????: PowerShell ?????? ?????????? ????backticks ?? node -e

## 🚪 باب الخروج للجهاز العالق — `/fix.html` (١ سبتمبر ٢٠٢٦)
محمد: «التعديلات الاخيرة في نسخة الموبايل لسة متعدلتش» (رابع مرة في يوم).

🐞 **الدرس:** حارس النسخة (`versionGuard` في `ServiceWorkerRegister.tsx`)
**مايقدرش** يصلّح جهاز عالق على SW قديم بيخزّن الـHTML (قبل v8): الجهاز
بياخد HTML قديم → يحمّل شنكات قديمة → الحارس عمره ما بيشتغل لأنه مبعوت
جوه الباندل الجديد اللي الجهاز مش واصله. **قفل دائري.**

✅ **الحل:** `public/fix.html` — ملف ثابت **بره تطبيق Next** على مسار
جديد. أي SW مهما كان قديم مالوش نسخة منه في الكاش → لازم يجيبه من الشبكة.
بيشيل كل تسجيلات الـSW + يمسح كل الكاشات + يرجّع على الهوم.
مضاف لـ`NEVER_CACHE` عشان ماينحبسش هو كمان.

📱 **لما حد يقول «التحديث مش واصلني»:** ابعتله `madmonacairo.com/fix.html`
— بيصلّح نفسه في ثانية، من غير ما يمسح التطبيق أو يفتح إعدادات المتصفح.

⚠️ **قبل ما تقول «الجهاز عالق»، اتأكد من السيرفر الأول** (٤ فحوص، دقيقة):
`git log origin/main..HEAD` فاضي · `curl .../sw.js` نسخته = الريبو ·
كلمة من التعديل موجودة في شنك الصفحة · الـHTML اللايف فيه النص الجديد.

## 👤 «رقم بإعلان واحد = فرد» + الصناعة B2B دايمًا بيزنس (١ سبتمبر ٢٠٢٦)
محمد نصًا: «أي رقم عارض إعلان واحد هنتعامل معاه إنه فرد، بس هنسيبه
يستخدم الـB2B ويتصنف بيزنس».

**التطبيق** (`compute_seller_class` — تريجر `set_seller_class` على listings):
- 🏭 `track='industry'` = **أول فحص**، بيرجّع `business` دايمًا مهما كان
  عدد الإعلانات. المورد الجديد بأول إعلان يفضل بيزنس.
- 👤 «إعلان واحد = فرد» على **أقسام المخزون بس** (كل إعلان = وحدة/سلعة):
  sale-property · properties · sale-vehicles · vehicles · sale-marine ·
  marine · shop · shop-home · tourism · equipment · events.
  العدّ بـ`phone_core()` — `01xx` و`+201xx` كانوا بيتعدّوا مالكين مختلفين.
- 🏗️ `developer` مستثنى — الإشارة `project_id` مش عدد الإعلانات.

⛔ **مستثنى عن قصد — متطبّقش القاعدة هناك:** الأقسام اللي موديلها
«بيزنس واحد = إعلان واحد + أصنافه جوّاه» (قاعدة ٢٥/٨): **مطاعم · طبي
وتجميل · منزلية · مهنية · تعليم · عناية**. الإعلان الواحد هناك **هو**
البيزنس. تطبيق القاعدة كان هيحوّل ١٦ مطعم و٨ شيفات و٣ عيادات لـ
`individual` — وهو تصنيف **مالوش وجود** في قسم المطاعم (مطاعم · شيفات
منزليين)، يعني كانوا هيختفوا من الفلتر خالص. لو اتقرر يتطبق هناك،
لازم تسميات `individual` تتضاف للمجموعات دي في `seller_class_labels` الأول.

## 🏭 تابات الأقسام في التراك ذو الرووت الواحد (١ سبتمبر ٢٠٢٦)
`showGroupHeadings = rootGroups.length > 1` بيمنع كروت المجموعات في أي
تراك عنده رووت واحد (**شركات وصناعة · مطاعم**) — وأقسامه كانت مستخبية
ورا ضغطة على بلّة وحيدة مالهاش معنى. دلوقتي `soleRoot` بيتحسب مختار
تلقائيًا وأقسامه بتتعرض كتابات كبيرة بعدّادات (`catCountDeep` — العدّ
شامل الأحفاد عشان قسم أبوه فاضي وأولاده مليانة يبان برقمه).

⚠️ **درس مرتبط:** فلتر نوع البائع كان مربوط بالمجموعة بس، فالتراكات
دي ماكانش ليها فلتر خالص. `seller_class_filters(p_group, p_track)`
بقت تقبل النطاق بالتراك، وصفوف `track:<اسم>` في `seller_class_labels`
بتدّي التسمية على مستوى التراك (من غيرها الدمج بيطلّع «معارض» قوارب
و«معارض» عربيات جنب بعض في تاب البيع).

⚠️ **وتبديل تاب القسم لازم يرجّع الشجرة من الأول** (مجموعة · تصنيف ·
بائع · مصدر · فرش) — من غير كده بتفضل عالق جوه مجموعة القسم القديم
وتاباتها فاضلة على الشاشة بتفلتر بقيمة مالهاش وجود في القسم الجديد.

## 🍳 التصنيف للعرض ≠ الحساب للقدرات (١ سبتمبر ٢٠٢٦)
محمد نصًا: «في قسم شيفات منزليين في المطاعم ده للأفراد بس، برضو هيبقى
ليه حساب بي تو بي».

**المبدأ المقفول:** `seller_class` بيحكم **العرض والفلتر** بس.
الـ**حساب والقدرات** (استورفرنت · منيو · لوحة) حاجة تانية خالص.
الشيف المنزلي بيتعرض «فرد» **و** بياخد B2B كامل. متربطش الاتنين ببعض.

🐞 اللي كان بيمنعه: `is_b2b` كان محسوب = (إعلانات منشورة ≥ ٢) في
مسارين (`trg_refresh_b2b_on_listing` + `refresh_b2b_flags`). والأقسام
اللي موديلها «بيزنس واحد = إعلان واحد + أصنافه جوّاه» (قاعدة ٢٥/٨)
البيزنس كله فيها إعلان واحد → **كل** مطعم وشيف وعيادة كانوا
`is_b2b=false`. قبل: ٠/٨ شيفات · ٠/١٩ مطاعم · ١٠٩ مورد بإعلان واحد
كلهم متقفلين. بعد: ٨/٨ · ١٩/١٩ · الإجمالي ٦١ ← ٩١.

✅ المصدر الواحد دلوقتي `supplier_is_b2b(uuid)`، والمسارين بينادوه:
إعلان منشور واحد في قسم «البيزنس هو الإعلان» (مطاعم · طبي وتجميل ·
منزلية · مهنية · تعليم · عناية · مناسبات · صناعة) **أو** ٢+ إعلانات
في أي قسم. فـ٧٩ مورد بإعلان واحد في أقسام المخزون فضلوا من غير B2B —
وده الصح: اللي بيبيع عربيته الوحيدة فرد مش تاجر.

⚠️ **مفيش تصنيف «شيفات منزليين» في `categories`** — أقسام `food` كلها
أنواع أكل (شرقي · مشويات · بدوي · بحري · كافيهات …). «شيفات منزليين»
**تاب فلتر** مصدره `listings.seller_class`. والريجيكس في
`compute_seller_class` (`home-chef|home-cook` · `شيف بيت|طبخ منزلي`)
**مابيطابق أي تصنيف موجود** — الاشتقاق الفعلي من `seller_kind='individual'`.

## 🔗 الإضافة = العرض: مصدر واحد للتصنيفات (٢ سبتمبر ٢٠٢٦)
محمد نصًا: «ياريت الإضافة تطابق العرض، علشان مش عارف إزاي إحنا بنضيف قسم
وبننسى نشوف لو حد حب يضيف مانيوال يعمل إيه، أو لو موظف حب يضيف. أنا
عايز المواضيع دي يكون ليها ربط علشان هي فعلاً مربوطة».

🐞 قاعدة تجميع التصنيفات كانت **مكتوبة مرتين** — نسخة في
`MarketplaceClient` (العرض) ونسخة في `AddListingClient` (الإضافة).
فأي قسم جديد يتظبط في واحدة وينسى في التانية. وده حصل فعلًا: العرض
اتصلح والإضافة فضلت بتعرض «شركات وصناعة» رووت واحد من غير مجموعات.
وكمان `add-listing/page.tsx` كانت **بترمي** حقول `group_*` من الأقسام
الفرعية وهي بتبني الشجرة، فالويزارد ماكانش يقدر يجمّع أصلًا.

✅ **القاعدة دلوقتي في `src/lib/categoryGroups.ts` — `resolveTopGroups`:**
1. جمّع الرووتس بالـ`group_slug` (الوضع الطبيعي: بيع · إيجار · خدمات).
2. لو طلعت **مجموعة واحدة ورووت واحد** → المجموعات الحقيقية عايشة على
   **أولاد** الرووت، فأعد البناء منهم (شركات وصناعة → صناعة الأدوية ·
   صناعة عامة · خدمات صناعية).
3. غير كده سيبها (مطاعم: مجموعة واحدة و١٣ رووت — الرووتس هي المستوى الأول).

⚠️ **أي شاشة تعرض أو تضيف تصنيفات لازم تنادي `resolveTopGroups`** —
ممنوع نسخة تالتة من المنطق. ولو ضفت قسم، اتأكد إنه بان في **التلاتة**:
`/marketplace` · `/add-listing` · «ضيف إعلان» في `/admin/listings`.

## 🔑 الموبايل واللوحة لازم يتفقوا على مين المدير (٢ سبتمبر ٢٠٢٦)
محمد: «لما باجي أدخل على حضور الموظفين من الـ٣ شرط في نسخة الموبايل
بيقولي مالكش صلاحية، مع إني بقدر أدخل من لوحة التحكم عادي».

🐞 `madmona_mgr_resolve` كان بيقبل `role IN ('admin','branch_manager')`
وبس — ودور محمد **`owner`**، وفريق مضمونة كلهم `staff` **عن قصد**
(`role='admin'` بيشغّل `is_admin()` في RLS على المنصة كلها). يعني
الحارس كان بيقيس بعمود مالوش لازمة في السياق ده، والصلاحية الحقيقية
في `permissions`.

✅ بقى يقبل: `owner` · `admin` · `branch_manager` · `permissions.all` ·
`permissions.can_manage_team`. ودي **نقطة واحدة بيعدّي منها ١١ دالة**
(`madmona_mgr_*`) — فالإصلاح فتح الشاشة كلها مش تاب الحضور بس.

⚠️ **القاعدة:** أي حارس صلاحيات لشاشة بيستخدمها الفريق لازم يتفق مع
باب اللوحة (`platform_admins`) — ويتجرّب **بجلسة المستخدم الحقيقية**
مش بالقراءة. ومتفحصش بـ`role` لوحده: عند مضمونة الدور متعمَّد إنه
مايكونش `admin`، والصلاحية في `permissions`.

## ☎️ قاعدة الأرقام: `phone_core` على الجنبين — دايمًا (٢ سبتمبر ٢٠٢٦)
🔴 **أخطر حاجة اتكشفت في الفحص الشامل:** `madmona_resolve` (اللي بتحدد
هوية أي مستخدم — أدمن/موظف/عميل) كانت بتقارن
`normalize_phone(x.phone) = v_phone` في **تلات مواضع**، و`v_phone` خام
من `madmona_accounts.phone_normalized` — **المخزّن بصيغتين** (`201…`
و`+201…`). أي حد جلسته على صيغة البلَس كان بيرجع بـ
`roles: {admin:[], employee:[], customer:[]}`: **الموظف بيدخل ويلاقي
نفسه مش موظف** — لا حضور ولا تاسكات ولا إشعارات — **ومن غير أي رسالة
خطأ توضّح السبب**. ٦١ جلسة نشطة على ٣٥ حساب. بعد الإصلاح رجعت ١٩ هوية
(٩ موظفين + ١٠ أدمن).

⚠️ ودي كانت السبب التاني وراء «مالكش صلاحية» في الحضور — اتخفت ورا
السبب الأول (`role='owner'`) فبعد إصلاحه الشاشة فضلت مقفولة.

✅ **القاعدة:** أي مقارنة أرقام في أي دالة أو كويري =
`phone_core(a) = phone_core(b)` — **الجنبين**. ممنوع تقارن عمود خام،
وممنوع تطبّق `normalize_phone` على جنب واحد. الفحص:
```sql
select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace
 where n.nspname='public' and pg_get_functiondef(p.oid) ilike '%phone_normalized%'
   and pg_get_functiondef(p.oid) not ilike '%phone_core%';
```

⚠️ **السبب الجذري لسه مفتوح — محتاج قرار محمد:** **٣٧ رقم ليهم حسابين**
في `madmona_accounts` (واحد بالبلَس وواحد من غيره) — من ضمنهم حساب محمد
ونورا وشهد. ده امتداد لمرض ٤.٧، بس في جدول ماكانش مغطّى بالحماية.
الدمج بيمس هوية مستخدمين فمايتعملش من غير إذن صريح.

## 🔴 العمولة كانت مقروءة للزائر (٢ سبتمبر ٢٠٢٦)
`commission_rules` (فيه `value` = النسبة و`auto_charge`) كان عليه
سياسة `USING(true)` — يعني أي حد معاه مفتاح anon (**وهو في كود الموقع**)
يقرا هوامش مضمونة كلها. ⛔ كسر مباشر لخط «العمولة في الباكاند فقط فقط
فقط». اتقفل، وآمن: `src/lib/commission.ts` بيقراه بـ`service_role` من
راوتات السيرفر بس.

⚠️ **الدرس:** فحص «الجداول اللي عليها RLS» **مش كافي**. لازم تفحص كمان
**السياسات المفتوحة** `USING(true)`:
```sql
select tablename, policyname, roles from pg_policies
 where schemaname='public' and cmd in ('SELECT','ALL')
   and (roles::text like '%anon%' or roles::text='{public}')
   and btrim(coalesce(qual,''))='true';
```

## 🧬 الجذر: توحيد الأرقام في الداتابيز (٢ سبتمبر ٢٠٢٦)
محمد: «لا، الصح يتعمل — وشوف إيه سبب المشكلة وعالجه من الجذر».

**السبب الجذري:** **٨ دوال** بتعمل `insert into madmona_accounts` وكل
واحدة بتحسب الرقم بطريقتها وبتكتبه **خام** (`employee_login_phone_pin` ·
`issue_madmona_token_for_admin` · `login_with_password` ·
`madmona_verify_magic_link` · `madmona_verify_otp` ·
`madmona_wa_login_confirm` · `tg_project_owner_account` · `wa_login_mint`).
**ولا واحدة بتوحّد الصيغة قبل الكتابة** — فنفس الشخص بياخد صف بـ`+201…`
من مسار وصف بـ`201…` من مسار تاني. النتيجة: ٣٧ رقم بحسابين، وهوية
الموظف بتضيع حسب مسار الدخول.

✅ **العلاج مش ترقيع الـ٨ دوال — الحماية في الداتابيز** (نفس منطق ٤.٨:
«مصدر الحسابات مش في الكود، فالحماية في الداتابيز بتشتغل مهما كان المصدر»):
- `trg_madmona_account_canon_phone` — بيوحّد `phone_normalized` قبل أي
  `insert/update`. الكود يبعت أي صيغة، الجدول يخزّن واحدة.
- `madmona_accounts_phone_uniq` (فهرس فريد) — **مستحيل** حسابين بنفس
  الرقم تاني.

**الدمج اتعمل من غير حذف:** ١٩٣ جلسة اتوجّهت للحساب الناجي (الأقدم)،
والصف المكرر أخد رقم «شاهد قبر» `merged:<الرقم>:<id>` فمايطابقش أي بحث
والأثر محفوظ. النتيجة: صفر بصيغة `+` · صفر مكرر · ٢٥٣ حساب (مفيش صف ضاع)
· صفر جلسة مكسورة.

🧪 **الحماية اتجرّبت بمحاولة حقيقية مش بالقراءة:** `insert` بـ
`+201002229982` (رقم موجود) → **اترفض**؛ رقم جديد اتبعت بـ
`+201999888777` → **اتخزّن `201999888777`**.

⚠️ **القاعدة:** أي عمود بيخزّن رقم تليفون في أي جدول لازم يكون عليه
تريجر توحيد + قيد فريد. المقارنة بـ`phone_core` علاج للعَرَض؛ العلاج
الجذري إن العمود نفسه مايقبلش غير صيغة واحدة.

📋 **ملاحظة للمراجعة (مش اتغيّرت):** `can_manage_team` = `false` عند
**نورا · عبير · شهد** بينما `true` عند إيمان ومديحة — مع إن القاعدة
فوق بتقول إن الستة عندهم صلاحيات الأدمن ما عدا الفلوس والتسعير.
(سامية `false` **عن قصد** — الفريق مستثنى صراحةً عندها.)

## 🔴🔴 عمود جديد من غير GRANT = الشاشة كلها بتقع (٢ سبتمبر ٢٠٢٦)
ضفت عمود `listings.seller_display_name` وبدأت أقراه من الفرونت —
**السوق كله** رجّع `401` و«مش قادرين نحمّل العروض دلوقتي». مش
الإعلانات اللي عليها العمود بس: **كل** الإعلانات.

**السبب:** صلاحيات `anon` على `listings` ممنوحة **عمود بعمود**
(٢٦٧ عمود في `column_privileges`) مش على الجدول كله. العمود الجديد
مش فيهم — و**PostgREST بيرفض الاستعلام كله** لو فيه عمود واحد مش
مسموح، مابيتجاهلوش.

⚠️ **ده أخطر من فخ الـGRANT بتاع ٤.٦** — ده على مستوى **العمود**،
و`has_table_privilege` بترجّع `true` وتغشّك تمامًا.

✅ **القاعدة:** أي عمود جديد على جدول بيتقري من المتصفح لازم ياخد
GRANT صريح **في نفس الميجريشن**:
```sql
grant select (اسم_العمود) on public.الجدول to anon, authenticated;
-- والفحص:
select has_column_privilege('anon','public.listings','اسم_العمود','SELECT');
```

## 🔍 البحث في السوق بيدوّر في كل حاجة (٢ سبتمبر ٢٠٢٦)
التاب الافتراضي «بيع». فاللي بيدوّر على «شعبان» (مطعم) أو «لانسر»
(عربية) من الشاشة الأولى كان بياخد **«مفيش نتائج»** والإعلان منشور —
بس في تاب تاني. اتأكد لايف: `?q=شعبان` = ٠ · `?track=restaurants&q=شعبان` = ١.

✅ أول ما المستخدم يكتب في البحث، الفلتر بالتصنيف/القسم **بيتشال**
والبحث بيدوّر في السوق كله. التصفّح العادي زي ما هو.
⚠️ ودي كانت غالبًا وراء شكوى «فيه إعلانات مش موجودة» — الإعلان موجود
والعميل مش لاقيه.

## 📣 حملة معرض Egypt Projects 2026 — دروس يوم ٤ سبتمبر ٢٠٢٦
- **رقم 337 اتفصل (qr_ready) بعد ٤ رسايل باردة** من طابور الحملة —
  ده الإنذار اللي بيسبق الحظر، وقاعدة «رد بس» (٢٠ يوليو) كانت صح.
  الحملات الباردة **ماتطلعش من رقم شخصي/بيزنس موثّق**. القرار بعدها
  لمحمد (رقم مخصص للحملات).
- **الپيكر في `wa-queue-send` بيستبعد الصف بصمت لو `campaign_name` فاضي**:
  `.not(col,'in',…)` مع NULL = الصف يختفي من غير lock ولا halt ولا خطأ.
  اتصلح بـ`.or(col.is.null, col.not.in…)`. أي رسايل تتحط في الطابور
  يدوي لازم `template_vars.campaign_name` يتحط.
- **حارس «رد بس» (`reply_only_mode`)**: `on` = يمنع كل بدء محادثة مهما
  كانت القايمة؛ `campaigns` = يسمح للأسماء في `reply_only_campaigns` بس.
  الكارت في `/admin/sending`.
- **تريجر جديد يتجرّب على المسارين**: الرفض المتوقع **والنشر العادي**
  لمورد عنده كتالوج. `tg_block_menu_item_listing` كان بيسأل عمود
  `mart_products.name` (الصح `name_ar/name_en`) ووقّف **كل نشر على
  المنصة ٧٥ دقيقة** من غير ما حد يحس.
- **Supabase Storage بيرفض مفاتيح فيها عربي** («Invalid key») — اسم
  الملف في التخزين = id أو ASCII بس؛ العربي يفضل في العنوان.
- **شاشات الأدمن بتدخل بالكوكي من غير باسورد** — أي زرار
  `disabled={!password}` بيطلع رمادي. وأي هوك جديد فوق كل `return`
  مبكر. والتحقق بـ`npm run build` + قراية مسار الرندر، مش typecheck.
- **«خلص» = الملف اتغيّر في `git show --stat`**: كوميت 9c321b2 قال إنه
  شال العمولة من /expo وماكانش فيه غير sw.js (سكريبت وقع قبل الكتابة).
  التحقق على المحتوى **قبل** الكوميت مش بعده.
- **صفحة العارض تتبني بس لو ≥٦ منتجات بصور مختلفة وأسماء حقيقية**
  (مش «Quick view»/«product_img1»/صورة موقع واحدة مكررة). من ١٧ «جاهز»
  بالفحص السطحي طلع ٥ بس فعلًا: egypoxy · eltawheed · glassrock ·
  penetron · rockal (+ al-tib). الباقي محتاج كتالوجهم PDF.

## 🧞 المارد بأدواته الكاملة على جوجل ستوديو — أمر محمد نصًا (٤ سبتمبر ٢٠٢٦)
محمد: «عايزين نفعل المارد وأدواته الكاملة على جوجل ستوديو». ده الأمر النصّي
اللي قفل ٢٧/٨ كان بيشترطه. اتفتحت الـ٢٦ أداة كلها في `marid_tool_settings`
(كانت ٦). الموديل `gemini-flash-lite-latest` — صفر أنثروبيك. الكاش دقيقة،
مفيش نشر. الرجوع: `update marid_tool_settings set enabled=false where
tool_name not in ('search_catalog','list_categories','who_is_this',
'get_my_orders','forward_to_supplier','record_unmet_demand')`.
البرومبت (`customer-concierge.ts`) أصلًا بيأمر باستخدام أدوات التسجيل لما
تكون مفتوحة — ملاحظات «مش شغلك» كانت بتتحقن بس للأدوات المقفولة.
⚠️ أرقام عارضي المعرض الـ٥٤ + موظفي مضمونة (بأي حالة) مسكّتين على المارد
(`marid_mute_list`) — محمد بيرد عليهم بنفسه.

## 🧞 برومبت المارد ثبت على جوجل المجاني — ٤ سبتمبر ٢٠٢٦ (مساءً)
- **اختبار حقيقي بإدخال عربي سليم** (رقم وهمي، شات الموقع = نفس المخ):
  بائع «عندي شقة للبيع في المعادي…» → `add_listing_oneshot` عمل درافت
  (`sale-properties-apartment` · ٣٬٥٠٠٬٠٠٠) والرد «استلمت عرضك 👌 اتسجّل —
  ابعت الصور». مشتري «عايز أأجّر… ١٥ ألف» → `record_unmet_demand` سجّل
  (المعادي · ١٥٠٠٠) والرد «مفيش… سجّلت طلبك». مصري، ١٨–٢٣ ث، صفر مكتبة.
- ⚠️ **فخ اختبار كلّف ساعتين:** `curl -d "…عربي…"` من Git Bash هنا بيبعت
  العربي **علامات استفهام** — الموديل كان بيرد على `????` فرشّح مشاريع
  العاصمة لبائع. الاختبار الصح: اكتب الـJSON في ملف UTF-8 وابعته
  `--data-binary @file`. وأول ما تشك في رد، اقرا `whatsapp_messages.body`.
- المهلة على مستوى **الطلب** (٤٥ ث تتقسم على ٤ أدوار) — مش على النداء.
  الموديل الأساسي بالقياس `gemini-3.1-flash-lite` (لو `GEMINI_MODEL` مش
  متحدد في بيئة Vercel — لو متحدد بيغلب). `3.5-flash-lite` بيرد في ٥٤ ث ساعات.
- المكتبة الاحتياطية (`marid_offline_reply`) بتصنّف **الرسالة الحالية بس**
  (كانت بتاخد السياق كله وتلاقي «للبيع» في رسالة قديمة).
- أرقام الاختبار الوهمية `01999888777–784` اتمسحت بالكامل (محادثات ·
  درافتس · مورد · ٥ حسابات). أي اختبار جاي: نفس النطاق وامسح بعده.
- **البوت لكل بيزنس** (موافقة محمد ٤/٩): مفيش جدول بيربط جلسة واتساب
  بمورد — `session_id` بيوصل لنقطة الدخول بس. البناء: `supplier_wa_channels`
  + تمرير الجلسة للمخ + برومبت مقفول على كتالوج البيزنس بنفس هيكل
  البرومبت ده (تصنيف ميكانيكي → أداة واحدة) ونفس الحراس.

## 🌍 الأرقام غير المصرية كانت مرفوضة في كل مكان (٤ سبتمبر ٢٠٢٦ — مساءً)
محمد: «رقم صاحب بيزنس لمونة 0585280538 — رقم إماراتي». الفحص: الرقم
موجود عندنا (`971585280538`)، **حاول يدخل ١١ مرة بلينك واتساب وصفر نجاح**
وصفر حساب. الأسباب كانت خمسة في خمس أماكن:
- `auth-helpers.normalizePhone` بترجّع null لأي رقم مش `+20` → `bad_identifier`.
- `phone_core` كانت بتسيب البلس (`+971…` ≠ `971…`) — هوية واحدة بتلات صيغ.
- `ensure_supplier_for_phone`: `'20' || right(digits,10)` → رقم مصري وهمي.
- `chat/normalizeEg` و`whatsapp.normalizePhone` و`chat/dm` و`invite-accept`:
  أي `0…` = مصر، والإماراتي المحلي (05…) كان بيتحوّل لـ`200585…`.
✅ القاعدة الواحدة دلوقتي في كل الأماكن دي: **مصر = `01xxxxxxxxx` أو
`1xxxxxxxxx` أو `20xxxxxxxxxx` بس؛ غير كده الرقم دولي كامل بكود الدولة**
(`+`/`00` بيتشالوا). المصري بيطلع زي ما كان بالظبط (اتأكدت بالقياس).
⚠️ الرقم المحلي الأجنبي من غير كود دولة (`0585…`) مش قابل للتمييز — يفضل
زي ما هو ومايتطابقش مع `971585…`. اطلبوا الرقم الدولي دايمًا في الفورمات.

## 🍽️ منيو المطاعم بتصميم TableQR + العملة من الداتابيز (٤ سبتمبر ٢٠٢٦)
محمد: «عايزين نعمل الديزاين المطاعم زي demo.tableqr.co — سواء في عرض
الماركتبليس أو في عرض البيزنس بتاع صفحة المورد، ونبدأ بلمونة».
- **كومبوننت واحد** `src/components/RestaurantCloud.tsx` بيتعرض في
  `/marketplace/[slug]` (بدل `RestaurantMenu` القديم) وفي `/s/[slug]`
  (بدل كارت الإعلان الواحد + الخدمات) لأي مطعم/كافيه/عصائر. نفس
  `restaurant_menu_items` + `_sizes` ونفس السلة (`src/lib/cart.ts`).
  أي تعديل على شكل المنيو يتعمل فيه هو — ممنوع نسخة تانية.
- 💱 **العملة من الصف مش «ج» ثابتة**: `listings.currency` ·
  `restaurant_menu_items.currency` · `mart_products.currency` (الافتراضي
  EGP) عبر `src/lib/currency.ts` (`currencyLabel` · `priceLabel`).
  لمونة (دبي) بالدرهم — سعر بعملة غلط = سعر مخترع (خط أحمر).
  `public_storefront_listings` و`public_salon_landing` بقوا بيرجّعوا
  `currency`/`slug`/`city`. أي شاشة جديدة بتطبع سعر تنادي `currencyLabel`.
- ⚠️ أداة تصوير المتصفح المدمج بترجّع **شاشة فاضية بعد أي سكرول** على
  الصفحات الطويلة (حتى `/marketplace` نفسها) — مش عيب في الصفحة.
  للتحقق البصري: `resize_window` بارتفاع كبير (3600) وصوّر من غير سكرول،
  وارجع `preset: desktop` بعدها. والـDOM بـ`find`/JS دليل كفاية.

## 🔐 توثيق الواتساب كان بيروح لرقم واقع (٥ سبتمبر ٢٠٢٦)
محمد: «لمونة بيبعت توثيق بالواتساب ومش عارف يستلم حسابه».
🐞 `pickLoginWa` في `/api/auth/wa` كانت بترجّع **337** فورًا لو صف
`wa_number_configs` بتاع 9982 عليه `enabled=false` (ده فلاج سكوت المارد،
اتقفل ٣/٩) — و337 رقم الحملات وحالته «initializing». ١٢ كود في يوم واحد
راحوا لواتساب مش متوصل وولا واحد اتأكد. ✅ الاختيار بقى `pickAuthWaSession`
بس (الترتيب + الجاهزية الحية + استبعاد رقم الحملات). وكود `MADxxxxx`
بيتأكّد في المخ **قبل** `marid_should_skip` — المسكّت كان بيتبلع.
⚠️ **الفحص الأول لأي «التوثيق مش شغال»:**
```bash
curl -s -H 'content-type: application/json' -d '{"action":"start"}' https://www.madmonacairo.com/api/auth/wa
```
`wa_number` لازم يكون رقم حالته `ready` على OpenWA (`/api/sessions`).
ومحاكاة الويبهوك (`event: message.received` على `/api/whatsapp/openwa?token=`)
بترجّع `login: verified` — من غير ما تبعت واتساب حقيقي.

## 🧭 خطوات استكمال حساب الشركة + لوحة البيزنس بتوكن الواتساب (٥ سبتمبر ٢٠٢٦)
محمد: «لما أي مورد يستلم حسابه نمشي معاه بخطوات استكمال حساب الشركة
(١- فروع ٢- موظفين …) لحد ما يخلص بناء الشركة بالكامل».
- `business_setup_progress(p_supplier_id, p_token)` → ٧ خطوات محسوبة من
  الداتا الحقيقية (هوية · فروع · مواعيد · موظفين · منيو/كتالوج · إعلان
  منشور · بوت الواتساب اختياري). **مفيش تشيك-بوكس يدوي** — الخطوة بتتقفل
  لوحدها. الكومبوننت `BusinessSetupSteps` في لوحة الإدارة (فوق الموديولات)
  ومختصر في كارت «بيزنسي» في حسابي (للمالك). خطوة جديدة = تتضاف في الدالة
  بس، والفرونت بيعرضها لوحده (label · hint · href · done · detail).
- 🔴 **اتكشف بالتجربة بتوكن لمونة:** الصفحة الرئيسية للوحة كانت بتقرا
  `suppliers` + المعاملات بعميل anon مستقل (`createClient` بالمفتاح العام)
  → «شركة غير موجودة» لأي مالك من بعد إغلاق ٢٨/٨. اتحلّت بـ
  `business_overview_bundle(p_supplier_id, p_token)` عبر `financeRpc`.
  ⚠️ **أي `createClient(anon)` مستقل جوّه شاشة في اللوحة = قنبلة موقوتة**:
  الفحص `grep -rn "createClient(" src/app/admin/business-finance`.
- 💰 نفس الصفحة كانت بتعرض «كوميشن مضمونة» + عمود كوميشن + «base commission
  X%» لصاحب البيزنس. بقت للأدمن بس (`is_admin` من الـRPC، والأرقام نفسها
  null لغيره في الداتابيز). **أي شاشة في اللوحة بتطبع عمولة تتحط ورا `isAdmin`.**

## 🔴 دوال لوحة البيزنس كانت مفتوحة لأي حد (٥ سبتمبر ٢٠٢٦)
اتكشفت وأنا بجرّب خطوات الاستكمال بتوكن لمونة الحقيقي:
`employee_set_password` · `set_employee_email` · `admin_update_employee_contact` ·
`admin_list_employees_for_manage` كانوا SECURITY DEFINER **متاحين لـanon من غير
أي حارس** — أي حد بالمفتاح العام يغيّر باسورد/رقم/PIN أي موظف (فريق مضمونة
كمان) ويدخل بحسابه. وباقي `admin_*` (فروع · خدمات · مهام · موديولات) مفتوحين
لأي مستخدم مسجّل على أي بيزنس.
✅ الحارس الواحد `schedule_edit_ok(supplier, p_token)` على ١٢ دالة +
`biz_edit_ok_{employee,branch,service,task}` بيحلّوا المورد من الصف. التواقيع
القديمة اتمسحت. اتجرّب بمحاولة حقيقية كـ`anon` → «مالكش صلاحية».
⚠️ **الفحص الدوري** (لازم يطلع فاضي):
```sql
select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace
 where n.nspname='public' and p.prosecdef and proname like 'admin\_%'
   and pg_get_functiondef(p.oid) not ilike '%_ok(%' and pg_get_functiondef(p.oid) not ilike '%is_admin%';
```
🧰 **الفرونت:** `withToken(supabase)` في `src/lib/rpc.ts` بيضيف `p_token`
للدوال المحروسة بس (قايمة `TOKEN_FNS` — دالة جديدة بتوكن = تتضاف هناك).
قراءة `suppliers` من أي شاشة في اللوحة = `business_supplier_head` مش
`from('suppliers')` (مقفول لصاحب البيزنس من ٢٨/٨).
🧑‍💼 **self-serve لصاحب البيزنس:** `business_branch_save` ·
`business_employee_save` · `business_employee_delete` — بدل insert مباشر كان
بيتبلع بصمت. صاحب البيزنس بتوكن الواتساب بس (من غير جلسة Supabase) بيقدر
دلوقتي يفتح: الرئيسية · الفروع · الفريق · الإعدادات · الكتالوج (اتجرّبوا بتوكن
لمونة). محرر المنيو (`/supplier/marketplace/<listing>/menu`) محتاج جلسة Supabase
— بتتصكّ مع توثيق الواتساب أصلًا.

## 🌍 الدولة والعملة — فتح الخليج (٦ سبتمبر ٢٠٢٦)
محمد: «لمونة بيزنس في الإمارات — تاب للعملة والدولة عند التسجيل ونعمّم
ونفتح دول مجلس التعاون» + «هيعرف منين الدولة والزائر هيتعرض ليه إيه».

- **المرجع:** جدول `countries` (ISO-2 · عملة · كود اتصال · علم) — مصر +
  ٦ دول خليج. و`cities` (٤٣ مدينة خليجية). نسخة الواجهة `lib/countries.ts`.
- **العملة مابتتسألش — بتتشتق من الدولة.** تريجر `set_currency_from_country`
  على listings · listing_drafts · suppliers · marketplace_suppliers ·
  **property_market_items** (البورصة — اتضافت ٦/٩، وأنون عنده SELECT
  على مستوى الأعمدة هناك فالعمود الجديد خد GRANT صريح).
  **الدولة بتكسب دايمًا** — اتجرّب: `country='EG'`+`currency='AED'` → EG/EGP.
  الكتالوج (menu_items · mart_products) **مالوش تريجر** ومربوط بـ`listing_id`
  مش `supplier_id` — يتحدّد صراحةً عبر إعلانات المورد.
- **الزائر — `lib/visitor-country.ts`، الترتيب الأكيد قبل التخمين:**
  كوكي `madmona_country` (اختياره) → هيدر `x-vercel-ip-country` (مجاني،
  صفر API) → `EG`. كود مش في قايمتنا يقع على مصر.
  الفلترة في **Server Component** — فزرار الدولة (`CountryToggle`) بيكتب
  الكوكي **وبيعمل ريلود**. `/marketplace` و`/real-estate/market` بقوا
  dynamic (ƒ) عن قصد — `revalidate=3600` في البورصة بقى بلا أثر.
- `get_marketplace_category_counts(p_country)` — الـ0-arg اتمسحت.

⚠️ **`<TopNav>` مش في الـlayout — بيتركّب لكل صفحة بإيدها** (add-project ·
business-lounge · real-estate…). **الماركت والهوم عندهم هيدر خاص.** أي
زرار «في الهيدر» لازم يتحط في ٣ أماكن: TopNav + MobileHome +
`MarketplaceClient.tsx` (هيدر الماركت) — وإلا بيغيب عن الصفحة اللي بتهم.
📏 **الفحص:** SSR HTML مش دليل على غياب زرار في هيدر كلاينت. الصح `find`
في المتصفح — ولما اللغة والدولة يغيبوا **مع بعض** يبقى الهيدر نفسه مش
متركّب، مش الزرار.
ℹ️ «القاهرة» في HTML زائر دبي = JSON-LD الشركة في `layout.tsx`
(`addressCountry: EG`) — عنوان مضمونة نفسها، صح ويفضل.

## 💱 العملة من الصف في كل شاشة — تريجرات الأصناف والحجوزات (٦ سبتمبر ٢٠٢٦)
محمد: «اتأكد إن كل الصفحات والشاشات متوافقة مع الشغل الجديد». صفحة لمونة كان
فيها «٢٢ ج.م» وسط ٢٠ سعر بالدرهم — مصدره `ProductBuyBox` في `OrderActions.tsx`
(`t('common.egp')`)، وكل شاشات الحجز/الأوردرات/ERP كانت بتطبع «ج.م» حرفيًا.
- **أي سعر بيتطبع = `currencyLabel(row.currency)`** — ممنوع `t('common.egp')`
  أو «ج.م» حرفيًا في أي شاشة عميل/مورد. الفحص: `grep -rn "common\.egp\| ج\.م" src/app`.
  الاستثناء الوحيد: أسعار الصرف والذهب (`FinancialTicker` · بورصة رجال الأعمال) —
  دي بالجنيه فعلًا.
- **العملة بتتوارث بتريجرات مش بالكود:** الحجز من الإعلان
  (`trg_booking_currency_from_listing`)، الأوردر من المورد
  (`trg_order_currency_from_supplier`)، والأصناف (`pricing_rules` ·
  `restaurant_menu_items` · `mart_products`) من الإعلان
  (`set_child_currency_from_listing`). الكود يبعت أي حاجة، المصدر بيكسب.
- `v_business` بقى فيه `country` + `currency` — شاشات ERP تاخد العملة من
  `biz.currency` (نوع `Business` في `business-access.ts`).
- 🔴 `supplier/marketplace/bookings/[id]` و`orders/[id]` كانوا بيعرضوا «عمولة
  مضمونة −X» و«صافيك» للمورد من زمان — اتشالوا (قاعدة ٤/٩).
- ⚠️ `src/types/supabase.ts` **ملف يدوي مش متولّد** — أي عمود/جدول جديد يتضاف
  فيه بإيدك (اتضاف: currency/country على ٦ جداول + `cities` + `countries` +
  ويو `v_business`). وإلا PostgREST typing بيرمي `SelectQueryError` على الاستعلام كله.

## 🤖 بوت واتساب البيزنس — ربط ذاتي + ترشيح من الكتالوج + ليد في CRM (٦ سبتمبر ٢٠٢٦)
محمد: «محتاجين نفعّله بحيث يرشّح منتجات البيزنس لصاحب البيزنس ويظبط ليه الليد».
- **الربط من لوحة البيزنس** `/admin/business-finance/<id>/whatsapp` (تاب «🤖 بوت
  الواتساب» + خطوة ⑦ في الاستكمال): `/api/business/wa-channel` بيعمل جلسة
  OpenWA (`POST /api/sessions` → `/start` → `/webhooks` على `/api/whatsapp/openwa?token=`)
  وبيعرض QR (`GET /api/sessions/:id/qr` → `{qrCode}` لما الحالة `qr_ready`). أول ما
  تبقى `ready` بنعرف الرقم ونكتب `supplier_wa_channels.session_id = الرقم` (ده اللي
  `business_channel_context(data.to)` بتطابق عليه) + صف `wa_number_configs` (openwa).
- **الرد:** `business-concierge` مقفول على كتالوج البيزنس، أسعاره بعملته (لمونة د.إ)،
  ومخرجاته JSON `{reply, lead:{name,interest,intent,wants_human}}`. وضع البيزنس =
  صفر أدوات · مفيش دعوة لشات مضمونة · مايدخلش مكتبة المارد.
- **الليد:** `business_bot_record_lead()` → upsert في `biz_customers`
  (`source='whatsapp_bot'` · tags واتساب-بوت + intent · notes بتتراكم بالتاريخ) +
  إشعار داخلي لصاحب البيزنس (`notification_queue` type `business_lead` بلينك CRM).
- ✅ **اتجرّب حقيقي على لمونة** بمحاكاة الويبهوك (٥ رسايل): الرد من المنيو بالدرهم،
  والليد «منى · hot · ٢ أفوكادو توصيل البرشا» اتسجّل + الإشعار اتطبخ. اتمسح بعدها.
- 🐞 **درسين كلّفوا ٤ نشرات:** (١) `revoke … from public` بيشيل EXECUTE من
  `service_role` كمان — الدالة اللي السيرفر بيناديها لازم `grant … to service_role`
  صراحةً. (٢) **supabase-js مابيرميش exception على خطأ RPC** — `try/catch` مابيمسكش
  حاجة، لازم تقرا `{ error }` من النتيجة. الفشل كان صامت تمامًا (permission denied
  وبعدها قيد `biz_customers_source_check`) والرد بيطلع عادي.
- ⚠️ `receivingNumber` في ويبهوك OpenWA كانت مصري بس (`^20\d{10}$`) — بقت أي رقم
  دولي؛ من غيرها رقم بيزنس خليجي مربوط مايردّش خالص.

## 📅 الحجز: يوم وساعة واحدة لأي خدمة — «من/إلى» للإيجار بس (٦ سبتمبر ٢٠٢٦)
محمد: «إزاي في حجز العيادات مكتوب من وإلى وهو المفروض يوم واحد؟». صفحة `/book`
كانت بتحدد «ميعاد واحد» بقايمة أسماء (٣ أنواع) فأي `period_type` جديد
(`per_session` بتاع العيادات) كان بيقع على فورم الإيجار. القاعدة المعكوسة دلوقتي:
**المدة (hourly/daily/weekly/monthly) هي الاستثناء، وأي نوع تاني = يوم وساعة**؛
والساعة = يوم واحد + «عدد الساعات». نوع تسعير جديد مايحتاجش تعديل في الصفحة.

## 🧭 ويزارد «كمّل شركتك» — التوجيه خطوة بخطوة (٦ سبتمبر ٢٠٢٦)
محمد: «هو محتاج توجيه لأنه مش هيعرف يضيف موظف إلا لما يضيف فرع».
- خطوات الاستكمال (٥/٩) كانت **تشيك ليست** — لينكات جنب بعض. دلوقتي فيه ويزارد
  `/admin/business-finance/<id>/setup` (تاب «🧭 كمّل شركتك»): خطوة واحدة على الشاشة
  بالترتيب — الهوية ← **الفرع بعنوانه ومواعيده (فورم جوّه الخطوة)** ← **الموظفين
  (لو مفيش فرع: «ضيف فرعك الأول» ورجوع)** ← الكتالوج ← إعلان منشور ← بوت الواتساب.
- المصدر: `business_setup_state(p_supplier_id, p_token)` = التقدم + الفروع + الموظفين
  في نداء واحد. الكتابة بنفس دوال الفروع والفريق (`business_branch_save` ·
  `business_employee_save`) — **ممنوع** مسار كتابة موازي للويزارد.
- **صاحب البيزنس الجديد بيوصلها لوحده:** `/supplier/register` بيحوّله على
  `/setup?welcome=1` (كان بيودّيه على فورم إعلان). كارت «بيزنسي» في حسابي وشريط
  الخطوات في اللوحة بيودّوا عليه. خطوة جديدة = تتضاف في `business_setup_progress`
  + ترتيبها في `ORDER` جوّه الصفحة (لو ليها فورم جوّاها).
- 🧪 التجربة بجلسة المالك: `set_config('request.jwt.claims', '{"sub":"<profile_id>",
  "role":"authenticated"}', true); set local role authenticated;` وبعدين نادي الدالة.

## 🛵 الدليفري — أسطول مضمونة + تسعير بالمسافة والمركبة (٦ سبتمبر ٢٠٢٦)
محمد: «موديل الدليفري يفتح شاشة خاصة بالدليفري — بيجيله الأوردر عبارة عن إيه
ومكان الاستلام ومكان التسليم، والسعر بيتحدد على حسب المسافة ونوع المركبة».
- **الموديل = أسطول واحد تحت مضمونة** (مش طيارين لكل بيزنس): الطيار بيسجّل
  `/delivery/register`، الأدمن بيراجع أوراقه ويسند، شاشته `/delivery/<token>`.
  مضمونة «في النص» — الطيار بتاعنا هو اللي بيضمن الاستلام والتسليم والتحصيل.
- **اللوحة `/admin/delivery`**: الرحلات (تعبئة من أوردر الماركت: الاستلام من فرع
  المورد، التسليم من `delivery_lat/lng` العميل، الأصناف والتحصيل) · الطيارين
  (أوراق/اعتماد/رفض) · التعريفة. البوابة `/api/admin/delivery` (كوكي اللوحة) →
  `/api/delivery` (بالسر) — **مفيش منطق في البوابة**، مصدر واحد.
- **السعر:** `delivery_quote` = max(أقل أجرة، فتح عدّاد + لكل كيلو × المسافة)،
  المسافة `delivery_distance_km` = هافرساين × ١.٢٥ (صفر API خارجي). تريجر
  `trg_delivery_trip_autoquote` بيسعّر الرحلة لوحدها لو فيها إحداثيات + مركبة
  و`fee_source='auto'`. الإسناد بيفضّل طيار بنفس المركبة.
- ⛔ **التعريفة (`delivery_vehicle_types.base_fee/per_km/min_fee`) فاضية عن قصد** —
  خط أحمر «ممنوع تخترع سعر». محمد بيكتبها من تاب «التعريفة»؛ لحد ما تتحدد
  `configured=false` والرحلة سعرها يدوي.
- ⚠️ أوردرات الماركت الحالية **من غير إحداثيات** (`delivery_lat/lng` فاضية) —
  التسعير الأوتوماتيك محتاج الـcheckout يسجّل لوكيشن العميل (لسه ماتعملش).

## 📍 لوكيشن العميل في الـcheckout (٦ سبتمبر ٢٠٢٦)
محمد: «أيوة طبعًا» — الأوردرات كلها كانت من غير `delivery_lat/lng` لأن الزرار كان
«اختياري» ومستخبي، فالدليفري ماكانش يتسعّر بالمسافة.
- الكتلة «📍 حدد مكانك على الخريطة» بتنبيه لو ناقصة (مش بتمنع الطلب)، زرار
  «موقعي الحالي»، وخانة **لصق لينك لوكيشن** (واتساب/خرايط جوجل): الطويل بيتقرا
  في المتصفح (`parseLatLng` من `LocationPicker`)، والقصير `maps.app.goo.gl` بيتفك في
  `/api/geo/resolve` (السيرفر بيتبع التحويل — صفر مفاتيح، دومينات خرايط بس).
  معاينة خريطة `maps.google.com/maps?output=embed` بعد التحديد.
- الإحداثيات بتتبعت بعد إنشاء الأوردر عبر `set_order_location` (زي ١٤/٨) →
  تعبئة رحلة الدليفري من الأوردر بقت بتجيب التسليم بإحداثياته → التسعير بالمسافة.
- 🧪 اتجرّب لايف بـPlaywright (سياق بصلاحية geolocation): لصق لينك → الخريطة
  اتثبتت على الإحداثيات. ⚠️ بعد ما اللوكيشن يتحدد الزرار اسمه بيبقى «موقعك اتسجّل ✓».

## 💼 التسعير — السوق مجاني · السيستم بـ١٠٠٠ ج «بدل كتير» (٦ سبتمبر ٢٠٢٦)
محمد نصًا: «عايز أستهدف أصحاب البيزنس إنهم يشتركوا معانا بـ١٠٠٠ ج فقط لا غير بدل
٢٠٠٠ ج… والـ١٠٠٠ نظير السيستم الـERP والـCRM» — أورجانيك بالكامل، وأنا اللي متولّي
التسويق (مش Postiz ولا Railway).
- **رسالتين ما يتخلطوش:** (١) **العرض على سوق مضمونة مجاني** بالعمولة فوق السعر
  (قاعدة ٢٠ يوليو و٤/٩ — «السعر اللي بتطلبه هو اللي بتاخده»). (٢) **سيستم إدارة
  البيزنس** (لوحة الإدارة: فروع · موظفين · CRM · بوت واتساب · دليفري …) = **١٠٠٠ ج
  بدل ٢٠٠٠**. أي نص يقول «اشتراك» لازم يبقى عن السيستم — مش عن العرض على السوق.
- ⛔ **ممنوع ذكر السعر القديم** — محمد بعدها: «العرض ساري لعدد حساب — هنقول بـ١٠٠٠ ج بدل
  كتيييير، لا هنقول ٢٠٠٠ ولا ٣٠٠٠». يعني في أي نص: «١٠٠٠ ج بدل كتير» + «لعدد محدود من
  الحسابات». عدد الحسابات من `site_settings.erp_offer_seats` (لو فاضي: «عدد محدود» من غير
  رقم)، والمتبقي = العدد − `campaign_leads` المتحوّلة (رقم حقيقي) عبر `/api/campaign/offer`.
- **مدة الاشتراك محمد ماقالهاش** → الصفحة `/pro` مابتنطقش بمدة إلا لو اتحطت في
  `site_settings.erp_offer_period` (بتتقرا من `/api/site-settings` — قايمة بيضا).
- **الليدات:** `campaign_leads` (campaign + UTM) عبر `/api/campaign/lead` → پوش
  لفريق الإعلانات → كارت «📣 الحملات وليداتها» في الداشبورد (عدّادات + حالة
  الليد + واتساب). حملة جديدة = صفحة بتبعت `campaign` + سطر في `CampaignLinks`.
- **النشر:** أنا بجهّز (ريل + كابشن + لينك بـUTM) ودرافت — الضغطة النهائية من
  محمد/سامية. مفيش إعلانات مدفوعة ومفيش واتساب بارد.
- 🧭 **(٧/٩) محمد نصًا:** «الاشتراك لبرامج الإدارة فقط، لكن الباقي بعمولة أو نسبة أو
  رقم ثابت — عايزين نقول مضمونة إيه، وإن الـ١٠٠٠ ج لبرنامج الإدارة». الصياغة
  المعتمدة في `/pro` وأي محتوى: **«مضمونة = ١٢ أبليكيشن في واحد وإحنا في النص
  بنضمن الصفقة»** → (أ) **المنصة** (سوق · إيجار · عيادات · صالونات · أوردرات ·
  دليفري · محترفين) = **بعمولة على المعاملة، من غير اشتراك** (من غير رقم عمولة).
  (ب) **برنامج الإدارة** (حسابات · CRM · موظفين/حضور/مرتبات · بوت واتساب · موقعك)
  = **١٠٠٠ ج بدل كتير** لعدد محدود. ممنوع كلمة «اشتراك» على المنصة.

## 📣 النشر الأورجانيك من كروم السوشيال — دروس ٧ سبتمبر ٢٠٢٦
محمد: «شغّل الحملة وافتح البراوزر وتولى كل حاجة… أنا مش فايق للتسويق».
- **الجلسات في `chrome-social-profile` (بورت 9223)** — تشغيله `launch-chrome-social.ps1`.
  `curl localhost:9223` بيفشل بسبب بروكسي البيئة؛ `fetch` من node بيشتغل. الاتصال
  `connectOverCDP('http://127.0.0.1:9223')`.
- 🐞 **CDP بيتجمّد كله** لو تاب رفع تيك توك اتعمل عليه `page.goto` (حوار «Leave site?»)
  أو لو المحرر فتح وسط عشرات الـworkers. العلاج: اقفل التابات من
  `GET /json` + `/json/close/<id>`، ولو فضل معلّق أعد تشغيل كروم السوشيال.
  وقبل أي سكريبت جديد اقتل عمليات node القديمة اللي ماسكة CDP.
- **إنستجرام ✅ بيشتغل:** `posters/post-instagram-reel.cjs` — Create (لينك `a[href="#"]`)
  → «Post» (نص مطابق) → file chooser → Next ×٢ → كابشن `insertText` → Share.
  اتنشر ريل البراند ٧/٩ فعلًا.
- **تيك توك:** `posters/post-tiktok.js` — حوار «Save draft? … Save anyway» كان بيبلع
  الحفظ بصمت (اتصلح). زرار النشر لازم `button:text-is("Post")` — `has-text` بيطابق
  لينك «Posts» في السايدبار ويطلّع «exit?». `TT_POST=1` بينشر مباشرة (للفيديو اللي
  فيه صوت مدمج). محرر الويب فيه لوحة Sounds بتاب «Unlimited» (المكتبة التجارية)
  بس الصفحة بتقع «Something went wrong» بعد أي تدخل تاني — ماتعتمدش عليها.
- **فيسبوك ✅ الطريق اللي بيوزّع (٧/٩ مساءً — محمد: «انشر على صفحتي الشخصية»):**
  من كروم محمد (إضافة Claude in Chrome): الهوم → input الملف اللي جنب «Photo/video»
  في كومبوزر «Create a post» → `file_upload` → اكتب الكابشن في «What's on your mind»
  → Next → Next (بيحوّل لـ«Edit reel» / «Reel settings») → Post. الميوتيشن بترجّع
  `publishing_flow: ASYNC_NOTIF` والريل بيبقى **ظاهر للزائر من غير تسجيل دخول خلال
  دقيقة**. البراند: `facebook.com/reel/2014437112571368` · التايتل: `reel/1642548764244953`.
  ⛔ **شاشة `/reels/create/` ماتستخدمهاش:** بتدّي `FALLBACK` + توست «saved as a draft»،
  الريل بيفتح لصاحبه بس (Shared with Public · View insights) والزائر بيشوف «This page
  isn't available» ومش بيظهر في تاب Reels — اتأكدت بمقارنة ريل قديم من نفس البروفايل.
  🧪 **التحقق الوحيد المعتمد:** افتح رابط البوست من كروم السوشيال (مش مسجّل في فيسبوك)
  — `caption visible: true` = موزّع. «Shared with Public» عند صاحب الحساب مش دليل.
  🐞 التاب لازم يكون **مرئي** (`visibilityState === 'visible'`) وإلا الرفع مابيبدأش:
  ثبّت نافذة كروم فوق (`SetWindowPos HWND_TOPMOST`) + فعّل التاب من شريط التابات
  (Windows-MCP Click) لو محمد فاتح تاب تاني، وفكّها بعد النشر.
- **الصوت:** `voiceover-edge.cjs` = فويس أوفر مصري مجاني من Edge (`ar-EG-ShakirNeural`،
  `msedge-tts` متسطّب في `scripts/node_modules`) + دمج بـffmpeg مع `tpad` لو الصوت
  أطول من الفيديو. محمد: «استخدم لهجتي للصوت» — `voiceover-lahajati.cjs` (صوت بدر ·
  مصري قاهري · إعلاني، ٢٥٠ حرف/مقطع) بس **الديمو المجاني بيرجّع 200 فاضي** من الجهاز
  ده. ✅ **اللي اشتغل ٧/٩ (محمد: «سجلت في لهجتي»):** حساب محمد في كروم الحقيقي
  (Claude in Chrome) → أداة TTS-Voiceover → صوت «بدر» (id 1408) → النص كامل (٤٣٧ حرف،
  نقطة/حرف) → الملف في Audio Archive برابط عام `audio_file?S3=…` (المحتوى mp3) → ينزّل
  ويتدمج بـffmpeg (`tpad`). الناتج `output/reel-brand-12-apps-lahajati.mp4` (٣٩ ث) —
  ده اللي اتنشر على تيك توك وفيسبوك. أي ريل جاي = صوت لهجتي مش Edge.

## 🎬 قواعد محتوى التايتل — محمد نصًا (٧ سبتمبر ٢٠٢٦ مساءً)
- «فين عنصر المفاجأة؟ إنت بتشرح إحنا بنعمل إيه» · «مش عايز إحنا ليه قولنا التايتل ده» ·
  «عايزين الناس تجرّب من غير ما يعرفوا إحنا بنصنّف إزاي». يعني في أي ريل/بوست عن
  `/title`: **الخطاف الأول** («من صورتك بقى؟ يلا بينا — هنقولك تايتلك») → النتايج
  **اسم ← تايتل وبس** (من غير note ولا سبب ولا قاعدة) → المفاجأة في الآخر («دي مش نكت —
  دي نتايج الموديل بجد») → CTA. **ممنوع** شرح ليه الموديل قال كده أو إزاي بيصنّف.
- «صاحب بيزنس دي عايزها صاحب ورشة نجارة، صاحب مكتب تأجير سيارات»: برومبت
  `/api/title-scan` بقى يطلّع «صاحب + النشاط بالتحديد» ويمنع «صاحب بيزنس/مكتب» العامة
  (كوميت 93ca551). نتايج المشاهير اتعادت عليه (ماسك → صاحب مصنع · ساويرس → صاحب شركة).
- 🎙️ لهجتي: زرار Generate ساعات مابيستجيبش لضغطة الإضافة — الأكيد
  `document.querySelector('.create-speech-btn').click()` بعد ما تحط النص بـsetter
  الـprototype + `input` event. النقاط بتتخصم فورًا (١/حرف) — لو الرصيد ماتغيّرش يبقى
  ماتولّدش. الملف في Audio Archive (أحدث `audio_file?S3=…` أول واحد).
- ريل التايتل v2 (`title-celebs-v2`، ٤٧ ث): اتنشر ٧/٩ مساءً على إنستجرام (script ✅)
  وفيسبوك (من كروم محمد) وتيك توك.

## 📣 باقي المنصات — اللي اتنشر عليه ٧/٩ مساءً من كروم محمد (محمد: «انشر على باقي المنصات»)
- **يوتيوب ✅** قناة Madmona (`studio.youtube.com/channel/UCQJFRUo9XMkSAthYw_I-c8g`): `/upload`
  → input الملف المخفي → العنوان (ctrl+a وبعدين اكتب) → الوصف → «No, it's not made for
  kids» (إجباري) → Next ×٣ → Public → **استنى «Checks complete. No issues found»** (٢–٣
  دقايق، وإلا حوار «still checking») → Publish. الشورت: `youtube.com/shorts/dotYm_EmhFw`.
- **ثريدز ✅** `threads.com` مسجّل بـ@madmona.cairo: New thread → input الملف (بيقبل mp4)
  → النص → Post. `threads.com/@madmona.cairo/post/Dc_3l_HjGc8`.
- **لينكدإن ⛔** حساب Madmona Cairo موجود بس الجلسة مقفولة (صفحة باسورد) — ماينفعش أدخل
  باسورد؛ محمد يسجّل مرة وبعدها أنشر. **إكس ⛔** `x.com/home` بيحوّل لـpremium_sign_up ومفيش
  زرار تغريد — الحساب مش مسجّل فعليًا.
- سجل `campaign_posts` بقى فيه `threads` و`x` في قيد المنصات.

## 🧭 /system + SLA + خطة المحتوى (٧ سبتمبر ٢٠٢٦ — آخر الليل)
- محمد: «عايز أعرف الناس نظام الـERP والـCRM يقدر يعمل إيه ويفيدهم إزاي» + «حاجة اسبيشيال
  وشرح واضح للـB2B» → صفحة عامة `/system` (ثابتة، صفر API): ٥ محاور من erpModules +
  «بيتشكّل على نشاطك» + ٣ خطوات + CTA لـ/pro. فيها بلوك «العرض على السوق مجاني» جنب
  «برنامج الإدارة ١٠٠٠ ج بدل كتير» عشان الرسالتين ما يتخلطوش. لينك ليها من /pro.
- **SLA** مسودة داخلية في `docs/business/sla-madmona.md`: كل رقم بين `[…]` قرار محمد
  ومش وعد للعميل. ⚠️ **مفيش escrow للعربون في الكود** — ريل «عم صابر» قصة؛ ماتوعدش بيه
  قبل ما يتبني. مفيش جدول disputes لسه.
- خطة المحتوى `docs/marketing/content-plan-b2b.md`: ٦ شرائح بوجعها وخطافها + ١٢ ريل
  بالترتيب + إيقاع ريل كل يومين + ملخص الأحد. قواعد الانتشار: وجع في أول ثانيتين، قصة
  بشخص واحد، المفاجأة في الآخر، من غير شرح إزاي بنشتغل، CTA واحد.
- المارد: **ممنوع يسأل على رقم** (قاعدة في البرومبتين + حقن رقم المتكلّم في أي أداة فيها
  phone/supplier_phone فاضية — `marid-brain.ts`).

## 💰 «عايز أدخل فلوس» — الحقيقة من الداتا (٧ سبتمبر ٢٠٢٦ — آخر الليل)
- 🔴 **برنامج الإدارة مش مقفول على حد:** `suppliers.has_erp_crm=true` لكل الموردين تقريبًا
  و`subscription_tier='free'` للكل، ومفيش أي شاشة في اللوحة بتتحجب بالـtier (الفحص:
  `grep -rn has_erp_crm src` → عرض بس). يعني اللي بندعوه بـ١٠٠٠ ج **هو عنده أصلًا ببلاش**.
  قبل أي بيع لازم محمد يحدد: إيه اللي مجاني (اقتراح: الإعلان + الصفحة) وإيه اللي بفلوس
  (اقتراح: HR والبصمة · الحسابات · CRM · بوت الواتساب · الدليفري) — والحجب يتعمل في
  `erpModules` بالـtier. مفيش جدول مدفوعات للاشتراك لسه.
- 🔴 **التنفيذ في الـCRM واقف:** ٤٬٧٥٠ رقم موزّعين، ٤٬٠٧٠ منهم `new` عمرهم ما اتكلّموا.
  مديحة: ١٬٦٥١ رقم و**صفر مكالمة مسجّلة**. آخر مكالمة مسجّلة لأي حد ٣/٩. الأرقام مش المشكلة.
- ✅ اللي اتعمل: ٤٠ رقم مهتم (interested/offer_sent) اتحطوا أول قايمة بكرة ٩ ص بملاحظة العرض ·
  ١٤ عارض معرض (صفحاتهم مبنية) اتضافوا للـCRM لعبير (`source_label='erp1000_expo'`) ·
  لمونة مهمة لمحمد شخصيًا (`flow_tasks`، دبي، أنشط مورد). السكريبت:
  `docs/business/sales-script-erp1000.md` (رسالة الواتساب تتبعت بإيد الموظف من /crm).
- القياس بكرة: `crm_calls` لكل موظف + `crm_contacts.status` (won/interested/lost).

## 🔎 الترافيك — الحقيقة من Search Console (٨ سبتمبر ٢٠٢٦)
- محمد: «زهقت من الترافيك القليل» · «مترجعليش أنا صفر في الكلام ده» — التسويق كله عليّا.
- Search Console اتوثّق تلقائي لـ`https://www.madmonacairo.com/` تحت madmona@madmonacairo.com
  (Domain name provider — سجل DNS بتاع Google Workspace). السايت ماب مبعوت من ٤/٨ وبيتقري.
- **الأرقام (٢٨ يوم):** ١٦ نقرة · ٣٦ ظهور · ترتيب ٨.٧. **مفهرس ٦٣ صفحة بس من ١٬٠٣٤:**
  ٩٢٦ «Discovered – currently not indexed» (جوجل شاف الرابط ومازحفش) · ٢٨ canonical بديل ·
  ٩ redirect error · ٥ crawled-not-indexed · ٣ duplicate. ده السبب الحقيقي: جوجل بيتجاهل
  ٩٤٪ من الموقع. العلاج: محتوى حقيقي في صفحات الأقسام + روابط داخلية + Request indexing
  للصفحات المهمة (اتعمل لـ/system و/title — من URL inspection، الزرار «Request indexing»،
  حصة يومية محدودة) + إصلاح الـredirect errors.
- IndexNow: مفتاح `public/<key>.txt` (`scripts/indexnow.key`) — الـ١٬٢٥٥ رابط اتبعتوا لـBing/Yandex (200).
- Vercel Analytics آخر ٢٤ ساعة: ١٧ زائر · ٨٨ مشاهدة · ٥ من فيسبوك.
- ⚠️ لهجتي: الجلسة في كروم محمد بتخرج (بيحوّل لـ/login) — لو كده استخدم Edge مؤقتًا **وقول
  لمحمد صراحة** إن الصوت مش لهجتي (سأل «بتستخدم لهجتي؟»). الريل #1 (order-lost-whatsapp)
  نزل ٨/٩ على إنستجرام وتيك توك بصوت Edge — يتبدّل.
- 🐞 ffmpeg: `tpad` + `apad` مع بعض بيرمي «No space left on device» (مش مساحة فعلًا) —
  استخدم `tpad` بس مع `-map 1:a -shortest`.
- Google Play Console: الحساب على moh91arabco@gmail.com (شخصي) — البروفايل والتطبيقات
  اتشالت ٢٨/٨ (هوية مش متحققة + رقم مش موثّق) — الحل من صاحب الحساب بس.
- Google AI Studio (المارد): Free tier متخطّي (٥٤٣/٥٠٠ طلب/يوم · ٢٤/١٥ في الدقيقة) → 429.

## 📣 النشر في جروبات فيسبوك + تغيير صوت لهجتي (٨ سبتمبر ٢٠٢٦)
محمد: «عايزك تبقى تنشر في جروبات — عايز انتشار» + «الصوت مش أحلى حاجة» + «صوت مصري
أو الإعلان كله يبقى خليجي ويستهدف الخليج».
- **الجروبات من كروم محمد الحقيقي** (Claude in Chrome): `/groups/joins/` بيطلّع القايمة.
  المسار: زرار «Write something…» (`.click()` من JS بيفتح الحوار) → كتابة بـ`type` →
  `file_upload` على input الملف اللي جوّه `[role=dialog]` («Add to your post») → Post.
  **استنى لحد ما الحوار يقفل** قبل أي navigate — لو نقلت وهو مفتوح البوست بيضيع
  (حصل في «مقاولات اليوم» مرتين، والجروب ده مابيقبلش البوست أصلًا — ماتعيدش).
  أغلب الجروبات = «Pending admin approval». «بيع شراء كل حاجة» موقوف (paused).
  الجروب بتاعنا (1204649112937687) بينشر فورًا. اتنشر النهارده في ٥ جروبات
  (مسجّلين في `campaign_posts` بـ`utm_medium=group`). **ماتزودش عن ~٥ في اليوم**
  عشان حساب محمد مايتقفلش.
- **النافذة لازم تكون مرئية للرفع**: سكريبت `_pin2.ps1 pin 9748` (EnumWindows على
  pid كروم محمد، مش `MainWindowHandle` — بيطلع فاضي) وبعدها `unpin`.
  ⚠️ كروم السوشيال (TikTok Studio) عنده pid تاني — ماتثبّتوش بالغلط.
- **تغيير الصوت في لهجتي**: الـ`<select>` مالوش أثر. الصح
  `[...document.querySelectorAll('.voice-card')].find(c=>c.innerText.startsWith('جابر')).click()`
  والتأكيد `.voice-card.selected`. الأرشيف بيكتب اسم الصوت قبل النص.
  عينات مصرية اتبعتت لمحمد: جابر · عازم · بهجت (`output/voice-samples/`).
  الرصيد ٢٬١٧٨ نقطة — إعادة الدليل الكامل بصوت تاني (~٤٬٥٠٠ حرف) **مش متاحة**
  من غير شحن؛ ريل واحد خليجي (~٤٠٠ حرف) متاح.
- محمد بعدها: «دخلت بحساب تاني اشتغل» → حساب لهجتي جديد **422220** (١٠٬٠٠٠ نقطة). الدليل الكامل
  اتعاد بصوت **جابر** (مصري قاهري، اللهجة value=7) = ٢٬٦٦٣ نقطة → `output/madmona-full-guide-gaber.mp4`
  (٤:٠٧). النسخة القديمة بصوت بدر في `guide/v1-badr/`. الرصيد المتبقي ٧٬٣٣٧.
  **أي صوت جاي = جابر** لحد ما محمد يقول غير كده. يوتيوب: `youtu.be/h_QX-GviraY`.

## 🎬 فيديو شرح ERP+CRM بالشاشات + صوت عازم (٨ سبتمبر ٢٠٢٦ — مساءً)
محمد: «فين شرح الايه ار بي والسي ار ام بالشاشات زي ما اتفقنا!!!» + «الصوت مش حلو خليك في المصري»
(رفض بدر وجابر). الصوت الحالي **عازم** (لهجتي · حساب 422220 · اللهجة 7) لحد ما يقول غير كده.
- **الشاشات من كروم محمد (مسجّل دخول اللوحة)**: مفيش جلسة للوحة في كروم CDP 9222، وصكّ توكن
  `madmona_sessions` من SQL **اترفض** (خط أحمر: مش بنعمل جلسات). الحل: `erp/caploop.ps1` (لقطة
  شاشة كاملة كل ٤ ثواني بـSetProcessDPIAware — من غيرها اللقطة 1200×800) + تنقّل بالإضافة على
  شاشات `/admin/business-finance/<id>/<module>` مع `document.documentElement.style.zoom='0.8'`،
  وبعدين اختيار اللقطة بالـtimestamp (`Date.now()` من JS) والقص `crop=2480:1430:20:{350|222}`
  (الشريط الداكن «Claude started debugging» بيزحزح المحتوى — يتكشف بـsignalstats YAVG).
  ⚠️ التاب لازم يكون **مرئي** (Windows-MCP Click على شريط التابات) + النافذة مثبّتة فوق.
  ⚠️ باتش `browser_batch` طويل (>٤٠ ث) بيرجّع timeout بس **بيكمّل التنفيذ** — اعمل باتشات قصيرة.
- **البناء** `erp/build.cjs`: صوت بـ٤ مقاطع (≤١٠٠٠ حرف) ومدة كل شاشة = نصيبها من حروف مقطعها.
  ⛔ `zoompan` بيقصّ العناوين العربية (يمين) — استخدم `scale=1920:-2,crop=1920:1080:0:y` بس.
- **يوتيوب**: العنوان ≤١٠٠ حرف وإلا «Saving…» للأبد وNext معطّل من غير رسالة واضحة (الخطأ في
  `ytcp-form-input-container[invalid]`). **Escape بيقفل حوار الرفع** (بيتحفظ درافت — يتفتح من
  `/video/<id>/edit` → «Edit draft»). الناتج: `youtu.be/FyyoF7WVW4E` · فيسبوك
  `reel/28174678648868724`.

## 🎙️ لهجتي: أداة Voiceover بتتجاهل اللهجة — استخدم V2 (٨ سبتمبر ٢٠٢٦ — بليل)
محمد: «اللغة مش مصري ليه ومش واضحة». اتأكدت بالتقاط طلب التوليد نفسه (FormData):
- **`/tools/text-to-speech-voiceover`** و**`/tools/text-to-speech-superior` (V1)**: الطلب فيه
  `id_voice` بس — **مفيش `dialect_id` خالص** حتى لو قايمة «اختر اللهجة» متغيّرة. اللهجة = لهجة
  الصوت الأصلية (جابر/عازم خليجي → ده اللي طلع «مش مصري»). بدر لهجته مصرية أصلًا.
- **`/tools/text-to-speech-superior-v2`**: الطلب فيه `dialect_id=7` (المصرية القاهرية) ✅ —
  نفس الكروت والـ`#simple-text-input` والزرار `#generate-btn`. **أي توليد مصري = V2 + قيمة 7.**
- «Custom Prompt» و«emotion mode» **للمشتركين بس** (توست «متاح فقط للمشتركين») — مش متاحين بالنقاط.
- الفحص السريع قبل أي توليد: لفّ `fetch`/`XMLHttpRequest.send` واطبع مفاتيح الـFormData
  (من غير `_token`) — لو مفيش `dialect_id` يبقى الأداة غلط.

## ✅ الروابط الحالية بعد تصحيح الصوت (٨ سبتمبر ٢٠٢٦ — ٨ مساءً)
محمد: «أي واحد فيهم المهم الأوديو يكون صح» → **جابر + المصرية القاهرية عبر V2** هو الصوت المعتمد.
- **الدليل الكامل:** يوتيوب `youtu.be/SlbA0njkbEI` · فيسبوك `reel/2226528131536348` (القديم
  `h_QX-GviraY` بقى Private والريل القديم اتمسح).
- **شرح ERP+CRM بالشاشات:** يوتيوب `youtu.be/8UgodOMdwfk` · فيسبوك `reel/4552847934933691`
  (القديمين `FyyoF7WVW4E` Private و`reel/28174678648868724` اتمسح).
- الملفات: `output/madmona-full-guide-gaber-egy.mp4` · `output/madmona-erp-crm-screens-gaber-egy.mp4`.
- رصيد لهجتي (حساب 422220): **١٬٣٥٦ نقطة** — مايكفيش ريل جديد من غير شحن.
- ⚠️ تبديل التاب في كروم محمد: **Windows-MCP Snapshot** (بدون vision) بيطلّع إحداثيات كل تاب
  بالاسم — الضغط بإحداثيات محفوظة وقع على تاب فيسبوك بتاعه هو (2028,50). وتابات Studio بتتجمّد
  لما تبقى مخفية — افتح صفحة جديدة في التاب المرئي بدل ما تصلّح المجمّد.

## 🚀 ليلة ٨ سبتمبر ٢٠٢٦ — ٤ ريلز + إعلان توظيف على كل المنصات
محمد: «انشر التلاتة على كل المنصات وتابع التسويق… اعمل إعلان توظيف مندوبين مبيعات… شوفلك تريند… اتصرف».
- **حساب لهجتي الجديد 422494** (١٠٬٠٠٠ نقطة → ~٧٬٩٠٠). الصوت المعتمد: جابر + V2 + dialect 7.
- ريلز جديدة (`reels.config.js`): `staff-attendance` · `clinic-80-calls` · `salon-at-risk` · `trend-80s-privacy`.
  المدد بتتظبط على الصوت بـ`_fit_reels.cjs <slug…>` قبل التسجيل. الخلفيات `bg/erp-*.jpg` من لقطات اللوحة.
- **التوزيع اللي بيشتغل (كل ريل ~١٢ دقيقة):** إنستجرام + تيك توك من كروم السوشيال (`_ig_post.cjs` +
  `posters/post-tiktok.js`، واحد ورا التاني، وبعد كل ريل اقفل تابات tiktok/instagram من `/json/close`
  وإلا CDP بيتجمّد — ⚠️ **ماتقفلش كل التابات** لأن كروم بيقفل نفسه). فيسبوك (بروفايل) + يوتيوب شورتس +
  ثريدز من كروم محمد في **تاب واحد مرئي** (النافذة بتتقفل minimized من نفسها — `_pin2.ps1 pin` بيرجّعها).
  التحقق: `_social_check.cjs` (TT posts + IG reels) و`/mohamed.m.nassef.7/reels` و`/videos/short` في Studio.
- **إعلان التوظيف** `hiring/` (كارت 4×5 + كابشن): «عمولة + بونص» من غير أرقام (زي `/careers`)، CTA
  `madmonacairo.com/careers` + واتساب ٠١٠٠٢٢٢٩٩٨٢. اتنشر: بروفايل محمد + جروب مضمونة + ثريدز.
- **الترند:** العربية ٨/٩ «ترند الثمانينات فخ لجمع صورك وبياناتك» → ريل `trend-80s-privacy` بيحوّل
  الخوف لـ/title («صورتك مابتتخزنش» — حقيقي: `title-scan/route.ts` مابيخزنش الصورة).
- كل المنشورات في `campaign_posts` (روابط تيك توك/ثريدز/شورتس الترند تتحدث لما تظهر).

## ✅ ٨ سبتمبر ٢٠٢٦ — ١٠ ريلز على ٥ منصات + أرقام اليوم
- التلات ريلز الأخيرة (`showroom-catalog` · `contracting-pnl` · `setup-wizard`) اتوزّعوا كاملين:
  بروفايل محمد + إنستجرام + تيك توك + شورتس + ثريدز، وكلهم مسجّلين في `campaign_posts` بروابطهم.
  شورتس: `H0zZn9--jio` · `RpoIBMP1DLE` · `BILNEhZ_B8Q`.
- 🐞 **درسين اتأكدوا الليلة:** (١) قفل **كل** التابات في كروم السوشيال بيقفل كروم نفسه —
  سيب تاب واحد على الأقل (`/json/close` للتيك توك والإنستجرام بس). (٢) تاب كروم محمد اللي
  **مش مفعّل** بيرجّع `visibilityState:'hidden'` والرفع مابيبدأش — اشتغل على التاب المفعّل نفسه
  (نفس التاب لليوتيوب وبعده ثريدز) بدل ما تفتح تابات متوازية.
- ⚠️ **حد الجروبات:** ٨ بوستات جروبات النهارده — فوق حد الـ٥ الآمن. مفيش نشر جروبات تاني
  قبل بكرة، حماية لحساب محمد.
- 📊 أرقام ٨/٩ آخر الليل: `campaign_posts` ٥٤ بوست · تيك توك (المشاهدات) موظفين ٨٣ · عيادات ٨١ ·
  صالونات ٧٤ · ترند ٣ · التلاتة الجداد ٠ (لسه) · فيسبوك بروفايل ٣٣٣/٣٢٩/٢٨٦/٢١١/٢١٠/١٦١ ·
  إنستجرام ٠–٨ (ضعيف) · `title_scans` **٠** · `campaign_leads` **٠**.

## 💬 اللينك في أول كومنت — ٩ سبتمبر ٢٠٢٦
محمد: «اللينك في أول كومنت والريل يخلص بـCTA».
- اتحط **كومنت أول** بلينك بـUTM (`utm_medium=comment`) على **١٠ ريلز** في بروفايل محمد.
  فيسبوك بيطلّع كارت معاينة للينك جوّه الكومنت — يعني ضغطة واحدة بدل ما الناس تكتب الرابط.
- 🐞 **فخ التعليق من الإضافة:** أول تفاعل بعد أي `navigate` **بيضيع** (الصفحة لسه بتاخد الفوكس) —
  الكتابة بتتقال «Typed» والخانة فاضية. الوصفة اللي بتشتغل:
  `navigate` → استنى ٨ ث → نداء JS تسخين (`focus()`) → استنى ٣ ث → `focus()+execCommand('insertText')`
  → `Return`. الكليك بالإحداثيات مش ثابت (مكان خانة الكومنت بيتحرك حسب طول الكابشن).
- ⚠️ بعد أي محاولة فاشلة فيسبوك بيفتكر إن فيه درافت — الـnavigate اللي بعدها بيترفض بحوار
  «Leave site?»؛ استخدم `force: true` (الدرافت مش مهم، الكومنت اتنشر فعلًا).
- 🧪 **التحقق:** صفحة الريل للزائر (مش مسجّل) بتطلّع **حائط تسجيل دخول من غير كومنتات** —
  فالدليل هو **عدّاد الكومنتات = ١** في نسخة الزائر، مش نص الكومنت.

## 📉 الترافيك الحقيقي — ٩ سبتمبر ٢٠٢٦ (Vercel Analytics، آخر ٧ أيام)
**١٩٤ زائر · ٢٬٢٩٠ مشاهدة صفحة · Bounce ٢٧٪** (الزوار ↓٣٧٪ عن الأسبوع اللي قبله).
- **الصفحات:** `/` ١٠٥ · `/marketplace` ٤٨ · `/account/work` ٤٤ · `/account` ٣٥ ·
  `/add-listing` ٢٣ · `/supplier/erp` ١٩ · `/admin-entry` ١٦ — **يعني أغلب الترافيك الفريق نفسه**.
  ⛔ **`/pro` و`/title` و`/system` مش في أعلى الصفحات خالص.**
- **المصادر:** m.facebook.com ٨ · facebook.com ٧ · lm.facebook.com ١ = **١٦ زائر من فيسبوك في ٧ أيام**،
  مقابل **١٬٧٠٠+ مشاهدة ريل**. نسبة التحويل من المشاهدة للزيارة ≈ **١٪**.
- **UTM: فاضية** · `title_scans` **٠** · `campaign_leads` **٠**.
- 🎯 **الخلاصة:** المشكلة مش المشاهدات — المشكلة إن مفيش طريق من الريل للموقع.
  عشان كده اتعمل: كومنت أول بلينك + كرت نهاية فيه الرابط. القياس بكرة على نفس الشاشة.

## 🎨 الهيدر البرتقالي في التطبيق المثبّت — المانيفست (٩ سبتمبر ٢٠٢٦)
محمد: «الهيدر لونه برتقالي!!» + «تاب الـ٣ شرط وحسابي مش شغال في تليفون محمد».

🐞 **الجذر (لون برتقالي):** `public/manifest.json` — وهو **اللي التطبيق بيستخدمه
فعلًا** (`manifest: '/manifest.json'` في layout.tsx، ومتأكد من الـHTML اللايف) —
كان لسه فيه `theme_color: "#FA8125"` من كوميت الريبراند القديم
(69a804f «البرتقالي أساسي»). ميتا `theme-color` في الـHTML أخضر `#059669`،
بس **التطبيق المثبّت (PWA) بياخد لونه من المانيفست مش من الميتا** — فالبرتقالي
كان بيبان على الموبايل بس ومحدش شايفه على الديسكتوب.
✅ بقى `#04352A` = نفس لون هيدر `MobileHome` بالظبط.
⚠️ فيه ٣ مانيفستات تانية (`manifest.webmanifest` · `chat-manifest.webmanifest` ·
`elite.webmanifest`) — **مش** في المسار الأساسي. أي تغيير لون لازم يبدأ من
`manifest.json` وإلا مالوش أثر.
📱 المانيفست متخزّن في التطبيق المثبّت — محمد محتاج يفتح
`madmonacairo.com/fix.html` أو يعيد تثبيت التطبيق عشان اللون يتحدّث.

✅ **القايمة و«حسابي» شغالين على اللايف** — اتجربوا بالفعل (مش بالقراءة) على
٣٦٠ و٣٩٠ و٤١٢ بكسل: زرار «القائمة» موجود وجوه الشاشة و`elementFromPoint`
بيرجّعه (مفيش حاجة فوقه)، الدروار بيفتح، و`/account` بترد من غير أي خطأ
كونسول. يعني اللي على تليفونه **نسخة قديمة متخزّنة** — العلاج `fix.html`.
🧪 السكريبتات: `scripts/reels/playwright/_mobile_check.cjs` (فحص الهيدر بعرض
مختلف) و`_mobile_click.cjs` (فتح الدروار) و`_acct_check.cjs`.
🐞 **Playwright بيعلّق على `connectOverCDP` حتى بتابين بس** لما كروم يبقى شغال
من ساعات — اقتل العملية وأعد تشغيلها (`launch-chrome-debug.ps1`) بدل ما تدوّر
في الكود.

⚠️ **ملاحظة على الكوميتات:** كوميت `7fb6749` (اللي فيه `manifest.json`) رسالته
اتكررت بالغلط من الكوميت اللي قبله — التوثيق الصح للتغيير ده هو السطور دي.

## 👤 «حساب محمد مش شغال» — سببين متراكبين مش واحد (٩ سبتمبر ٢٠٢٦)
محمد: «تاب الـ٣ شرط وحسابي مش شغال في تليفون محمد» ← المقصود **محمد عبدالجابر**
(مضمونة — مصر الجديدة، الدور `staff` / «مسؤول التصوير والمحتوى»).

🐞 **الاتنين كانوا لازم يتصلحوا مع بعض، وإصلاح واحد بس مابيبانش:**
1. `business_employees.phone = NULL` — و`madmona_resolve` بتطابق **بالرقم**
   (`phone_core(e.phone) = v_core`). من غير رقم مفيش هوية موظف خالص.
2. `status = 'terminated'` — الدالة بتشترط `e.status = 'active'`.
   حطّيت الرقم واختبرت فضل `is_employee: false` — **الرقم لوحده مايكفيش**.

✅ بعد الاتنين، الاختبار بجلسته الحقيقية (`madmona_resolve(<token>)`) رجّع
`is_employee: true` + الفرع + صلاحياته (حضور · جدوله · عمولته · ماركتبليس شخصي).
والبروفايل اتحدّث لوحده لـ`role='business_ops'` (تريجر) والاسم اتظبط.

📝 **الاسم كان متكتب غلط** «محمد **غ**بدالجابر» — اتصحّح لـ«عبدالجابر».
🔑 **الدخول:** عنده **PIN** (`pin_code` موجود) ومفيش `password_hash` — يعني بيدخل
بالرقم + الـPIN. الباسورد بيتكتب بإيد محمد من `/admin/staff` (خط أحمر: ممنوع
أولّد باسورد). وله صف في `platform_admins` أصلًا.

⚠️ **القاعدة:** لما حد يقول «حسابي مش شغال»، افحص **الأربعة** مع بعض قبل ما
تقول اتصلح: الرقم موجود؟ · `status='active'`؟ · `supplier_id` سليم؟ ·
فيه PIN أو باسورد؟ — وبعدين **اختبر بـ`madmona_resolve` بجلسته الحقيقية**،
مش بالقراءة. إصلاح واحد من الأربعة بيسيب المستخدم في نفس الشاشة الفاضية.

## 🔑 «مفيش حساب بيتعمل» — المسار سليم، المشكلة تسريب في القمع (٩ سبتمبر ٢٠٢٦)
محمد: «عايز أتأكد إن الناس اللي بتدخل بتعرف تعمل حساب… وموضوع الـOTP لازم
أتأكد منه، ده بج ممتد».

✅ **اتجرّب لايف من أوله لآخره النهارده — وشغّال:**
1. `POST /api/auth/wa {action:'start'}` → كود `MADxxxxx` + `wa_number` =
   **201114621551** (يعني `pickAuthWaSession` قرا الحالة الحية ولقى 982 مش
   جاهز فنزل للمساعد — الفولباك بتاع ٣/٩ شغّال).
2. محاكاة ويبهوك OpenWA برسالة واردة فيها الكود →
   `{"ok":true,"forwarded":true,"brain":{"ok":true,"login":"verified"}}`
3. `GET /api/auth/wa?code=…` → `{verified:true}` · `POST {action:'finish'}` →
   `token_hash` + `madmona_token` + بروفايل وحساب اتعملوا فعلًا.
   (بيانات الاختبار — رقم 01999888779 — اتمسحت بالكامل بعدها.)

📉 **الأرقام الحقيقية:** `wa_inbound_verifications` = **٢٩ كود اتطلب في ٧ أيام،
واحد بس اتوثّق** (لمونة ٤/٩). يعني المشكلة **مش في الكود** — الناس بتاخد
الكود ومابتبعتهوش. المسار بيطلب من المستخدم إنه **يسيب الموقع ويفتح واتساب
ويبعت كود** — وده أكبر تسريب في القمع.

⚠️ **فخ في القياس:** `whatsapp_messages` **مافيهاش** رسالة الكود حتى في الحالات
اللي نجحت — الويبهوك بيأكّد الكود ويخرج من غير ما يسجّل الرسالة. فـ
«مفيش رسالة واردة» **مش دليل** إن الرقم مش شغّال. القياس الصحيح =
نسبة `verified` في `wa_inbound_verifications`.

🎯 **الخلاصة للقرار:** إصلاح الكود مش هيزوّد الحسابات — المطلوب تقليل خطوات
الدخول (دخول جوجل ظاهر أول حاجة، أو كود بيتبعت بدل ما المستخدم يبعت).

## 🧭 لوحة واحدة لصاحب البيزنس + شاشة دخول واحدة (٩ سبتمبر ٢٠٢٦ — بعد الضهر)
محمد: «السبلاير داشبورد مش بتفتح على الداشبورد الكاملة… فيه كام شاشة مختلفة
ومين بيسمع فين؟» ثم «نفّذ الأربعة وخلي كل حاجة تودّي على اللوحة الكاملة… وحاول
الموضوع مايلخبطش الناس».

**اللي كان (٤ شاشات · ٦ مداخل · كل مدخل بيودّي لشاشة مختلفة):**
| الشاشة | مين كان بيودّي ليها |
|---|---|
| `/admin/business-finance/<id>` **الكاملة** | «بيزنسي» في الدرج · كارت MyWorkspaceCard · خطوات «كمّل شركتك» |
| `/supplier/erp` مختصرة (٦ تابات) | أزرار الفلوس في الدرج · لينكات مضمونة القديمة |
| `/supplier/dashboard` القديمة | كارت «نشاطي التجاري» في حسابي · لينكات المارد |
| `/supplier/erp/{products,catalog,materials,production,crm,projects}` | موديولات النشاط في الدرج |

**القاعدة دلوقتي: اللوحة الكاملة هي الوحيدة.**
- الدرج (`WorkspaceMenu`): كل لينك (الفلوس + موديولات النشاط) بيتركّب بـ`withAdmin()`
  → `/admin/business-finance/<id>/<seg>`. **صفر** لينك لـ`/supplier/erp`.
- شاشات ٢٨/٨ اتركّبت **جوّه** اللوحة كموديولات: `products` (منتجاتي وخدماتي) ·
  `marketplace-catalog` (اللي يظهر في السوق) · `materials` · `production` · `media`
  (الصور — كانت تاب في القديمة بس). نفس الكومبوننت — الصفحة في
  `/supplier/erp/<x>` بتقبل `supplierId` prop والصفحة في اللوحة بتمرّره من المسار.
  ⚠️ الشاشات دي (وMediaTab) بتشتغل بجلسة Supabase بس (`resolveBusiness`/RLS) —
  صاحب البيزنس الداخل بتوكن الواتساب لوحده لسه مش هيشوفها. الجذر مش اتحل هنا.
- `/supplier/erp` و`/supplier/dashboard` و`/auth/login` = **تحويل بس** (الكود
  القديم محفوظ في `Legacy*.tsx` جنبهم — مش مربوط بأي مسار).
- **بعد الدخول** — مصدر واحد `src/lib/landing.ts` (`resolveLanding`): موظف
  مضمونة → `/admin/listings` · صاحب بيزنس → لوحته الكاملة · غيره → `/home`.
  بيقبل البابين (جلسة أو `madmona_token`). `?next=`/`?redirect=` بيتحترموا؛
  القيمة `__auto` = «قرّر إنت». مستخدم في `/login` (باسورد + واتساب) و`/auth/callback` (جوجل).

**الدخول:** شاشة واحدة `/login` — جوجل أول حاجة ← رقم/إيميل + باسورد ← واتساب.
`/auth/login` (كانت شاشة تانية بشكل تاني و٢٥ لينك بيودّوا ليها) بقت تحويل.
**تأكيد الرقم:** السياسة زي ما هي (٢/٨): مش على باب الدخول — بيتطلب لما يلزم
(عرض أصل · أوردر) من `/auth/complete-phone` بالواتساب بس (التوثيق بالمكالمة
اتشال ٣/٩ بأمر محمد؛ الفولدر `/api/verify-call` مش موجود — التعليق القديم بس).

## 🚨 دخول الموظف بالـPIN — الرجعة اللي عملتها بإيدي وصلّحتها (٩ سبتمبر ٢٠٢٦ — العصر)
محمد: «محمد دلوقتي موقفه إيه؟ أنا مش عارف أدخل بحسابه أصلًا، وتاب حسابي عنده مش بيفتح».

🐞 **الجذر (٤ خطوات، كلها متقاسة):**
1. `login_with_password('01009303858', <PIN>)` → `success:true` بتلات صيغ للرقم
   (01… · +201… · 201…) — الدخول نفسه **شغّال**. (`1009303858` من غير صفر بترفض — طبيعي.)
2. `/api/login`: صفّه في `platform_admins` بباسورد `LOCKED-` فبيتخطّى فرع الأدمن ويوصل
   لفرع الموظف صح.
3. **الرجعة:** `resolveLanding` (توحيد الوجهة الضهر) كان بيودّي أي `is_staff` لـ
   `/admin/listings` — والموظف بالـPIN معاه **توكن واتساب بس** (لا كوكي لوحة ولا جلسة
   Supabase)، فشاشة الأدمن بتطلّع 401 على كل نداء وتبان «مش داخل». قبل التوحيد كان
   بيروح `/me`.
4. ✅ الموظف → `/me` («شغلي»). أدمن اللوحة بباسورد → `/admin/listings` مباشرة من /login.

🧪 **الإثبات بجلسة حقيقية لمحمد عبدالجابر (توكن مؤقت اتمسح بعدها):**
`/login` → اتحوّل لوحده على `/me` · `/me` = «لوحة شغلي محمد عبدالجابر عبدالحمد ·
مسؤول التصوير والمحتوى · مضمونة - مصر الجديدة · ٣ تاسك متأخر» · `/account` بتفتح
(حجوزاتي · أوردراتي · محفظتي) · صفر أخطاء كونسول. السكريبت: `_emp_flow.cjs <token>`.

⚠️ **القاعدة اللي اتأكدت تالت مرة النهارده:** أي تعديل على الدخول/الوجهة لازم يتجرّب
بـ**تلات هويات** قبل «اتعمل»: زائر · موظف بتوكن بس · أدمن بكوكي. وجهة واحدة
لـ`is_staff` غلط — فيه نوعين staff (بتوكن / بكوكي) والشاشات اللي تفتح لكل واحد مختلفة.

## 🩹 «الـ٣ شرط مش بيفتح وحسابي بيلود على الفاضي» على تليفون واحد بس (٩ سبتمبر ٢٠٢٦ — المغرب)
محمد عن تليفون محمد عبدالجابر. على أي جهاز تاني (وبجلسته هو) الاتنين شغالين.
🐞 **الجذر:** التطبيق المثبّت بيفضل مفتوح في الذاكرة أيام، وإحنا نشرنا **١٠+ مرات
النهارده** → الكلاينت القديم بيطلب chunks/RSC من نسخة اتشالت من السيرفر →
hydration بتفشل → الأزرار ميتة (الـ٣ شرط) والشاشات اللي بتتحمّل client-side
بتلف للأبد (حسابي). **مش الـSW** (network-first للـHTML والأصول من v4/v8).
✅ **العلاج على طبقتين:**
1. **Skew Protection** — مفعّل في فيرسل (Settings → Advanced، Maximum Age = ١٢ ساعة).
   Next 14.2 بيشتغل معاه تلقائي؛ `experimental.useDeploymentId` **مش مقبول** في 14.2.
   يعني الكلاينت المفتوح أقل من ١٢ ساعة بياخد أصوله القديمة ومايقعش.
2. **`components/ChunkReload.tsx`** (متركّب في layout): أي ChunkLoadError/فشل
   استيراد → reload مرة واحدة (حارس دقيقة في sessionStorage) — للي فتحه أكتر من ١٢ ساعة.
📱 **الجهاز اللي واقع دلوقتي** (فتحه قبل الإصلاح) محتاج مرة واحدة: `madmonacairo.com/fix.html`
أو يقفل التطبيق من الخلفية ويفتحه. بعدها الطبقتين بيمنعوا التكرار.
⚠️ **قاعدة:** أي يوم فيه نشر متكرر = المستخدمين اللي فاتحين التطبيق بيقعوا لو مفيش
الطبقتين دول. متشيلهمش.

## 🔒 «حسابي بيلف على الفاضي» — الجذر الحقيقي: قفل navigator.locks في الـPWA (٩ سبتمبر ٢٠٢٦ — بليل)
محمد (بعد إصلاح الكلاينت القديم): «تاب حسابي بقولك بيلف على الفاضي».
🐞 **الجذر:** `src/lib/supabase-browser.ts` ماكانش فيه `lock` مخصص — يعني supabase-js
بياخد قفل **`navigator.locks`** في `getSession()` والريفريش. في التطبيق المثبّت
القفل ده بيعلّق (تاب/ووركر ميّت ماسكه) → `getSession()` **مابترجّعش أبدًا** →
`/account` بيفضل على `loading` للأبد، ودرج الـ٣ شرط بيفضل فاضي. نفس السبب
اللي خلّى `financeRpc.ts` يبطّل supabase-js يوم ٢٥/٨ — بس الباقي فضل عليه.
✅ **العلاج على ٣ طبقات:**
1. `auth.lock = madmonaInprocLock` (قفل في الذاكرة بيسلسل النداءات جوّه الصفحة) —
   مفيش navigator.locks خالص. آمن لأن التخزين safeStorage ومفيش تابات بتتشارك ريفريش.
2. `src/lib/session-safe.ts` → `getSessionSafe(ms)`: الجلسة أو null، ومستحيل تعلّق
   أكتر من ٤ ثواني. مستخدمة في `/account` · `WorkspaceMenu` · `MobileHome`.
3. `/account`: اللي معاه `madmona_token` الشاشة بتفتحله **قبل** ما نسأل Supabase،
   و`init().catch(...)` بيطلّع الشاشة من loading مهما حصل.
⚠️ **قاعدة:** أي شاشة واجهة تنادي `supabaseBrowser.auth.getSession()` مباشرة =
قنبلة على الـPWA. استخدم `getSessionSafe`. الفحص: `grep -rn "auth.getSession()" src/app src/components`.

## 🚪🚪 دخول الموظف بالـPIN = البابين — والإثبات لايف (٩ سبتمبر ٢٠٢٦ — بليل)
محمد (٣ مرات): «المشكلة لسه قايمة… مش بيجيب تاب شغلي… حسابي بيلود على الفاضي».
🐞 **الجذر النهائي (اتعاد إنتاجه بموظف اختبار على اللايف):** `/api/login` للموظف
كان بيرجّع `madmona_token` **بس**. «شغلي» (`/account/work` → `get_my_work_home` من
غير p_token) والتاب الخامس (`is_madmona_staff()`) وRLS و٧٩ شاشة بتسأل
`getSession` — كلهم عايزين **جلسة Supabase**. فالموظف يدخل بالـPIN ويلاقي
«حسابي» بدل «شغلي» و«سجّل دخولك الأول» جوّه شغلي. نفس «الدرس الأكبر» ٢٥/٨.
✅ **نفس الدخلة بتفتح البابين** (زي الواتساب): `auth_user_for_employee_login`
(service_role — الرقم → مستخدم Supabase → إيميله) → `generateLink(magiclink)` →
`token_hash` → الواجهة `verifyOtp`. best-effort: لو مفيش مستخدم Supabase للرقم،
بيكمّل بالتوكن زي قبل (ومحتاج يتعمله واحد).
🧪 **الإثبات (موظف اختبار PIN، اتمسح بعدها):** `/login` → `/me` · جلسة Supabase موجودة
· التاب الخامس «شغلي→/account/work» · شغلي بتفتح (حضور · مكالماتي…) · صفر أخطاء.

⚠️ **فيرسل بطّل يبني من GitHub** بعد ديبلوي 0aeba12 — ٣ pushات متتالية (862d957 ·
6bea7b1 · 01bcd68) **ماظهرلهاش أي ديبلوي**. `npx vercel ls` هو الفحص، و
`npx vercel --prod --yes --scope madmonaadmin-1699s-projects` هو البديل (اشتغل).
**بعد أي push: اتأكد إن ديبلوي جديد ظهر قبل ما تقول «اتنشر».**

🎁 كارت «البقشيش» في `/me` بيظهر بس لو فيه بقشيش فعلًا (محمد: «مش المفروض يتفتح كده»).
🔔 `generate_recurring_tasks` بقت تعلّم إشعارات ما قبل اليوم كمقروءة مع توليد
التاسكات (محمد: «النوتيفيكيشن تتشال كل يوم مع توليد التاسكات»). اتعمل تنضيف
يدوي مرة: ٤٬٩٨٤ → ١٣ غير مقروء. ⚠️ **الجوب `generate-recurring-tasks` مقفول
(enabled=false) من ٢٦/٨** ضمن قفل فئة work — التنضيف اليومي مش هيشتغل لحد ما
محمد يأمر بفكّه صراحةً.
👤 محمد عبدالجابر: `role_ar` اتصحّح لـ«أوفيس بوي» (كان «مسؤول التصوير والمحتوى»).

## 🧹 «شغلي» للأوفيس بوي + جوب التاسكات اتفتح (٩ سبتمبر ٢٠٢٦ — بليل)
محمد: «افتحه، والأوفيس بوي يشوف الحضور والتاسكات بس ويدخل الشات يوثّق الشغل».
- **الجوب:** `generate-recurring-tasks` بقى `enabled=true` بأمر محمد نصًا (كان واقف من
  ٢٦/٨). `unlock_orchestrator_job` رجّعت «مفيش قفل» (مفيش صف في job_locks) فالتفعيل
  اتعمل بـupdate مباشر. اتشغّل يدوي مرة: ١٢ تاسك النهارده. النبضة بتشغّله يوميًا.
- **الأوفيس بوي** (`business_employees.role_ar = 'أوفيس بوي'` — الصفة من الداتابيز، مش
  فلاج في المتصفح): «شغلي» بتوريه **الحضور والانصراف + مهامي + كارت «وثّق شغلك» → /chat**
  بس. مخفي عنه: «مكالماتي» (CRM) و«الطلبات». المصاريف/الإعلانات أصلًا بصلاحيات مش عنده.
  🧪 اتأكد لايف بموظف اختبار أوفيس بوي (اتمسح): الحضور ✓ مهامي ✓ وثّق شغلك ✓ ·
  مكالماتي ✗ الطلبات ✗ المصاريف ✗ الإعلانات ✗ · التاب الخامس «شغلي».
- أي صفة جديدة عايزة شاشة مختصرة = نفس النمط (`officeBoy` في `BizCard`) — مش صفحة تانية.

## 🔑 الدخول باسورد بس · 1551 للدخول فقط · الموظف مش صاحب بيزنس · عهدتي · مالك + موظف واحد (٩ سبتمبر ٢٠٢٦ — بليل)
محمد نصًا: «هنشيل تاب الـPIN نهائي وهيبقى كله باسورد» · «خلي الرقم 1551 يكون فقط مسؤول
عن تسجيل الدخول» · «أي أكونت متسجّل كموظف مش لازم يتصنف كصاحب بيزنس» · «تاب للعهد
والسلف والمشتريات الخاصة بيه علشان تتسوي في العهدة» · «حساب البيزنس يفتح لموظف واحد بدل ٥».
- **الـPIN اتشال من الدخول:** ٧١/٧٤ موظف نشط كانوا PIN بس → الـPIN بقى باسورده bcrypt
  (مفيش سر جديد اتولّد، محدش اتقفل)، و`login_with_password` بقت bcrypt بس (خطأ: «الباسورد غلط»).
  `employee_login_phone_pin` (بصمة الجهاز) زي ما هي. `sql/2026-09-09_login_password_only.sql`.
- **1551 = رقم الدخول الوحيد:** `auth_wa_order='201114621551'` والمارد مسكّت عليه (`enabled=false`).
  ⚠️ `/api/auth/wa` لسه بيطلّع `alternatives` (337 · 3449 · 3443) — لو محمد عايز 1551 بس
  في الواجهة كمان، الـalternatives تتشال من الرد.
- **الموظف ≠ صاحب بيزنس:** `workspace_menu_context` بقت ترجّع `staff_can_manage` (لموظف
  مضمونة) و`relation`/`can_manage` (لأي بيزنس). الإدارة = role owner/admin أو
  permissions.all/can_manage_team/can_view_finance/can_manage_listings. الدرج · AdminGuard ·
  landing · تحويلات /supplier/erp و/dashboard: اللي مش بيدير → «شغلي» بس.
  🧪 لايف بموظف أوفيس بوي: الدرج «شغلي» بس ✓ · /admin/listings مقفول ✓ · لوحة مضمونة
  الكاملة مقفولة ✓ · /supplier/erp → /account/work ✓.
- **«عهدتي وسلفي ومشترياتي»** (CustodyCard في شغلي — للأوفيس بوي): الموديل كان موجود
  (custody_items · employee_advances · طلبات) بس مفيش طريق للموظف يسجّل مشتريات — اتبنى
  `my_custody_ledger` + `employee_custody_purchase` (مصروف `custody_purchase` + خصم من
  `cash_spent`، بيرفض أكتر من المتبقي). فئة المصروف اتضافت للقيد وشاشات المصاريف.
  حالة العهدة اللي معاه = `held` (مش assigned). 🧪 لايف: ٥٠٠ → ٣٨٠ → ٣٠٠ بعد مشتريتين.
  طلباته = سلفة/عهدة بس (`RequestForm kinds`).
- **حد الموظفين:** `marketplace_suppliers.max_employees=2` (مالك + موظف) لكل الموردين، مضمونة 50،
  والتريجر `trg_enforce_employee_cap` افتراضيه ٢ (بيعدّ الصفوف النشطة بما فيها المالك).
  ⚠️ «Elite Beauty Salon & Spa» عنده ٥ موظفين نشطين من قبل — التريجر بيمنع الإضافة بس، مابيمسحش.
- 🧪 سكريبتات الإثبات: `_gate_e2e.cjs` · `_custody_e2e.cjs` · `_officeboy_pw.cjs`.

## 🚪🚪 «بيبان مسجّل دخول وتسجيل الخروج مش بتسمع» — البابين على الخروج كمان (٩ سبتمبر ٢٠٢٦ — بليل)
محمد: «حساب محمد… مش عارف بيسجل دخول إزاي» ثم «لما بيفتح الأبليكيشن بيبان كأنه مسجّل
دخول وكل ما أكتب تسجيل خروج مش بتسمع».
- 🐞 **الجذر الأول (من الداتا):** تليفون محمد عبدالجابر ماسك `madmona_token` صالح
  و`auth.users.last_sign_in_at` بتاعه من ٢٩/٧ — كل دخلاته فتحت باب واحد. إصلاح `/login`
  بيصلّح الدخلات الجاية بس. ✅ `/api/auth/upgrade` + `ensureSupabaseSession()`
  (`src/lib/session-upgrade.ts`): توكن صالح → الرقم → مستخدم Supabase
  (`auth_user_for_account_phone`: موظف الأول وإلا أي حساب بنفس الرقم) → magiclink →
  verifyOtp في نفس الشاشة. «شغلي» و«حسابي» بيناديوها بدل getSession.
- 🐞 **الجذر التاني:** ٧ أزرار خروج كلها `supabaseBrowser.auth.signOut()` **بس** — بتسيب
  التوكن وصفّه في `madmona_sessions`، فالتطبيق يرجّعه «داخل» (ومن النهارده الترقية كمان
  تفتحله الباب التاني من التوكن ده). و`/home` كان بالعكس (توكن بس).
  ✅ `src/lib/sign-out.ts` → `signOutEverywhere()`: `madmona_logout` + `owner_logout` +
  `DELETE /api/admin-entry` + مسح مفاتيح الجلسة المحلية + `signOut({scope:'local'})` بمهلة.
  **أي زرار خروج جديد ينادي دي** — الفحص: `grep -rn "auth.signOut(" src` لازم يطلع فاضي.
- 🧪 **الإثبات لايف** (`_logout_e2e.cjs` بموظف اختبار باسورد، اتمسح بعده): دخول → البابين
  مفتوحين وشغلي بتفتح → خروج من حسابي → التوكن وجلسة Supabase اتمسحوا · صف الجلسة اتمسح
  من السيرفر · `/login` بيعرض الفورم · شغلي «سجّل دخولك». والترقية اتجرّبت بتوكن حقيقي:
  `found:true · via:employee · token_hash ✓`.
- ⚠️ **`.env.vercel-prod` قيم Supabase (URL · service key) فاضية** (`""`) — الموجود فيه
  `WA_SERVICE_SECRET` بس. عايز مستخدم Supabase للاختبار؟ محاكاة الواتساب
  (start → webhook → finish) هي الطريق، مش `auth.admin.createUser`.
- 🔑 **دخول محمد عبدالجابر:** `/login` → `01009303858` + الباسورد اللي محمد كاتبه في
  `/admin/staff` (الـPIN مابقاش بيدخّل). حسابه في الداتابيز: `is_staff:true ·
  staff_can_manage:false · relation:employee · أوفيس بوي`.

## 🔢 موظف مضمونة اللي مش بيدير → «شغلي» بس · الإشعارات تتمسح يوميًا · التوثيق من شغلي (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «الهوم بيدج فيه تاب شغلي؟ + العهدة مش ظاهرة + الإشعارات تتمسح وتتجدد يوم بيوم + الأرقام اللي
ظاهرة عند محمد مش المفروض تظهر — إصلاح شامل» ثم «الإصلاحات لازم تكون على مستوى البيزنس ككل»
و«توثيق إتمام التاسك لازم يكون من شاشة شغلي زي باقي الناس».
- **«الأرقام» = `/me`**: وجهة كل staff بعد الدخول، وبتعرض «عمولة الشهر» و«مستحق لسه» وإدارة
  الفريق. القاعدة (عامة لكل بيزنس، مش لمحمد): `staff_can_manage=true` → `/me` · غير كده →
  `/account/work` (في `landing.ts` **و**`/me` نفسها بتحوّل). التوكن بس؟ الشريط السفلي
  (`useMadmonaStaff`) بيرقّي الجلسة من الهوم فتاب «شغلي» بيظهر.
- **الإشعارات:** `generate_recurring_tasks` **بتمسح** `employee_notifications` بتاعة الأيام اللي فاتت
  (كانت بتعلّمها مقروءة) — `sql/2026-09-09_notifications_delete_daily.sql`. تنضيف يدوي مرة: ٥٬٠٦٦.
- **العهدة:** كارت «عهدتي» بيظهر للأوفيس بوي من شغلي؛ لو فاضي يبقى مفيش عهدة متسلّمة له —
  الإدارة بتسلّمها من `/admin/business-finance/<id>/custody` والتسوية بمشترياته من الكارت.
- **التوثيق من شغلي:** كارت «وثّق شغلك → الشات» اتشال. الكل بيقفل مهامه بالإثبات من «مهامي»
  (`complete_my_task p_proof/p_proof_url`). مفيش مسار توثيق تاني.
- 🧪 لايف بأوفيس بوي اختبار (اتمسح): الدخول → `/account/work` · `/me` → `/account/work` ومفيش
  عمولة · شغلي: حضور ✓ عهدتي ✓ مكالماتي ✗ الطلبات ✗ · جهاز بتوكن بس على الهوم → تاب «شغلي»
  والجلسة اترقّت. السكريبت `_officeboy_landing_e2e.cjs`.
- ⚠️ **الجذر البنيوي اللي بيخلي الإصلاحات تتكرر:** بابين دخول (توكن / Supabase) **وشاشتين**
  للموظف (`/me` بالتوكن · `/account/work` بالجلسة). كل شاشة جديدة لازم تقبل البابين
  (`ensureSupabaseSession` أو `p_token`) — والاتجاه: شاشة موظف واحدة هي «شغلي».

## 🧾 العهدة وتسويتها · CRM بالصلاحيات · إثبات بصورة · حذف عضو من الشات (٩ سبتمبر ٢٠٢٦ — آخر الليل)
- **العهدة كانت مش مندمجة:** شاشة `/custody` في اللوحة كانت بتكتب `status='assigned'` (القيد
  بيرفضها → صفر عهد اتسجّلت) ومن غير `employee_id` (فالموظف مش هيلاقيها) ومفيش تسوية.
  دلوقتي: `admin_custody_board/assign/settle/return` (حارس `schedule_*_ok` + `financeRpc`).
  التسوية = المسلَّم − المشتريات = المتبقي · الموظف بيرجّع كاش · العجز بيتقيّد سلفة
  (`employee_advances` status `pending`) تلقائيًا. الموظف بيصرف من «عهدتي» في شغلي.
  🧪 SQL بجلسة محمد: assign 100 → settle 80 → عجز 20 → سلفة ✓ (اتمسح).
- **CRM لمن عنده صلاحيات:** `crm_can_use()` — موظف مضمونة بـ`permissions={}` (الأوفيس بوي)
  ومش في `crm_staff_settings` → `crm_my_queue` بترفض. `crm_my_badge` زي ما هي (بتحدد تاب شغلي).
  ⚠️ اتأكدت بالداتا: الأوفيس بوي مالوش أرقام في CRM ولا في روم الشركة ولا إشعارات — لو لسه
  شايف أرقام يبقى نسخة قديمة على الجهاز (`fix.html`) أو شاشة لازم أشوفها بلقطة.
- **إثبات بصورة من شغلي:** زرار 📷 في كل مهمة — `lib/image-compress.ts` (١٢٨٠px · JPEG ٠.٧٢)
  → `/api/tasks/proof` (توكن Supabase · service role · `content-images/task-proofs/<uid>/`)
  → `complete_my_task(p_proof_url)`. للأوفيس بوي الإقفال نفسه بيفتح الكاميرا (`photoRequired`).
- **حذف عضو من الشات:** `remove_room_member` + زرار «شيل» للـowner أو admin في الروم (مش المالك).

## 🧩 الويزارد كان بيضيّع العنوان والسعر من ٤/٩ + طلبات العهدة + المعارض تاب لوحده (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «العميل خلّص الإعلان ماتضافش عندنا ولا جاله إشعار» · «مبقوش ٥ خطوات؟».
- 🐞 **الجذر (من الداتا):** درافت `ba8e6a0d` اتبعت `submitted` ٨ مرات في ٣ دقايق والعنوان
  «(جاري التحرير)» والسعر فاضي والمدينة فاضية — بس فيه صورة وبيانات تواصل. السبب: يوم ٤/٩
  أزرار «كمّل» بين الأقسام اتشالت (`Nav` بيرجّع null من غير `nextLabel`) وكانت **الوحيدة**
  اللي بتنادي `handleNext` بتاع الأقسام (العنوان/المدينة/المواصفات · السعر · المنيو · الكتالوج).
  الإشعارات وصلت فعلًا («📥 ويزارد: (جاري التحرير)») بس محدش عرف إنها إعلان.
  ✅ `useReportChange` في كل قسم → `onSectionChange` في الأب (حفظ تلقائي ٩٠٠ مللي) + النشر بيبعت
  الأقسام كاملة وبيرفض لو ناقص عنوان/مدينة/سعر(أو منيو)/صورة. **«٥ خطوات» = شاشة واحدة من ٢٨/٨**
  بأمر محمد؛ `total_steps=5` في الجدول ديفولت بس.
  ⚠️ **قاعدة:** أي قسم في الويزارد بيمسك حالته محليًا لازم يبلّغ الأب (`onChange`) — زرار مخفي
  = داتا ضايعة بصمت. الفحص: `_wizard_autosave_probe.cjs` (بيراقب PATCH فيه title/price).
- 🧾 **طلبات العهدة:** `admin_list_pending_requests` بقت ترجّع `custody` + `admin_approve_custody_request`
  (تأكيد بالمبلغ → `held` وتظهر في «عهدتي» · رفض → تتمسح) + `notify_managers_new_request`
  (پوش للمديرين مع أي طلب) + طلب عهدة بمبلغ = `kind='cash'`. 🧪 SQL بجلسة محمد عبدالجابر: طلب →
  ظهر في القايمة + اتأكد → held (اتمسح).
- 🏛️ **المعارض:** رسايل العارضين (`ExpoManualSend`) في `/admin/exhibitions` بس — اتشالت من «شغلي».
- ⚠️ **الأرقام عند الأوفيس بوي (مثبت بجلسته الحقيقية على v104):** صفر أرقام غريبة في كل الشاشات
  (`_as_employee_scan.cjs` بتوكن من محاكاة الواتساب). CDP 9222 بيقع كتير — السكريبتات الجديدة
  بتشغّل كروم بنفسها (`chromium.launch({channel:'chrome'})`).

## 🧩 «المنتجات والخدمات» شاشة واحدة + البيع اليدوي (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «تاب منتجات وخدمات وتاب تاني قائمة الخدمات وتاب تاني خدمات عاملين تعارض، ولما باجي أضيف
مش بيرضي، والمنتج/الخدمة لو هيتسجّلوا يدوي مش عن طريق الحجوزات هيحصل إزاي؟».
- 🐞 **«مش بيرضي يضيف»:** فورم المنتج كان بيكتب `qty_on_hand`/`reorder_level` والجدول
  `inventory_products` فيه `current_stock`/`reorder_threshold` → كل insert بيفشل بصمت (alert بالخطأ).
  ⚠️ قاعدة: أي شاشة بتكتب في جدول، طابق أسماء الأعمدة على `information_schema` مش على الذاكرة.
- **الموديل (٣ جداول مختلفة — ماتخلطهمش):** `inventory_products` = مخزون بيتباع ·
  `services_catalog` = خدمة بتتحجز بميعاد (الحجوزات بتقرا منها) · `mart_products` = كتالوج السوق للإعلان.
  الشاشة `/admin/business-finance/<id>/products` فيها تابين (منتجات · خدمات) — `services-catalog` اتشالت
  من قايمة الموديولات (الراوت شغال) و`services` بقت «استهلاك الخدمة من المخزون».
- **البيع اليدوي:** `record_manual_sale` → قيد دخل «مبيعات» في `financial_transactions` (+ خصم مخزون
  للمنتج) — زرار «بيع» على كل منتج/خدمة (`ManualSaleModal`). الحجوزات لسه ليها مسارها؛ ده للبيع في المحل.

## 🧱 المنتج الأولي · تعديل التسوية · «كل بند بيتسجّل مرتين» (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «خلي البيع اليدوي يظهر في الحسابات + تاب لمنتج أولي بيدخل في خدمة أو منتج نهائي ونربطهم +
تاب لتعديل تسوية العهدة + راجع ليه كل بند بيتسجل مرتين».
- **الحسابات:** أي `financial_transactions` بيتقيّد لوحده في دفتر اليومية (`trg_erp_auto_post_tx` —
  `erp_settings.auto_post_from_transactions` مفعّل لـ٣٣٠/٣٣٣ مورد). البيع اليدوي بيظهر في
  الحسابات/قائمة الدخل من غير أي كود إضافي. الإلغاء = `is_void=true` (التريجر بيعكس القيد) — مش delete.
- **المنتج الأولي:** الخامات = `inventory_products.item_class in ('material','consumable')` (تاب «الخامات»
  جوّه «المنتجات والخدمات»). الربط: منتج نهائي ← خامات في `product_material_consumption` (زرار 🧱 ·
  `admin_list/set_product_materials`)، خدمة ← خامات في `service_product_consumption` (كان موجود).
  `record_manual_sale` بتخصم الخامات المربوطة مع كل بيع.
- **تعديل التسوية:** `admin_custody_settle_edit` (بيعيد الفرق وسلفة العجز) — زرار على العهدة اللي اتسوّت.
- 🐞 **«مرتين»:** مشتريات «عهدتي» كانت بتتسجّل مرتين — ضغطة مزدوجة على الموبايل قبل ما `busy`
  يتحدّث. ✅ قفل فوري بـ`useRef` + حارس في `employee_custody_purchase` (نفس العهدة+المبلغ+الوصف خلال
  دقيقتين = نفس العملية). التكرار بتاع ٩/٩ اتصلح: قيوده اتلغت (void) والصفوف اتمسحت والمتبقي رجع للعهدة.
  ⚠️ **قاعدة:** أي زرار بيكتب فلوس = قفل بـref **و** dedupe في الدالة نفسها — `disabled={busy}` لوحده مش كفاية.

## 🏛️ لوحة مضمونة مقصورة · بروتوكول اشتراك العيادات · بيان العهدة (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «ليه فاتح كل التابات لمضمونة؟ تابات مضمونة تخص شغل مضمونة — العمولات من الماركتبليس ونظام
الاشتراكات — لحد ما أقولك إن عندنا نشاط جديد» · «المنيو للمطاعم بس، الباقي قائمة الخدمات» ·
«نخلص البروتوكولات… الريل مايزيدش عن دقيقة» · «تعديل بيان العهدة بس بعد المراجعة يتقفل».
- **لوحة مضمونة** (`PLATFORM_SUPPLIER_ID` = c8b7b9d7…): `PLATFORM_MODULE_KEYS` في `erpModules.ts` — فلوس
  (نظرة عامة/عمولات · حسابات · مصاريف · كاش · مرتبات · تقارير) + فريق (حضور · تاسكات · مونيتور · طلبات ·
  عهد · صلاحيات) + عملاء (CRM). **نشاط جديد لمضمونة = محمد يقول الأول** وبعدها نضيف مفاتيحه.
- **«المنيو» = مطاعم بس** (`tracks.includes('restaurants')`)، غير كده «قائمة الخدمات».
- **بروتوكول الاشتراك (الحقيقة في الكود):** `auto_calc_madmona_commission_gross` كانت بتحسب عمولة على
  **أي** دخل — دلوقتي بس لو العميل جاي من مضمونة (أوردر السوق / حجز `source='madmona'`). البيع اليدوي
  والحجز المباشر = صفر. المرجع `docs/business/clinic-subscription-protocol.md` + الريل
  `clinic-1000-protocol` (٦٠ ثانية بالظبط، صوت جابر V2 من لهجتي — محتاج كروم محمد).
- **بيان العهدة:** الموظف يعدّل/يمسح بنده طول ما العهدة `held` (`employee_custody_purchase_update/delete`)؛
  بعد التسوية `locked` من الداتابيز والواجهة.

## 🧩 المنتجات والخدمات بالبابين + الصور + «اخفي مضمونة ADMIN» (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «المنتجات والخدمات بتعلق كل مرة بيكتب الصفحة للمورد… لازم تدخل جوه السيستم كمنتج نهائي أو
خدمة نهائية ويبقى ليها احجز/اشتري وتسمع في الماركتبليس» + «مكان للصور» + «اخفي كلمة مضمونة أدمن المنصة».
- 🐞 **«بتعلق»:** شاشة الخدمات كانت `supabase.from('suppliers')` بعميل anon (مقفول ٢٨/٨) → `if (!supplier)
  return <Loader/>` للأبد؛ وشاشة المنتجات على `resolveBusiness` (جلسة Supabase بس) → صاحب البيزنس بتوكن
  الواتساب مرفوض. ✅ كتابة جديدة `/admin/business-finance/<id>/products` على `financeRpc` بالبابين
  (`business_catalog_bundle` · `business_product_save/delete` · `business_service_save/delete` ·
  `toggle_catalog_visibility(...,p_token)`). `services-catalog` و`materials` بقوا تحويل لتابات جوّاها.
  الكود القديم: `LegacyServicesCatalog.tsx` · `/supplier/erp/products` (لسه بيستخدم Legacy).
- **الصور:** `image_url` على `inventory_products` و`services_catalog` (GRANT select) · رفع عبر
  `/api/catalog/photo` (Bearer أو madmona_token) · تريجر `catalog_sync_listing_photo` بيحطها في
  `listing_photos` (caption `catalog-image`) → تظهر في السوق أول ما الصنف «في السوق».
- **«مضمونة ADMIN»:** الشيل بيختفي على أي `/admin/business-finance/*` حتى لموظفي مضمونة.
- ⚠️ **قاعدة:** أي شاشة جوّه لوحة البيزنس = `financeRpc` + دوال بـ`p_token`. `createClient(anon)` أو
  `resolveBusiness` هناك = «بتعلق» لصاحب البيزنس.
- 🐞 **النشر للسوق من الكتالوج كان مستحيل (٣ أسباب اتصلحوا ٩/٩):** `toggle_catalog_visibility` بتعمل
  `order by sort_order` على categories (مش موجود) · بتعمل الإعلان published من غير صورة فتريجر الحد
  الأدنى بيرفضه · `schedule_edit_ok(NULL)` بترجّع true للأدمن. دلوقتي: صورة إجبارية → إعلان draft →
  الصورة بتتزامن → published. `sql/2026-09-09_toggle_catalog_visibility_fixes.sql`.

## 🔑 الدخول لأي حساب بالإيميل أو الرقم + 1551 بس + العهدة بالسالب + صور الكتالوج (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «أكّدلي إن أي زائر أو أي حد عنده حساب يقدر يسجّل دخول بالإيميل أو الرقم» · «1551 لتسجيل الدخول
علشان مش هنبعت منه رسايل تاني» · «مش عايز سيرة الماركتبليس في الإعلان» · «استورد صور المنتجات اللي ليها
صور» · «العهدة ممكن تتسوّى ويكون لطالب العهدة فلوس».
- **الدخول (`/api/login`) ٣ فروع بالترتيب:** أدمن اللوحة → موظف (باسورد) → **أي حساب Supabase**
  بإيميل أو رقم (الرقم → إيميله الداخلي `<رقم>@madmonacairo.com` عبر `auth_user_for_account_phone`/`phoneToEmail`)
  → `signInWithPassword` → الواجهة `setSession` + `syncModuleSession`. 🧪 لايف: رقم ✓ · إيميل ✓ · باسورد غلط ✗.
  رسالة الخطأ واحدة للكل. **الحساب الجديد** = جوجل أو واتساب (المستخدم بيبعت الكود لـ1551) — `/auth/signup`
  القديمة بتبعت كود من بريدج Railway الميت (9982) = رسالة باردة → ماتوجّهش عليها.
- **1551:** `/api/auth/wa` بيرجّع `alternatives: []` والترحيب بعد الدخول متوقف (`whatsapp_config.auth_welcome_enabled`
  مش `'1'`) — صفر رسايل صادرة من 1551. المارد عليه `enabled=false`.
- **الريل `clinic-1000-protocol`:** من غير أي ذكر للسوق — البيزنس وإيه اللي العيادة بتاخده بالـ١٠٠٠ ج (٦٠ ث).
- **صور الكتالوج:** اتستوردت من `listing_photos` لـ٦٧ منتج (الخدمات ٠ — مفيش إعلانات خدمات بصور).
- **العهدة بالسالب:** المشتريات بتتقبل أكتر من المتبقي؛ التسوية لو المتبقي سالب بتسجّل صرف كاش للموظف
  («رد مصاريف عهدة» في `financial_transactions`) بدل ما ترفض. الواجهة بتعرض «ليك عند الشركة X».
- 📵 **نشر إعلان من الكتالوج مش محتاج توثيق رقم** (٩/٩): `enforce_listing_publish_requirements` بتاخد رقم صاحب
  البيزنس اللي عنده حساب (`phone_verified_source='business_account'`). محمد: «هو أصلًا عنده حساب وليه رقم».
- 📥 **استيراد منتجات من إكسيل/CSV** في «المنتجات والخدمات»: `ProductsImportModal` (xlsx في المتصفح، الأعمدة
  بأسمائها العربي/الإنجليزي) → `business_products_import` (upsert بالاسم/SKU). `sql/2026-09-09_business_phone_rule_and_products_import.sql`.
- 👥 **حذف أعضاء جروبات الشركات (٩/٩):** في «جروب شركة X» محمد عضو عادي والمالك صاحب البيزنس — الحذف كان
  للمالك/الأدمن بس. `is_platform_manager()` (موظف مضمونة بـall/can_manage_team أو صاحب المنصة) بيقدر يشيل
  أي عضو غير المالك من أي `kind='company'` — `remove_room_member` + زرار «شيل» (`canKick` في chat/team).
- 📩 **ترحيب بعد الدخول (٩/٩ — محمد: «عادي ممكن ترحّب بيه بعد الدخول وتبعت اللينك اللي كان واقف عليه»):**
  `whatsapp_config.auth_welcome_enabled='1'` → `sendLoginWelcome` بيرد على رسالة الكود من نفس الرقم (1551)
  باللينك اللي كان رايحه. ده **رد** على رسالة المستخدم مش رسالة باردة. `'0'` = إيقاف.

## 🗂️🚀 القسم من صاحب البيزنس + النشر التلقائي + تنضيف الدرافتات + جروب شركة مضمونة (٩ سبتمبر ٢٠٢٦ — آخر الليل)
محمد: «إعلان تيكوود أثاث مكتبي نزل في الأثاث المنزلي — مش عايز إعلانات تنزل في أقسام غلط تاني» · «أي إعلان كامل
المواصفات ينزل، المراجعة مش يدوي» · «فضّي الدرافتات القديمة ومتراجعش ورايا» · «مش عارف أحذف من جروب شركة مضمونة
وشايف أكونتات لنفس الشركة مرتين» · «استكمال البيانات يروح على المنتجات والخدمات» · «ألوان الدخول زي الهوم».
- **القسم:** `inventory_products.category_id` / `services_catalog.category_id` — صاحب البيزنس بيختاره في فورم المنتج/الخدمة
  (`CategorySelect`: «مقترح لنشاطك» من `catalog_categories` ثم كل الأقسام). لو سابه فاضي → `guess_category_id(الاسم)` (جذور
  كلمات اسم القسم — بدون AI). الاستيراد: عمود «القسم» بالاسم أو تخمين من اسم الصنف (`guessed_category` في النتيجة → «راجعهم»).
  `toggle_catalog_visibility` بتستخدم قسم الصنف قبل أي تخمين، وتعديل القسم بيحدّث الإعلان المربوط.
  ⚠️ **مراجعة الصور بموديل AI مش متعملة** — خط أحمر التكلفة؛ محتاجة موافقة محمد على التكلفة الأول (Gemini free-tier
  متخطّي). اللي شغّال دلوقتي تصنيف نصّي + اختيار صاحب البيزنس.
- **النشر التلقائي:** `try_auto_publish_listing` بتتنادى من تريجر على `listing_photos` (صورة جديدة) و`listings` (تعديل
  عنوان/سعر/مدينة/قسم لدرافت). المراجع = التريجرات الموجودة (صورة حقيقية · سعر · رقم أو `business_account` · حد أدنى)؛ اللي
  بيفشل بيتسجّل في `auto_publish_log` بسببه (٨ من ٣٠ درافت: «مفيش سعر» لمطاعم/عيادات · ١ مكرر).
- **الدرافتات القديمة (>يومين) اتمسحت:** ٢٨ `listings` → سلة المهملات (`restore_listing`) · ٣٤ ويزارد →
  `_backup_wizard_drafts_20260909` · ٣١٠ `instant_listing_drafts` → `_backup_instant_drafts_20260909`.
- **جروب شركة مضمونة:** `sync_company_group` كانت بتاخد المالك من `marketplace_suppliers.profile_id` = بروفايل «شاهد قبر»
  (`dup:01002229982#…`) → مالك وهمي بيرجع مع كل مزامنة ومحدش يقدر يشيله. `profile_canonical()` بيحوّله للحقيقي، مالك واحد
  لكل جروب، الأشباح بتتجاهل، و`remove_room_member` بتسمح بشيل مالك زيادة طول ما فيه مالك تاني.
- **«كمّل شركتك»:** خطوات الكتالوج/النشر → `products` (مش `/add-listing`)، والعدّ من `inventory_products`+`services_catalog`
  (كان بيعدّ `mart_products` بس = «جداول فاضية»).
- **الألوان:** `/login` · `/owner/login` · `/join` · `/add-listing/success` · `/v` = هيدر `#04352A` + أرضية `#FAFAF7` زي الهوم.
- `sql/2026-09-09_catalog_category_auto_publish_company_room.sql`.
- 🎙️ **ريل العيادات (٩/٩ بليل):** محمد: «فتحت لهجتي — استخدم صوت مصري فورمال» → **بدر** (id 1408) على V2 بـdialect 7 + أسلوب
  «احترافي» (الطلب اتأكد: `dialect_id=7 · id_voice=1408`). الناتج `output/reel-clinic-1000-protocol-badr-egy.mp4` (٤٣ ث).
  الصوت `vo-clinic-1000-protocol-lahajati.mp3` · المشاهد اتظبطت عليه بـ`_fit_reels.cjs`. **مش منشور** — مستني كلمة محمد.
