# ⛔ اقرا الجزء ده الأول — أوامر محمد الثابتة

> كل سطر هنا محمد قاله بلسانه. **ماينفعش يتسأل عليه تاني.**
> الباقي بعد الخط الطويل تحت أرشيف وتفاصيل — ارجعله عند اللزوم بس.

## ١) الفلوس — الرصيد بيتحرق قدام عينيه
«انا بشوف الرصيد بيتاكل قدام عيني» · «متتعبنيش طول اليوم»

كل نداء أداة بيبعت المحادثة كلها من أولها. الجلسة الطويلة تكلفتها **تربيعي**.

- **متطبعش خرج كامل لأي أمر** (`tsc` · `npm` · `git log`). وجّه لملف
  وبعدين `findstr` على اللي يهمك بس.
- **متعملش `select *`** على جدول عريض.
- **متلفّش على الديبلوي كل شوية** — استنى مرة واحدة طويلة.
- **متجرّبش نفس الحاجة أكتر من مرتين.**
- شغّل الحاجات المستقلة مع بعض في نداء واحد.
- المتصفح: `channel: 'chrome'` على جهازه. النسخة المدمجة بايظة،
  والسحابة مالهاش وصول للموقع.

## ٢) خطوط حمرا
- **ممنوع تولّد باسورد وتخزّنه plaintext.** محمد بيكتبه بإيده من اللوحة.
- **ممنوع تخترع سعر أو نسبة أو إحصائية** في أي حاجة بتروح لعميل.
- **العمولة في الباكاند فقط فقط فقط.** ممنوع أي خانة أو معاينة عمولة
  في أي شاشة إضافة أو أي مكان في الأبليكيشن. **المبدأ: السعر اللي
  البايع بيقول عليه هو اللي بياخده** — مضمونة بتحط نصيبها فوقه في
  الباكاند. عمولة العربيات = **١٪** (اتفاق ٢٤/٨ — `cars_commission_pct`).
- **ممنوع أي إرسال واتساب جماعي من السيرفر** — ده اللي ضرب أرقامه.
  الطابور يفضل واقف (`queue_send_enabled='0'`)، وهو بيبعت بإيده من `/crm`.
- التنبيهات الداخلية **نوتيفيكيشن مش واتساب**.
- **المارد (ردود واتساب بالـAI) عالي التكلفة** — سويتشات:
  `marid_reply_mode='off'` = ماحدش يرد، `'template'` = قالب واحد
  ثابت من `marid_reply_template`، غير كده = المارد يشتغل بـClaude.
  الأدمن مستثنى دايمًا. الحل الدائم: تمبليتس مبنية على نيّة الرسالة.

## ٣) الفريق — متسألش عليهم تاني
محمد ناصف (owner) · **أحمد سامي** · **سامية ايهاب** · نورا · عبير ·
شهد · مديحة.

⚠️ **أحمد وسامية موظفين معاه وليهم صلاحيات كاملة.** خلاص.

> «افتح صلاحيات لفلان» = **بابين مش باب واحد**:
> 1. `business_employees.permissions` (التطبيق) ← `{all:true}`
> 2. `platform_admins` (لوحة `/admin`) ← سطر بحساب شخصي
> افتح الاتنين مع بعض. البنت اتحطّت في الأول بس مرتين قبل كده — والنتيجة
> إنها بتفتح التطبيق وتتقفل في وشها اللوحة، فمحمد يرجع يقولّي تاني.

**بابين دخول مختلفين — اللخبطة بينهم كلّفت يوم كامل:**

| الباب | الجدول | بيفتح |
|---|---|---|
| `/admin-entry` (إيميل+باسورد) | `platform_admins` → كوكي | لوحة `/admin` |
| دخول التطبيق العادي | `auth.users` + `profiles` | التطبيق |

صفحة جوّه `/admin` **مالهاش حق تطلب جلسة Supabase** — تستخدم
`adminPanelStage()` من `src/lib/platform-staff.ts`.
موظفين مضمونة دورهم `business_ops` **مش** `admin` عن قصد (لأن
`role='admin'` بيشغّل `is_admin()` في RLS في كل المنصة).
الصلاحيات التفصيلية في `business_employees.permissions` (`all:true`).

## ٤) توزيع أرقام الـCRM
مطاعم + تجميل → **نورا** · طبي + مصانع → **مديحة وعبير** ·
عقارات (الأغلبية) → **عبير** · الباقي → **مديحة**.

الموزّعين (`crm_staff_settings.is_dispatcher=true, receives_leads=false`):
**محمد ناصف · أحمد سامي · سامية**. الموزّع مايبقاش شايل أرقام
(receives_leads=false)، بس بيشوف تاب «الفريق كله» في الموبايل
ويقدر يفتح قايمة أي مندوب (`p_as`).

## ٤.١) التاسكات
**كل واحد يشوف تاسكاته هو بس.** `flow_tasks` بتتفلتر لموظف محدد
(owner_id أو employee_id أو assignee_email أو assignee_name).
الشات (`/chat/tasks` ← `get_my_tasks`) والموبايل (`/account/work`
← `get_my_work_home.my_tasks`) بيقروا نفس المصدر — لو أضفت حاجة
في واحد ضيفها في التاني.

## ٥) رقم استقبال ملفات العملاء
**٠١٠٠٢٢٢٩٩٨٢** (`INTAKE_WA` في `src/lib/crmScripts.ts`).
في الاسكريبتات: **متبدأش ببيزنس المطعم**.

## ٦) نماذج العروض
الوحيد الباقي: `/s/demo-clinic` (عيادة = موديل حجز خدمات فعلاً).
باقي النماذج (سيارات/مصنع/متجر/عقارات) **اتمسحت يوم ٢٤/٨** — صفحة
النموذج شاشة خدمات وأقسام وعرضت العربية «بمدة ٦٠ دقيقة» كأنها جلسة
سبا. محمد: «بيانات لا تصلح لموديل السيارات». متعملهاش تاني —
النشاطات دي بتتعرض في الماركتبليس نفسه.

## ٧) حاجات بتكسر على ويندوز
- **PowerShell بيبوّظ العربي في الأوامر** → اكتب الرسالة في
  `.commitmsg.txt` واستخدم `git commit -F`.
- **`move` مابيعرفش الأسماء العربية** → سكريبت Node.
- **`cmd /c "... & echo END"` بيرجع فورًا** → وجّه لملف، استنى، بعدين اقرا.

## ٧.٥) معرض ولا فرد — seller_kind
كل إعلان ليه `listings.seller_kind`: `business` (معرض/نشاط ← تعامل
B2B) أو `individual` (فرد). تريجر `trg_listings_seller_kind` بيملاها
تلقائي من `derive_seller_kind()` لأي مسار إدخال. **أي فيتشر جديد
يتعامل مع إعلانات لازم يفرّق بينهم.**

> ⚠️ قاعدة محمد: «التعديل يتعمل على مستوى قاعدة البيانات كاملة مش
> جدول و١٠ لأ» — أي عمود/مفهوم جديد لازم يتحط في: الجدول الأساسي +
> المسودات (listing_drafts + instant_listing_drafts) + الـRPCs +
> التريجرز + الـUI في نفس التعديل. مفيش نص شغل.
> وكمان: «أي تعديل = كوميت + بوش + ديبلوي فورًا» — مش في آخر الجلسة.
> و«أي تعديل لازم يكون **موثّق** بحيث مايتعارضش مع أي جلسة تانية،
> والتاريخ معروف لكل الجلسات» — يعني: رسالة كوميت بتشرح الجذر،
> وتعليق في الكود بتاريخ وكلام محمد نصًا، وسطر هنا لو قاعدة جديدة.

## ٤.٢) قواعد اتقفلت خلاص — متفتحهاش تاني
- **اسطمبات البيزنس** في `src/lib/erpModules.ts` (مصدر واحد):
  صالون/سبا · عيادة · مطعم · مقاولات · معارض عربيات · **عقارات
  (real_estate — اتضافت ٢٤/٨)**: الوحدات (صفحة جديدة) + المشاريع
  والتحصيل والجدول الزمني والربحية والحجوزات (إعادة استخدام صفحات
  المقاولات — مفيش نسخ موازية). نموذج العرض: «رويال إستيت (نموذج)»
  `/admin/business-finance/2ad0cab4-7c50-4968-8277-00952c935388`
  — theme فاضي عن قصد (محمد: تصميم الديسكتوب من غير ألوان متخزنة).
- **تاسكات المبيعات اليومية**: `generate_sales_daily_tasks()` عبر
  orchestrator job `sales-daily-tasks` (state-based: بعد ٩ صباحًا +
  فيه مندوبة من غير تاسكات). ٤ تاسكات: مكالمات (بعدد قايمتها الفعلي)
  + متابعة المستحق + تسجيل النتايج + **تاسك ماركتنج بيلف بأيام
  الأسبوع**. الموزّعين مستثنيين.
- **تريجر `trg_daily_tasks_dedupe`**: نفس الموظف+اليوم+العنوان =
  مفيش إدخال تاني حتى لو الأصلي مقفول — التاسك المقفول مايتولدش تاني.
- **جدول فريق مضمونة اليومي (٢٥/٨)**: ٩ ص ← ٦ م، الأحد–الخميس،
  راحة ١–٢. مصدره **`recurring_task_templates`** (سطر لكل تاسك
  بميعاده) وبيتولّد كل يوم من جوب `generate-recurring-tasks`
  (اتفك قفلها من قفل «فئة work» ٣/٨).
  🕘 **المرساة (٢٥/٨ مساءً)**: محمد: «بلان بالوقت على حسب معاد تسجيل
  الدخول». `anchor_mode='clockin'` (الافتراضي لفريق مضمونة) = التاسك
  بتتولد لحظة أول بصمة عن طريق `trg_tasks_on_clock_in` وميعادها =
  الحضور + `offset_minutes` بسقف ١٧:٤٥. `'fixed'` = ميعاد حائط
  (الراحة ١:٠٠). الجوب اليومي بيولّد الثابتة **بس**. الأقسام في
  `business_employees.metadata->team` (مبيعات · دعم فني · أوفيس ·
  إشراف · الإدارة) — الجدول والمونيتور بيقروا منها.
  📺 **المونيتور** (`/admin/business-finance/[id]/monitor`): صورة
  اليوم الحية من `get_team_monitor()` — حاضر/غايب · إنجاز · المتأخر ·
  التاسك الحالية والجاية. بيتحدث كل دقيقة.
  🧰 **أي شاشة جديدة في اللوحة تنادي الداتابيز عن طريق
  `src/lib/financeRpc.ts`** — fetch مباشر (مفيش supabase-js بسبب
  قفل navigator.locks في الـPWA) + p_token تلقائي.
  📋 **التاسكات تفصيلية حية (٢٥/٨ مساءً)**: `task_dynamic_detail()`
  بتتحقن في description وقت البصمة — ☎️ أول ١٢ رقم من قايمة المندوبة
  الفعلية · 📣 اسكريبت بوست جاهز من أحدث إعلان حقيقي (النماذج
  مستبعدة) · 🔍 المستني مراجعة بأسمائه. الفاصل `⸻` بيفصل الثابت عن
  الحي — `refresh_task_details_today()` بتستبدل الحي بس. شاشة «شغلي»
  فيها زرار «التفاصيل» (get_my_work_home بقت بترجّع description).
- **🔔 التاسكات مسموعة (٢٥/٨ مساءً)**: محمد: «خانة الاسكدجوال هي اللي
  مسمعة + تاسك من مكالمة محتاجينه يسمع». ٣ مصادر پوش (notification_queue
  → كرون البوش كل دقيقة): تاسك يومية جديدة (`notify_employee_new_task`
  اتوسعت) · تاسك من مكالمة (`trg_notify_new_flow_task` على flow_tasks —
  أول إشعار ليها) · ميعاد التاسك وصل (`notify_due_tasks()` عبر job
  `task-due-notify`، نافذة ١٥ دقيقة + dedupe بـtask_id في
  notification_queue). اتجرب بمسار حقيقي: تاسك اختبار لمحمد → الپوش
  اتبعت فعلًا. ⚠️ ٤ موظفين لسه مافعّلوش الإشعارات على موبايلاتهم
  (سامية · نورا · شهد · الأوفيس بوي) — مايوصلهمش صوت لحد ما يفعّلوا.
- **🔁 دورة السوشيال مرتين يوميًا (٢٥/٨)**: سامية بتنشر (بوست الصبح +
  بعد الضهر بالاسكريبت المعتمد من social_packs)، **وكل** الباقيين —
  الأوفيس والإدارة كمان — عندهم تاسك شير ١٤:١٥ و١٧:١٥ (بروفايل شخصي +
  جروب على الأقل + رياكشن).
- **🚪 دخول الفريق للوحة /admin (٢٥/٨)**: «الحساب ده مش موجود» عند شهد
  = مفيش صف في `platform_admins` (باب اللوحة بكوكي + باسورد، منفصل عن
  جلسة الأبليكيشن). الستة اتسجلوا بدور **`staff`** — دخول بس، من غير
  قوة `is_admin()` (القوة من صلاحيات كل واحد). الباسورد = نفس باسورد
  الأبليكيشن (التريجر `sync_employee_password_to_platform_admin`
  بيزامنه؛ الصف لازم يكون موجود الأول عشان المزامنة تلاقيه). اللي لسه
  مالوش باسورد صفّه فيه `LOCKED-…` — بيتفك أول ما الباسورد يتظبط من
  شاشة حسابات الموظفين. الإيميل الداخلي `<رقم>@staff.madmonacairo.com`
  قيد جدول بس — الدخول بالتليفون.
- **🔒 بيع العقارات والعربيات: مفيش أرقام موردين للعميل (٢٥/٨)**:
  محمد: «التواصل يفتح في شات مضمونة مع الطرفين — مش دايركت».
  `partyPhoneHidden` في صفحة الإعلان (sale-properties · sale-vehicles ·
  sale-tourism): كل أزرار الواتساب/التليفون بتفتح على رقم مضمونة
  (INTAKE ٠١٠٠٢٢٢٩٩٨٢) ورقم الفرع مخفي. **أي CTA تواصل جديد في
  القسمين دول يحترم نفس الفلاج.** الأقسام: مبيعات · دعم فني ·
  إدخال بيانات/تشغيل · إشراف وتوزيع · المنصة. المحتوى والسوشيال
  مكتوب ومتقفول في `sql/2026-08-25_madmona_daily_schedule_9_6.sql`
  لحد ما يتحدد مين شايله. **محمد عبدالجابر أوفيس بوي** (مش إدخال
  بيانات) — قسم إدخال البيانات والتشغيل لسه من غير حد.
  التعديل بيتعمل من شاشة **«جدول التاسكات اليومي»**
  (`/admin/business-finance/[supplierId]/schedule`) مش من SQL:
  ميعاد · عنوان · شرح · أولوية · أيام · إيقاف بدل المسح · وزرار
  «ولّد تاسكات النهارده». تاب «المهام» بيعرض تاسكات يوم بعينه، ده
  بيعرض القالب اللي بيتولّد منه.
  🔐 كل دوال `recurring_task_*` بتفحص `can_manage_business_team()` —
  كانت SECURITY DEFINER مفتوحة لأي حساب مسجّل بأي `supplier_id`.
  ⚠️ درس (٢٥/٨): الحارس ده بيمرّ لـ`platform_admins` أو لمين عنده
  `can_manage_team` — فأول ما اتحط، الشاشة قفلت في وش كل الفريق
  وحساب محمد الشخصي (`moh91arabco@gmail.com` دوره `supplier` مش
  `admin`؛ الأدمن هو `madmona@madmonacairo.com`). **أي حارس جديد
  على شاشة بيستخدمها الفريق، جرّبه بجلسة موظف حقيقي قبل ما تقول
  خلصت** — `set_config('request.jwt.claims', ...)` وبعدين نادي الدالة.
  ⚠️⚠️ **الدرس الأكبر (٢٥/٨ — بعد ٣ محاولات إصلاح):** لوحة
  business-finance ليها **نظامين دخول**: جلسة Supabase (auth.uid)
  **وتوكن واتساب** `madmona_token` (جدول `madmona_sessions`) — وده
  اللي محمد وأصحاب البيزنس بيدخلوا بيه، من غير أي جلسة Supabase.
  أي RPC جديدة لشاشة جوّه اللوحة **لازم** تقبل `p_token uuid` وتفحص
  بـ`schedule_access_ok()` / `schedule_edit_ok()` (بيقبلوا النظامين؛
  التوكن بيتفحص بمنطق `admin_check_finance_access` نفسه). حارس
  بيفحص auth.uid() بس = الشاشة شغالة لموظفي مضمونة ومقفولة في وش
  محمد وكل أصحاب البيزنس.
- **صلاحيات فريق مضمونة (٢٥/٨)**: نورا · مديحة · عبير · شهد · ايمان ·
  سامية عندهم صلاحيات الأدمن الكاملة **ما عدا الفلوس والتسعير**
  (`can_view_finance` / `can_manage_pricing` = false لحد أمر صريح).
  محمد: «عايز صلاحيات للتيم إنه يكون أدمن ويقدر يضيف إعلان لأي مورد
  أو بيزنس». ده بيشتغل عن طريق `is_madmona_staff()` جوّه
  `can_edit_supplier_listings()` — **مش** بتغيير `profiles.role` لـ
  `admin` (ده بيفتح `is_admin()` على المنصة كلها بما فيها الفلوس).
  الأوفيس بوي مستثنى. نورا كانت `permissions = {}` فاضية تمامًا.
- **ليه الإعلان بيفضل درافت**: `ListingForm` بيعمل الإعلان
  `status='draft'` بالتصميم، والنشر خطوة تانية بتعدّي على تلات
  تريجرز: صورة واحدة على الأقل · سعر (ما عدا العقارات) · **إثبات
  رقم الواتساب** (`phone_verified_at` أو رسالة واردة من نفس الرقم أو
  `publish_exception_until` ساري). قياس ٢٥/٨: من ١٩٦ درافت، ١٨٣
  من غير أي صورة و١٨٢ رقمهم مش متأكد — واحد بس كان هينشر لو دُس نشر.
- **⚡ أداء `listings` (٢٥/٨)**: بوليسي القراءة بتنادي ٦ دوال لكل صف.
  `can_edit_supplier_listings()` بتخرج فورًا لو `auth.uid()` فاضي
  (الزائر مالوش أي فرع يعدّي منه) وكوستها 500 عشان المخطّط يأجّلها:
  صفحة ٢٤ إعلان من 438ms/26,470 بافر → 46ms/6,513. **أي دالة
  جديدة في بوليسي على جدول كبير لازم يكون ليها خروج مبكر للزائر.**
- **🕘 الحضور مايتقفلش بالسكوت (٢٥/٨)**: محمد: «السيستم بيخلي الناس
  تنصرف وهما لسه في الشغل». الداتا: **١٠٠٪** من الانصرافات في ٤ أيام
  كانت `auto_offline` ولا واحدة `auto_app_exit` — يعني مسار «خرج من
  الفرع» عمره ما اشتغل، السكوت دايمًا بيسبقه. نبضة صفحة ويب على
  موبايل في جيب مستحيل تفضل شغالة. القواعد دلوقتي:
  `auto_clockout_offline_sessions` بتقفل عند **نهاية الوردية**
  (`supplier_branches.closes_at` + ٣٠ دقيقة سماح) أو بعد **٤ ساعات**
  سكوت — مش ٣٠ دقيقة؛ و`employee_auto_attendance` **بتفتح نفس
  الجلسة تاني** لو الرجوع في نفس اليوم وخلال ٤ ساعات من قفل
  أوتوماتيك، بدل ما تعمل صف جديد (نورا ٢٤/٨ كان عندها ٧ جلسات
  مقطّعة = ٣.٥ ساعة في يوم كامل). ورسالة الواتساب بقت للجلسة
  المنسية بس، مش لكل قفل يومي.
- **🖼️ الصور الواقعة (٢٥/٨)**: ٦٠٨ ملف في `content-images` محتواهم
  **SVG** (كروت بلايس-هولدر بعنوان الإعلان، من مسارات الـrehost
  والبوستات لما الصورة الأصلية تفشل) متسمّيين `.jpg`. الـoptimizer
  بيرفض SVG افتراضيًا بـ400 — ٢٤ إعلان منشور صورهم كانت مكسورة
  والملف نفسه 200. الحل: `dangerouslyAllowSVG` + CSP معقّمة
  (`script-src 'none'; sandbox`) في next.config.mjs. اتأكد: ٢٥/٢٥
  رابط صورة من الصفحة الجديدة = 200 (كانوا ٧/٢٥).
  الروابط القديمة بـ`dpl` قديم هتفضل 400 لحد ما الصفحة تتحمّل تاني —
  ده طبيعي، الـHTML مش متخزّن (SW v8+).
  + صور S3 القديم وحساب كلاودينري `dqott4uei` **ماتوا نهائيًا** —
  صفوفهم اتشالت والتلات شقق اللي كانت كل صورهم من هناك اتوقفوا
  (paused) لحد ما صور حقيقية تترفع. حساب `duxfgqioc` (أبيكس/Elite) حي.
- **📡 SW v9 (٢٥/٨)**: النت يفصل → `/offline.html` (ثابتة، بترجع
  لوحدها مع حدث `online`) بدل «This site can't be reached».
  ضمانات v8 زي ما هي: HTML عمره ما بيتخزّن. أي تعديل في سلوك
  الكاش يفضل محافظ على الاتنين: لا نسخة عالقة، ولا صفحة خطأ خام.
- **مورّد المنصة مستثنى من مولّدي التاسكات العامة** في المسارين:
  `generate_daily_tasks_pulse()` (قوالب الأدوار) و
  `/api/agents/generate-tasks` (وكيل الـAI) — القوالب دي مكتوبة
  لمحلات وصالونات (مخزون · تنضيف محطة) مش لفريق منصة. عملاء B2B
  زي ما هما. أي تعديل على جدول الفريق يتعمل في القوالب مش في
  `daily_tasks` مباشرة.
- **حالة قفل التاسك اليومي = `completed` مش `done`** (`done` دي
  حالة `flow_tasks`). عدّاد `tasks_done_today` في `get_my_work_home`
  كان بيعدّ `done` فكان دايمًا صفر — اتصلح ٢٥/٨ ليقبل الاتنين.
  أي عدّاد جديد على `daily_tasks` يستعمل `in ('completed','done')`.
- **`wa_inbound_watchdog` بيقرا حالة الجلسات من OpenWA مباشرة**
  (pg_net على `/api/sessions` — المفاتيح في whatsapp_config:
  openwa_url/openwa_api_key). قاعدة محمد: «التقارير تقرا من داتا
  حية مش جداول ميتة».
- **مبدأ UI**: أي اختيار نوع بيزنس/تصنيف في أي شاشة إضافة يبقى
  **دروب ليست** مش جريد طويل (لسه ناقص تطبيقه في ويزارد /add-listing).
- **قنبلة completed_by اتفكت (٢٤/٨ بليل)**: daily_tasks.completed_by
  FK على auth.users مش business_employees — الدالة كانت بتكتب صف
  الموظف فكل قفل تاسك يومي كان بينفجر FK من يوم ٢٢/٨. الصح:
  completed_by = auth.uid(). اتجرب بجلسة عبير على تاسك حقيقي واتقفل.
  درس: جرب بجلسة المستخدم الحقيقية قبل ما تقول اتصلح.
- **ملكية المهمة = `is_my_flow_task()` بس** (عرض وقفل). أي تعديل
  على «مين المهمة بتاعته» يتعمل جوّاها هي — مش نسخة جديدة.
- **المارد بيقرا آخر ٣ رسايل بس** (منّنا أو من العميل) — بريف ١٨٠ حرف.
- **الميديا بتتفرّغ أول بأول**: صوت عبر Groq، صورة/PDF عبر نداء هايكو
  صغير بيكتب الوصف جوّه سجل الرسالة (`enrichMediaTranscript`).
- **سامية**: أدمن ماعدا الإداري والحسابات (فلوس/تقارير/تسعير/فريق/
  فروع = false صراحةً في business_employees.permissions).
- **إشعارات الحضور/الانصراف**: تريجر trg_attendance_notify على attendance_logs — أي مسار تسجيل بيبعت push. المستلمين في whatsapp_config: attendance_notify_profile_ids (مفصولين بفاصلة).
- **زوم iOS مقفول** (maximumScale:1 + خانات الموبايل 16px) — أي
  input جديد ماينزلش تحت 16px على الموبايل.

## ٧.٩) قاعدة قفل المشاكل — أمر مباشر من محمد (٢٤/٨)
> «لما أقولك على مشكلة، اقفلها من **كل** الاحتمالات — متسيبش احتمال
> واحد ينفع يكون هو السبب.»
يعني: (١) احصر كل المسارات اللي بتعمل نفس الفعل (دوال، راوتات،
شاشات، تريجرز). (٢) جرّب كل مسار بجلسة المستخدم الحقيقية مش
بالقراءة. (٣) اللي فيه نفس العيب اصلحه حتى لو محدش اشتكى منه.
(٤) وثّق نتيجة المسح.
أول تطبيق: مسح الـ١٣ دالة قفل تاسكات — طلع قنبلة FK تانية في
employee_complete_task_by_pin (مسار البصمة) واتفكت، والباقي
اتفحص واتجرب: complete_my_task (daily+flow) ✓ بالبصمة ✓
crm_task_update ✓ flow_task_set_status ✓ التوجلز ✓.

## ٨) طريقة الشغل
- **اشتغل، متسألش كتير.** لقيت مشكلة؟ صلّحها.
- **متخلّيهوش يجرّب مرة ورا مرة** — شخّص وأصلح وقوله اتعمل إيه.
- **اتأكد من الديبلوي بنفسك** — هات `/_next/static/chunks/*.js`
  ودوّر على كلمة من التعديل.
- **غلطت؟ قول غلطت وصلّحها — ومتكرّرهاش.**
- بعد الخلاص: سطرين. مش تقرير.

---
---

# قواعد العمل على مضمونة

> ## 🔒 حالة تشغيل قائمة — ٣ أغسطس ٢٠٢٦
>
> **٤١ وظيفة في فئة `work` واقفة ومقفولة بأمر محمد.** مايتشغّلوش إلا بأمر صريح منه.
> القفل بترايجر `trg_block_locked_job_enable` + جدول `orchestrator_job_locks` —
> أي محاولة `update orchestrator_jobs set enabled = true` هتفشل.
>
> **ممنوع تشغيلهم من نفسك مهما بدا السبب وجيه.** لو شفت وظيفة واقفة وانت شايف
> إنها لازم تشتغل، اسأل محمد — ماتفكّهاش.
>
> التشغيل: `select public.unlock_orchestrator_job('job_key');`
> التفاصيل: `docs/sessions/2026-08-03-work-jobs-locked.md` ·
> `system_runbook` topic = `orchestrator-work-jobs-locked`
>
> آثار مقصودة: `meeting-reminders` واقف (تذكيرات المواعيد مش بتتبعت) ·
> `snapshot-daily-kpis` واقف (`daily_kpis` مش بيتسجّل) · `olx-scrape-d1..d5` واقفين.
>
> شغّال عادي: الواتساب بيوصّل والمارد بيرد على العملاء.

> اتفاق مع محمد — ١٩/٢٠ يوليو ٢٠٢٦

## 📌 سجل الاتفاقات

**القاعدة صفر: أي حاجة نتفق عليها في الشات تتكتب هنا فورًا.**
مش في آخر الشغل ولا لما أفتكر — **وقتها**. الاتفاق اللي مش مكتوب
بيتنسى، وبنغلط فيه تاني، ومحمد بيضطر يكرّره. ده حصل فعلًا:
اتفقنا نسجّل الاتفاقات ومانفذتش، فمحمد اضطر ينبّهني.

| الاتفاق | التاريخ |
|---|---|
| أي داتا تتبعت للمارد (نص · صورة · PDF · **لينك**) تتفرّغ وتتفهم ويترد عليها | ٢٠ يوليو |
| المارد يعمل الشغل بنفسه — مايقولش «سجّل من اللينك ده» | ٢٠ يوليو |
| العمولة **١٠٪** بس، مفيش اشتراك ولا رسوم تسجيل | ٢٠ يوليو |
| المطاعم: نظام طلبات · بنرشّح المسجّلين · داش بورد من تاب حسابي · عرض ربط السيستم | ٢٠ يوليو |
| الرد يخرج من **نفس الرقم** اللي جت عليه الرسالة | ٢٠ يوليو |
| ممنوع بدء محادثات جديدة — `MARID_REPLY_ONLY` | ٢٠ يوليو |
| أي حركة غير طبيعية تتعالج **فورًا** مش بعد ساعات | ٢٠ يوليو |
| الشريك/المورّد المعتمد مايتعرّفش بمضمونة تاني | ٢٠ يوليو |
| لينك بورصة مضمونة العقارية إجباري لأي مطوّر أو سؤال عن عقار | ٢٠ يوليو |
| كل الأعضاء أدمن في جروب المورّد | ٢٠ يوليو |
| الناس تحس إنها بتكلّم بني آدم — ممنوع «أنا مساعد ذكي» | ٢٠ يوليو |
| **ممنوع نطلب داتا مرتين** — الطلب التاني معناه إننا ضيّعناها | ٢٠ يوليو |
| **ماتقولش «اتعمل» غير بعد إثبات** — الملف اتحفظ + الكوميت في `git log` + اتعمل push. التعديل المحلي لوحده مايكفيش | ٢١ يوليو |
| **المشروع واحد — دايمًا بأفضل شكل** — مفيش «ده مش من تعديلي» ولا «ده خطأ قديم». أي حاجة تشوفها بتضر تجربة المستخدم، اعتبرها مسؤوليتك وصلّحها أو نبّه عليها | ٢١ يوليو |
| **توثيق الرقم = الوارد بس** — مضمونة مابتبعتش أي كود بارد (حماية من الحظر). العميل يبعت `MADxxxxx` للمارد والرقم يتأكّد من مصدر الرسالة. دخول جوجل ياخد إيميل+اسم بس ويوثّق الرقم بالوارد. القرار المقفول كامل في `DECISIONS.md` | ٢٣ يوليو |
| **📁 مكان الريبو المحلي على جهاز محمد: `E:\madmona-app`** (على درايف E — **مش** على C). أي شغل محلي أو Desktop Commander يشتغل هنا | ٢٣ يوليو |

### 🎯 المشروع واحد — ملكية كاملة للتجربة

محمد: «أنا بتكلم عن تجربة المستخدم بشكل عام. مفيش حاجة اسمها ملهاش علاقة بتعديلي».

ممنوع التفكير المجزّأ. لو لقيت خطأ TypeScript قديم، لينك ميّت، شكل مش مظبوط،
أو أي احتكاك في التجربة — **مش «بره نطاقي»**. المشروع كله كيان واحد لازم يفضل
بأفضل شكل. صلّحها لو تقدر، أو على الأقل نبّه عليها بوضوح — بس ماتتجاهلهاش
وتقول «مش من شغلي».

### ✅ التحقق قبل ما تقول «اتعمل»

**«اتعمل» من غير دليل = ماتعملش.** حصل فعلًا: اتفقنا نشيل قسم كأس
العالم من الهوم بيدج، و`git log` أثبت إن `page.tsx` ماتلمسش خالص
من ٩ يوليو لحد ٢١ يوليو — الكلام اتقال والتعديل ماوصلش للـ disk.

**قبل ما تقول لمحمد إن حاجة خلصت، لازم:**
1. الملف اتحفظ فعلًا (مش بس ظهر في الشات).
2. `git log` بيوري الكوميت بتاعه.
3. اللي المفروض يبقى لايف اتعمله `push` — Vercel بينشر تلقائي،
   والتعديل المحلي لوحده **مايظهرش** على الموقع.

**⚠️ الـ Desktop Commander ممكن يفشل يتصل** وقت فتح الجلسة (timeout
٦٠ ثانية بسبب بطء إقلاعه — مؤكّد من اللوج). لو فشل، أي تعديل على
الملفات المحلية إما مايتنفّذش أو مايوصلش، والشات يبان كإنه اتعمل.
**لو DC مش راد، أعد فتح تطبيق Claude واتأكد إنه اتصل قبل أي تعديل.**
(اتطفت الـ telemetry بتاعته في ٢١ يوليو عشان يسرّع الإقلاع.)

### ⛔ «ابعتهالي تاني» = اعتراف بإننا ضيّعنا الداتا

الداتا اللي وصلتنا **ملكنا**. لو مش قادرين نقراها، المشكلة عندنا
مش عنده. نتصرف: نحوّل الـPDF لصور ونقراها بصريًا، ندوّر على
الملف في التخزين، نفتح اللينك، نستخرج من المحادثة نفسها.

الطلب التاني بيوصّل رسالتين: إننا مش منظّمين، وإن وقته مش مهم.
وكتير من الناس مابيردّش على الطلب التاني أصلاً.

## 🖼️ جودة الإعلان — واجهة المشروع

**الإعلان المكسور بيضرب الثقة في المنصة كلها، مش في نفسه بس.**
محمد: «مش شامم ريحة الجودة خالص ومش هعرف ابعت لحد جديد بالوضع ده».

| ⛔ ممنوع يتنشر | ليه |
|---|---|
| صورة تصنيف نمطية | ٨ مطاعم طلعوا بنفس الصورة بالظبط |
| بيزنس كارد كصورة إعلان | مش صورة المنتج ولا المكان |
| صورة مش ظاهرة/مكسورة | العميل بيفتح ويلاقي فراغ |
| صورة مكررة عبر إعلانات مختلفة | بيبان مزيّف |
| تصنيف غلط | معدات مطاعم كانت في «منتجات متنوعة» |

**القاعدة: العدد مالوش قيمة من غير جودة.** ٢٧ مطعم حقيقي
بصورهم أحسن من ٧٤ نصهم بصور نمطية.

### ⛔ بس الحساب مايتقفلش عشان الصورة

محمد: «مش معنى إن الصورة مش حلوة أو مش موجودة إننا نقفل
حساب مورد أو مطعم».

أنا عملت حارس بيمنع النشر لو الصورة ضعيفة — وده كان **غلط**.
المورّد اللي بعتلنا شغله يستاهل يظهر.

اللي بيضر: **الصورة النمطية اللي بتتكرر عبر إعلانات مختلفة**
(٨ مطاعم بنفس الصورة = شكل مزيّف). مش غياب الصورة.

الصح: الإعلان يظهر، والصورة الضعيفة تتعلّم (`quality_flag`)
عشان نستبدلها، ونجيب صورة من أي مصدر حقيقي — لوجوهم، موقعهم،
صورهم على الواتساب.

## ⛔ وعود المارد مايقدرش ينفّذها

**ممنوع يوعد بحاجة مش في إيده.** الوعد اللي مايتنفذش أسوأ من
«مش هينفع».

- ❌ **«هكلّمك على التليفون»** — المارد مابيتصلش. حصل مع مدير
  حاتي المجنون ٢٠ يوليو الساعة ١١:٠٦، والراجل استنى.
- ❌ **«هسجّل الأصناف من اللينك ده»** — لو مفيش أداة تفتح لينك.
  حصل مع Rino's Kitchen ٢٠ يوليو.
- ❌ **«تم التسجيل»** والصور ناقصة — الإعلان بيقف ومحدش يعرف.

## القاعدة الأساسية

**محمد بياخد قراراته بناءً على المعلومة اللي بديهاله. المعلومة الغلط = قرار غلط = ضرر حقيقي.**

فمسؤوليتي مش إني أرد بسرعة — مسؤوليتي إن اللي أقوله يكون **صح ومتحقق منه**.

## قبل ما أقول أي معلومة

**افصل بين "شفت" و"استنتجت".**

- ✅ «الرقم `NOT_VERIFIED` — جبتها من الـ API دلوقتي»
- ❌ «الـ API ماشتغلش ولا مرة» ← ده كان استنتاج من دليلين ضعيفين، وطلع غلط، وكلّفنا الرقم

لو مااختبرتش حاجة بنفسي، أقول: **«ده استنتاج، محتاج أتأكد»**. مش أقولها كأنها حقيقة.

## قبل ما أكتب كود

**اقرا الواجهة الفعلية — ماتفترضش.**

غلطة ٢٠ يوليو: كتبت `api/whatsapp/baileys/route.ts` بالتخمين من ملف تاني. أربع توقيعات غلط (`upsertConversation`, `logInboundMessage`, `getConversationHistory`, `callClaude`) → الويبهوك بيرمي 500 → رسالة أميرة ماردّش عليها.

**القاعدة:** أي دالة هستدعيها، أقرا تعريفها الأول. كل مرة.

## قبل ما أبني حاجة جديدة

**افحص اللي موجود.**

غلطة ٢٠ يوليو: بدأت مشروع Railway جديد من غير ما أبص على الـ ٤ مشاريع الموجودة. محمد هو اللي نبّهني.

## قبل أي إجراء غير رجعي

الحذف، إلغاء التسجيل، مسح داتا، تغيير على الإنتاج:

1. اتأكد من **السبب الحقيقي** — مش أول تفسير معقول
2. اعرض **الخسائر بالتحديد** — مش «ممكن تخسر حاجات»
3. لو الأدلة ضعيفة، **قول كده صراحة قبل التنفيذ**

## لما محمد يقول «اتصرف»

**يعني نفّذ فعلاً** — مش ترجع تسأل تاني. هو واثق في القرار التقني.

بس ده **مش تفويض إني أتصرف من غير ما أتأكد**. الثقة دي معناها إن عبء التحقق كله عليّا أنا.

## الاستثناءات — دايمًا أوقف وأسأل

- باسوردات، كروت دفع، أكواد OTP
- CAPTCHA / «Verify you are human»
- حذف بيانات مش راجعة
- إرسال رسايل لعملاء (النص لازم يتراجع الأول)

---

## دروس تقنية من ٢٠ يوليو

**١. `.vercelignore` كان بيحجب `/api/whatsapp/*`**
سطر `WHATSAPP` (المقصود بيه فولدر التوثيق) كان بيطابق `src/app/api/whatsapp/` كمان. الويبهوك كان **404 لشهور**. اتصلح بـ `/WHATSAPP`.
**الدرس:** لما مسار واحد بس يفشل والباقي شغال، شوف ملفات الاستبعاد قبل ما تشك في الكود.

**٢. الرقم إما على واتساب العادي أو على Cloud API — مستحيل الاتنين**
مفيش تطبيق ولا use case ولا مزود بيغيّر ده. لو عايز الاتنين → **رقمين**.

**٣. `callClaude` بتاخد نص بس**
`{systemPrompt, userMessage}` — مافيهاش `messages` array ولا بلوكات صور. الصور والـ PDF محتاجين تعديل عليها، مش شغالين تلقائي.

**٤. Claude مابيسمعش صوت**
التفريغ لازم مزود خارجي (Groq/Whisper).

**٥. Baileys محتاج بروسيس دايم**
Vercel serverless مايقدرش. عشان كده الخدمة على Railway بـ volume على `/data`.

**٦. واتساب بيبعت مُعرّف مخفي (LID) بدل الرقم**
`23889212117111@lid` — ده **مش رقم تليفون**. Baileys 6.7.9 مافيهاش أي طريقة
ترجّع الرقم منه. لو عيدت تركيبه لـ `@s.whatsapp.net` هتبعت لرقم مش موجود،
وBaileys هيقبله ويدّيك `wa_message_id` والرسالة تروح في الفراغ.
**القاعدة: رُد على نفس الـ JID اللي جت منه الرسالة — ماتعيدش تركيب أي مُعرّف.**

**٧. `wa_message_id` موجود ≠ الرسالة وصلت**
ده أخطر إشارة كاذبة قابلناها. لما محمد يقول «مارّدش» والداتابيز بتقول
«اتبعت» — الداتابيز مش دليل على الوصول. شوف الـ JID الأصلي.

**٨. أي سكريبت codemod لازم يحترم `'use client'`**
سكريبت `fix-localstorage.mjs` حط `import` فوق `'use client'` في ١٢ ملف.
الديركتيف لازم يبقى **أول سطر فعلي** — غير كده الملف يبقى مكوّن خادم
بيستخدم hooks، وnext build يقع. النتيجة: **٥ نشرات إنتاج فاشلة ورا بعض**،
وكل إصلاحات المارد فضلت مش منشورة، ومحمد بيقول «لسة مشتغلش» وأنا بختبر
كود قديم. اتصلح بـ `scripts/fix-use-client.mjs`.
**الدرس:** بعد أي سكريبت بيعدّل ملفات بالجملة — **اجري `npm run build`
واتأكد إن النشر نجح**، مش بس إن الكوميت اتدفع.

**٩. الكود القديم بيرد وأنا فاكره الجديد**
لما اختبار يرجّع نفس النتيجة القديمة بعد إصلاح — **مش دايمًا الإصلاح غلط**.
حط علامة تشخيصية في النص (زي `[صيغ:… صفوف:…]`)؛ لو ماظهرتش يبقى النشر
هو المشكلة مش الكود. `npx vercel ls` بيوري حالة البناء.

**١٠. `/health` بيعرض الكوميت الشغال**
`curl .../health` → `version.commit`. قبل ما تقول «اتصلح»، اتأكد إن النسخة
اللي فيها الإصلاح هي اللي شغالة فعلاً.


---

## 🧭 قواعد معمارية ثابتة — المارد (غير قابلة للتغيير / LOCKED)

> متفق عليها ومقفولة مع المالك. أي شغل في المشروع لازم يمشي عليها. أي أوتوميشن جديد يتبنى بالقواعد دي. آخر تحديث: 28 يوليو 2026.

**1) كل الأجينتس تحت المارد.**
مفيش أي أجينت أو كرون يشتغل لوحده أو بره تنسيق المارد (orchestrator). نقطة تحكّم واحدة، نبضة واحدة، ووتش-دوج يحميها ويرجّعها لو وقعت. ممنوع الكرونات المبعثرة أو الأنظمة الموازية.

**2) مش شغّال دوري — شغّال بالنبضة لما يكون فيه شغل.**
كل أجينت عنده «شرط شغل» (`orchestrator_jobs.work_check` = استعلام EXISTS). نبضة المارد (كل دقيقة) بتبصّ: «فيه شغل للأجينت ده؟» → تشغّله؛ «مفيش؟» → تعدّي. ممنوع أي جدول أعمى (`policy_cron`) يشغّل أجينت شغل على الفاضي. الكرون يفضل بس للبنية التحتية نفسها (النبضة، الطابور، مراقبة الصحة).

**3) البيانات الناقصة تتطلب أوتوماتيك.**
أي منتج/مشروع ببيانات ناقصة → المارد يطلب الناقص من مصدره (السمسار/صاحب الليستنج) مرة واحدة بس (dedup)، عبر قناة OpenWA/القوالب المعتمدة، وأرقام مصرية صح بس.

**4) النشر والتنظيف.**
المكتمل يتنشر تلقائي · الناقص يفضل ويتطلب · الناقص القديم يترحّل (`status=expired`, قابل للرجوع) — **مش يتمسح نهائي**. أي حاجة متعلّم عليها «ممنوع النشر / مستني تفويض» ما تتلمسش.

**5) النطاق.**
القواعد دي على **كل المنتجات** (مش بس العقارات)، وثابتة على مستوى مشروع مضمونة، وعلى مستوى أي شغل مع كلود (كو-ورك أو شات).

**المثال المعتمد (البورصة/المشاريع):** السمسار يبعت مشروع على الواتساب → النبضة تلاقي شغل → تستخرج المشروع بـClaude → تنشره → تطلب الناقص → تعمل صورة غلاف. كله بالنبضة، لما يكون فيه شغل — من غير جدولة ومن غير تدخّل يدوي.


---

## 🔒 نظام الواتساب — مقفول (LOCKED / غير قابل للتغيير)

> متفق عليه ومقفول مع المالك. **ممنوع أي جلسة تغيّره** — لا على مستوى المشروع ولا الأكونت. آخر تحديث: 28 يوليو 2026.

**القناة الرسمية = OpenWA** (whatsapp-web.js). التلات أرقام كلها `transport='openwa'`:
- `201002229982` — الرقم الأساسي
- `201026222337` — محمد ناصف (بيزنس موثّق)
- `201114621551` — الرقم المساعد

(جلسة `web` = شات الموقع، `transport=baileys` — قناة منفصلة تمامًا، مالهاش علاقة بأرقام الواتساب.)

**الممنوعات (مقفولة — ما تتلمسش):**
1. ممنوع إعادة تفعيل **ميتا كلاود** (`whatsapp-webhook` مقفول كـstub، `meta_cloud:disabled`). سبب القفل: كان بيعمل «مخّين» بيردوا على العميل سوا.
2. كل الإرسال يعدّي على **الجيت-واي** `/api/internal/wa-send` → يوجّه لـOpenWA حسب `wa_number_configs.transport`. **ممنوع** الإرسال يروح لبريدج بايليز الميت (`WA_SERVICE_URL` القديم على Railway) — ده كان بيرجّع «مفيش جلسة متصلة» ويحرق الليدز.
3. الطابور الخارجي (`fire_whatsapp_outbound_send`) والسويبر والتنبيهات → كلهم على الجيت-واي، مش على البريدج مباشرة.
4. ممنوع أي جلسة تغيّر `transport` أي رقم، أو توجيه الإرسال، أو ترجّع بايليز، أو تفتح ميتا كلاود. لو محتاج تغيير، ارجع للمالك الأول.

**الخلاصة:** الواتساب = OpenWA عبر الجيت-واي. ثابت ومقفول.


---

## 🖼️ حارس صور الإعلانات/البورصة — `image_text_sentinel` (تحت المارد)

أجينت تحت المارد بيمنع إن صورة إعلان أو غلاف بورصة يبقى فيه **كتابة محروقة** (منيو، برايس ليست، فلاير، خريطة، كارت، لوجو منافس، واترمارك). اتبنى بنفس القواعد الثابتة (بالنبضة لما يكون فيه شغل، بيطلب/يصلّح مش يمسح).

- **`work_check`:** فيه صورة رئيسية (real — مش برندنا `cloudinary` ولا `is_placeholder`) لسه `quality_flag = null`؟ أو غلاف بورصة منشور `cover_checked_at = null`؟
- **الفحص = Claude vision:** الصورة أساسًا كلام/جرافيك → يصلّح. رندر احترافي فيه لوجو صغير أو صورة حقيقية → يسيبها ويعلّمها `clean`.
- **التصليح (مش مسح):** يرقّي أنضف صورة تانية للإعلان؛ لو مفيش → يحط صورة القسم كواجهة (`is_placeholder`, `caption=cat-fallback-auto`) والأصلية تفضل في المعرض. البورصة: يفضّي الغلاف → كارت مبرند يتولّد (`bourse_cover_cards`).
- **بيعدّي على:** صورنا المبرندة (`cloudinary`) والـ placeholders — ما يتلمسوش أبدًا.
- **Edge function:** `image-text-sentinel`. التتبّع: `listing_photos.quality_flag` (null=لسه · clean · has_text · graphic · logo · broken) و`property_market_items.cover_checked_at`.
- **نسخ احتياطية (تنظيف 28 يوليو):** `_backup_primary_swap_20260728` (ترقيات الصور اليدوية). التنظيف اليدوي والحارس كله قابل للرجوع.

## 🔁 مزامنة البورصة للإعلانات — `sync_bourse_to_ads` (تحت المارد)

أي مشروع بورصة منشور (`property_market_items`, جاي من السماسرة) لسه من غير إعلان (`listings.project_id`) → المارد يعمله إعلان تلقائي بالنبضة: كاتيجوري `sale-properties-*` حسب `property_type`/المنطقة، سعر من `price_from`، مورد MADMONA، رقم التواصل الرسمي، وصورة الإعلان = **غلاف المشروع** (كارت المطور المبرند). دالة SQL `public.sync_bourse_to_ads(p_max)`. `work_check` = فيه مشروع بورصة منشور من غير إعلان. نسخة احتياطية `_backup_bourse_sync_listings_20260728` + وسم `directory_source='bourse-sync'`.

## 🏷️ كروت الإعلانات المبرندة — `listing-brand-cards` (تحت المارد)

الإعلان اللي واجهته صورة قسم مؤقتة (`caption='cat-fallback%'` — يعني كان فيه كتابة ومفيش صورة حقيقية) → المارد يستبدلها بـ**كارت مضمونة مبرند باسم النشاط** (لوجو المطعم/المحل) عبر `post-render` (template `value`). اختيار الاسم: اسم المورد الحقيقي لو نضيف (مش «مضمونة …» ولا «حساب …» ولا رقم) → وإلا عنوان الإعلان → وإلا اسم القسم. الكارت بيبقى `caption='brand-card'`. `work_check` = فيه صورة رئيسية `caption like 'cat-fallback%'`.

**السلسلة الكاملة لصور الإعلانات:** `image_text_sentinel` يكشف الكتابة → مربوط بمشروع؟ غلاف المشروع (لوجو المطور) · مفيش؟ صورة قسم مؤقتة → `listing-brand-cards` يحوّلها لكارت باسم النشاط. كله بالنبضة، ومايلمسش صورنا المبرندة (`cloudinary`) ولا الصور الحقيقية.

## 🖼️ عرض الصور — قايمة الدومينات + إعادة الاستضافة (مهم)

الموقع بيعرض الصور عبر **Next.js image optimizer**، اللي بيرفض أي دومين مش مسجّل في `next.config.mjs` → `images.remotePatterns` (بيرجّع **400** والصورة ماتظهرش). لو ضفت صور من مصدر جديد، لازم تضيف الدومين هناك. المسجّل حاليًا: supabase، `res.cloudinary.com` (كروتنا)، `media.canva.com`، `images.unsplash.com`، + مصادر سمسارة/مطاعم خارجية (yallamenu، graph.facebook.com، tripadvisor، `*.lovable.app`، sharkawy، wikilist، deliveryhero، linktree).

**الحل الجذري — `rehost-external-media` (أجينت تحت المارد):** أي صورة من مصدر خارجي (مش supabase ولا `res.cloudinary.com`) → المارد ينزّلها ويرفعها على `content-images/rehosted/` ويغيّر الرابط لتخزيننا. فكل الصور بتبقى من عندنا (ماتكسرش لو الدومين مش مسجّل، والصور الخارجية اللي بتنتهي متضيعش). `work_check` = فيه صورة رابطها خارجي ولسه ماتنقلتش. سجل `_rehost_log` (orig_url/new_url/status). الفاشلة (المصدر ميت) تتعلّم `failed` وتتعدّى. **بعد ما ده يشتغل، الدومينات الخارجية في `next.config` بتفضل بس كشبكة أمان للصور الجديدة قبل ما الأجينت ينقلها.**


## 🗂️ تنظيف صور الأقسام (categories) — تنفيذ 28 يوليو (لقطة واحدة، مش أجينت دائم)

مسح شامل لكل صور الأقسام (316 قسم / 59 صورة فريدة) بالـOCR (tesseract ara+eng) + معاينة بصرية. اتصلّح **8 صور** كان فيها كتابة محروقة أو مربعات خضرا فاضية (بتأثّر **38 قسم**):

- **كتابة/جرافيك:** `real/education-courses.jpg` (كتاب رياضة فنلندي، 12 قسم) → `learning-courses.jpg` · `real/printing.jpg` (بوستر إعدام تشيكي/ألماني قديم في الخلفية، 9 أقسام) → `printing-services.jpg` · `real/food-egyptian.jpg` (بادج MyPlate.gov) → `food-egyptian-v2.jpg` · `pro/cat-final/shop-motorcycle-parts.jpg` (واترمارك مصوّر) → `motorcycle-parts.jpg`
- **مربعات خضرا (placeholder قديم كان لايف):** `properties-v1.jpg` (properties + 11 شاليه ساحل، 12 قسم) → `properties-chalet.jpg` · `equipment-v1.jpg` (gaming-consoles) → `gaming-consoles.jpg` · `services-v1.jpg` (كان مشترك بين lawyers + recreation-kayak — **اتفصل لصورتين**) → `legal-services.jpg` + `recreation-kayak.jpg`
- كل الصور البديلة نضيفة/بدون كتابة (اتفحصت OCR + بصريًا)، اترفعت على bucket **`ads/categories/`** بأسماء جديدة (كسر الكاش)، واتأكد إنها بتظهر لايف عبر الـNext.js optimizer (200 image/jpeg). التحديث بمطابقة الـURL القديم بالظبط + فصل `services-v1` بالـslug.
- **`cat-img-upload`** edge function كان أداة رفع لمرة واحدة (service-role جوّه، anon JWT للنداء) → **اتقفل كـstub بيرجّع 403** بعد الاستخدام عشان الـanon key ما يقدرش يكتب في التخزين. الأقسام مجموعة ثابتة (مش user-generated زي الإعلانات/البورصة)، فمفيش أجينت دائم؛ لو احتجنا منع تلقائي لأي صورة قسم جديدة فيها كتابة، ممكن نضيف `work_check` على `categories` بنفس فكرة `image_text_sentinel`.


## 🧩 توحيد صور الأقسام: الإضافة (بيع/خدمات) = العرض (إيجار) — تنفيذ 28 يوليو

كان تراك **الإيجار (rentals) كله بصور (137/137)**، بس تراك **البيع (products) فيه 61 قسم نشط بدون صورة** والخدمات 32 — فاللي بيضيف إعلان بيع/خدمة كان بيشوف أقسام فاضية. الإجمالي **93 قسم نشط بدون صورة**. الإصلاح (مباشر، مش أجينت — الأقسام ثابتة):
- **56 قسم** اترقّعوا من **التوأم بنفس الاسم** (نفس القسم في تراك تاني عنده صورة، مثال `sale-properties-apartment` ← صورة `properties-apartment`).
- **35 قسم** اترقّعوا من **صورة القسم الأب** (أبناء تجميل → تجميل، أبناء الخدمات المنزلية → الخدمات المنزلية، أبناء الإلكترونيات → الإلكترونيات).
- **2 يتامى** (مفيش توأم ولا أب بصورة) اترفعلهم صور جديدة: `sale-kitchens` (مطابخ) و`equipment-restaurant` (معدات/مطابخ مطاعم).
- **النتيجة:** كل الأقسام النشطة (393/393) بقى عندها صورة — صفر فاضي. نسخة احتياطية `_backup_category_img_fill_20260728` (id/slug/old_image_url) للرجوع.

**التكرار:** مفيش أقسام مكررة حقيقية — كل التكرار في الأسماء هو **بيع (products) ↔ إيجار (rentals)** مقصود (سلاق `sale-` منفصل + تراك مختلف). الزيادة الوحيدة `sale-vehicles-used-car` («سيارة» مستعملة) **مقفولة أصلاً** (`is_active=false`).


---

## 🩺 قواعد الداتابيز — فخّين كلّفونا شغل ضايع (٢٢ أغسطس ٢٠٢٦)

محمد: «بلاقي حاجات بتقع بعد ما بنقفل الجلسة». اتكشف السبب، وده مش تخمين:

### ⛔ فخ ١: `CREATE FUNCTION` + `rollback` في نفس النداء = الدالة بتضيع

الـDDL في بوستجرس **جوّه ترانزاكشن**. فلو عملت:

```sql
create or replace function foo() ...;
begin; set local role authenticated; select foo(); rollback;   -- ❌
```

الـ`rollback` **بيلغي إنشاء الدالة نفسها**. التجربة بتطلع ناجحة، وإنت بتقول
«اتعمل واتجرّب»، **والدالة مش موجودة أصلًا**.

كده ضاعت `madmona_team_accounts` **مرتين** — وشاشة «إدارة الموظفين» فضلت
مكسورة في البرودكشن ومحدش واخد باله. و`crm_health` نفسها ضاعت أول مرة اتعملت.

✅ **القاعدة:** الإنشاء في نداء **لوحده**. التجربة في نداء **تاني**. وبعدها
تأكيد إن الدالة موجودة فعلًا:
```sql
select proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace
 where n.nspname='public' and proname = 'اسم_الدالة';
```

### ⛔ فخ ٢: زيادة بارامتر بـ`DEFAULT` بتعمل دالة **تانية** مش بتستبدل القديمة

`create or replace` بيطابق على **التوقيع** مش على الاسم. فلو زوّدت بارامتر،
بتبقى عندك نسختين، والنداء بالاسم من PostgREST ممكن يقع على القديمة.

حصل مع `crm_log_call` (١١ → ١٤ بارامتر) و`crm_my_queue` (١ → ٢).

✅ **القاعدة:** بعد أي زيادة بارامتر، امسح القديمة:
```sql
drop function if exists public.اسم_الدالة(الأنواع_القديمة);
```
وفيه **حارس في الداتابيز** (`trg_block_crm_overloads`) بيرفض أي `CREATE
FUNCTION` ينتج نسختين من نفس دالة `crm_*` — ماتشيلوش.

---

## 📇 نظام الـCRM — من فين تبدأ في أي جلسة جديدة

**الشاشات:** `/admin/crm` (الأدمن) · `/crm` (الموظف على الموبايل، جوّه
«شغلي» = `/account/work`) · `/admin/staff` (الفريق وحساباته)

**أول حاجة تعملها:**
```sql
select jsonb_pretty(crm_health());
```
بترجّع `ok:true` لو النظام سليم، ولو فيه دالة/جدول/عمود/بوليسي ناقص بتقولك
اسمه بالظبط. البانر ده بيبان كمان فوق في `/admin/crm`.

**لو فيه حاجة ناقصة:** شغّل `sql/2026-08-22_crm_functions_rebuild.sql` كامل.

**بعد أي تعديل على الداتابيز:**
```bash
node scripts/dump-crm-sql.mjs   # بيولّد ملف إعادة البناء من السيرفر نفسه
git add sql/ && git commit
```
لو ماعملتش كده، التعديل بيعيش في السيرفر بس — ودي بالظبط المشكلة اللي فوق.

**العقد:** جدول `crm_contract` فيه كل حاجة الشاشات معتمدة عليها. أي دالة
جديدة الشاشات هتناديها **لازم تتضاف للعقد** وإلا الفحص مش هيحميها.

**التوثيق والقرارات:** `sql/2026-08-21_crm_madmona.sql` — ليه كل حاجة اتعملت
كده، وإيه اللي **ماتعملش عن قصد**.



## البيزنس كلاود للمطورين (٢٤/٨/٢٠٢٦)
- كل مطور في بورصة العقارات (property_market_items.developer) ليه صفحة كلاود `/s/dev-*` — ٦٨ مطور = ٦٨ صفحة (اتعمل provision لـ Al Qamzi / J communities / Qatari Diar اللي كانوا ناقصين).
- `public_storefront_listings(p_slug)` بترجّع الوحدات المنشورة من listings **+ مشاريع البورصة** النشطة غير المحظورة للمطور (مطابقة بـ business_name = developer)، وكل عنصر فيه `href` جاهز — الفرونت في `/s/[slug]` يستخدم `u.href` وما يبنيش اللينك بنفسه.
- الهوية: ٢٩ مطور عندهم لوجو، الباقي (~٣٦) محتاجين ملفات لوجو حقيقية من محمد. مفيش ثيمات ألوان متخزنة للمطورين عن قصد (قاعدة «تصميم الديسكتوب من غير ألوان متخزنة»).
- درس git (٢٤/٨): `git add -A` شفط ١٨ ألف ملف (بروفايلات كروم بتاعت سكريبتات الريلز) — اتضافوا لـ .gitignore. **دايمًا add بالأسماء، مش -A.**


## مسميات «إنت مين؟» + دروب ليست الإضافة (٢٤/٨/٢٠٢٦)
- سؤال «إنت مين؟» في الإضافة بيتكلم بلغة القسم (sellerOptions في AddListingClient): الطب = دكتور حر / عيادة / مجمع عيادات (المجمع = business + فروع)، التعليم = مدرس / سنتر، العربيات = فرد / معرض-تاجر، العقارات = مالك / شركة-مطور-مكتب… المتسجّل دايمًا individual/business → seller_kind. أي قسم جديد ياخد الديفولت «فرد / نشاط تجاري».
- دروب ليست اختيار النشاط بتعرض **كل** التصنيفات مش تصنيفات التاب المفتوح بس، وكل مجموعة معنون عليها نوعها (— بيع/إيجار/خدمات/مطاعم).
- شجرة التصنيفات في fetch صفحة الإضافة بتجيب الأحفاد (مستوى تالت) وتضمهم ورا أبوهم — «سيارة زيرو» و«سيارة مستعملة» كانوا مش بيوصلوا للويزارد خالص قبلها.
- جدول attributes: كل «الماركة» في المركبات بقت select (٤٦ ماركة عربيات / ١٧ موتوسيكل + «أخرى») و«سنة الصنع» select من 2027 لـ1990 — اتعمل على مستوى الجدول كله مش تصنيف واحد. وفيه ٩ خانات options كانت متخزنة نصوص بدل {key,label_ar} (الأزرار كانت بتطلع فاضية) — اتصلحوا. sale-vehicles-luxury كان فيها خانتين «الماركة» — التوأم اتشال.
- درس SQL: `unnest(...) with ordinality as t(b,i)` مع CTE alias بالمقلوب `brands(i,b)` قلب الأسماء أرقام — دايمًا سمّي أعمدة الـCTE بنفس ترتيب الإخراج.


## مفاتيح أدوات المارد + قاعدة السكوت (٢٤/٨/٢٠٢٦)
- محمد: «المارد مش نافع إنه يضيف إعلانات ويستدعي أدوات فألغيها — الإضافة تكون
  عن طريق صاحب الإعلان». **الأداة اتقفلت من الداتابيز، ماتحذفتش من الكود.**
  الجدول `marid_tool_settings` · الشاشة `/admin/marid` → كارت «🔌 أدوات المارد».
  المطفي دلوقتي: `create_listing_draft` · `add_menu_items`.
- **قفل أداة = ٣ حراس مع بعض** (`src/lib/marid-tool-settings.ts`)، وواحد لوحده
  مايكفيش: (١) الأداة ماتتبعتش لكلود، (٢) تعليمة بديلة تتحقن في **آخر** البرومبت
  كله، (٣) `runMaridTool` بيرفض النداء ويسجّله في `marid_tool_blocked_log`.
  التعليمة لازم تبقى آخر حاجة لأن `customer-concierge.ts` فيها ١٥ تعليمة بتأمر
  بنداء `create_listing_draft`، وبلوك الميديا في `marid-brain.ts` كمان.
- حارس الوعد الكاذب في `marid-brain.ts` بقى بياخد `listingToolOff`: لما الأداة
  مقفولة التصحيح بيقول «قوله يضيف بنفسه» بدل «نادِ الأداة» — من غير كده كان
  هيبعت المارد يدوّر على أداة مش موجودة.
- **قاعدة السكوت:** `marid_should_skip(phone)` هي اللي بتقرر مين المارد مايردّش
  عليه — الفريق · رقم مارد · رقم كان مارد قبل كده · مسكّت يدويًا. المطابقة على
  آخر ١٠ أرقام. الحارس في `/api/whatsapp/baileys` (و`openwa` بيحوّل عليه فحارس
  واحد بيكفي الاتنين).
- ⚠️ **أوامر الأدمن من واتساب وقفت** — استثناء `!isAdmin(phone)` اتشال بناءً على
  تعليمة محمد. الرجوع من غير نشر: صف في `marid_skip_exceptions` بالرقم.
- التوثيق الكامل والقرارات: `sql/2026-08-24_marid_tool_switches_and_skip.sql`.

- (٢٥/٨/٢٠٢٦) /admin/listings هي الشاشة الوحيدة لكل الإعلانات: فيها لوحة «واردة الويزارد» (جدول listing_drafts) جنب الإعلانات الفعلية (جدول listings). المسار القديم /admin/listing-drafts بقى redirect لـ /admin/listings?stage=wizard — أي شاشة إعلانات جديدة تتضاف جوّه /admin/listings مش كصفحة منفصلة. محمد: «انا عايز مكان واحد يعرض الاعلانات كلها وهو ده الي يكون ظاهر عند الموظفين».

- (٢٥/٨/٢٠٢٦) إعلانات الويزارد مسموعة: تريجر notify_wizard_draft_submitted على listing_drafts بيبعت پوش لكل موظفي can_manage_listings لما الإعلان يبقى submitted، وكارت الإعلانات في شغلي فيه زرار «واردة الويزارد» بعدّاد من wizard_drafts_pending_count(). ⚠️ درس مهم: notification_queue_dedupe بيرفض نفس (recipient + title) خلال ٦٠ دقيقة — أي إشعار جديد لازم عنوانه يحتوي حاجة مميزة للحدث نفسه (اسم الإعلان/التاسك) مش عنوان ثابت، وإلا التاني في نفس الساعة هيتبلع بصمت.

- (٢٥/٨/٢٠٢٦) دروب ليست «اختار نشاطك» في الإضافة بتتبني بنفس نظام مجموعات كروت الموبايل (group_slug → roots → subs) مع dedupe عام بالـslug — أي تعديل على التصنيفات لازم يتشاف في الاتنين. توحيد التاسكات: get_my_tasks (الشات) و get_my_work_home.my_tasks (شغلي) لازم يرجّعوا نفس المجموعة بالظبط — اختبار التطابق: عدّ الـids المشتركة للموظف نفسه. ⚠️ رقم مضمونة العام 01002229982 معمول له is_platform_public_phone(): ممنوع يدخل في أي منطق «الملكية بالرقم» (asset_owners, auto-claim) — الرقم ده بيتحط مكان أرقام الموردين فأي ربط بيه بيرمي أصول المنصة كلها على حساب محمد الشخصي.

- (٢٥/٨/٢٠٢٦) نظام التجميل والمطاعم الواقعي: بيزنس واحد = إعلان واحد + أصنافه جوّاه. المطاعم → restaurant_menu_items، أي حاجة تانية (تجميل/خدمات/منتجات) → mart_products. ممنوع «إعلان لكل خدمة/صنف» وممنوع جدول أصناف جديد موازي — الاتنين دول هما النظام. موظفي مضمونة ليهم RLS policies (mart_products_madmona_staff / rmi_madmona_staff) وإدارة الأصناف من زرار 🍽️/🧾 في /admin/listings. عنوان الكتالوج في صفحة الإعلان بيتغير حسب التصنيف (تجميل/خدمات → «الخدمات والأسعار») عبر defaultCatLabel.

- (٢٥/٨/٢٠٢٦) /login شاشة واحدة موحّدة: فورم (رقم/إيميل + باسورد أو PIN) عبر login_with_password — ممنوع أي شاشة «أنا موظف» منفصلة على مستوى المشروع؛ الواتساب زرار بديل جوه نفس الشاشة. ⚠️ درس صور الإعلانات: listing_photos ليها policy لموظفي مضمونة (listing_photos_madmona_staff) — أي جدول تابع للإعلانات (صور/منيو/كتالوج/أسعار) لازم يكون فيه is_admin_or_listings_staff() في سياساته، لأن الموظفين مش is_admin() عمدًا، والفشل بيكون صامت (الرفع للـstorage بينجح والصف بيترفض).

- (٢٥/٨/٢٠٢٦) سلسلة النشر فيها تريجرين على listings لازم يتراعوا مع بعض: enforce_listing_publish_requirements (صورة واحدة + توثيق رقم بواتساب — موظف مضمونة بجلسته الحقيقية مستثنى من شرط الرقم، ومضمونة الحقيقية c8b7b9d7-6178-… والوصيّة 267e0655-… في الاستثناءات) و enforce_listing_content_minimums (رسالة «النشر متوقف: …» — بيرفض لو كل الصور placeholder من /ads/categories/ أو بوستر cloudinary posts/test، أو مفيش سعر لغير العقارات). الصورة اللي شكلها موجود في الثمبنيل ممكن تكون placeholder تصنيف مش صورة حقيقية — دايمًا اتأكد بعدّ الصور الحقيقية مش بوجود أي صف في listing_photos.

- (٢٥/٨/٢٠٢٦) قاعدة محمد: «الإصلاحات تشمل المشروع مش شاشة». طبقة الحقول الموحّدة للإعلانات هي جدول attributes (بالتصنيف) + listing_values — الويزارد (عبر /api/listing-drafts/attributes) وفورم التعديل (ListingForm) الاتنين بيقروا منها، وclaim_listing_draft بقى بينقل كمان product_details بتاعة الويزارد (brand→make …) لنفس القيم. وحدات التسعير مصدرها categories.allowed_pricing_periods/default_pricing_period في كل الشاشات. شجرة بيع المركبات اتفردت: «سيارة زيرو/مستعملة» مباشرة تحت «مركبات ونقل» والمستوى الوسيط «مركبات زيرو/مستعملة» اتقفل — أي تصنيف وسيط مكرر بيتقفل مش بيتمسح.

- (٢٥/٨/٢٠٢٦) الدخول الموحّد: /login → POST /api/login — بيجرّب platform_admins الأول (bcrypt + scrypt القديم، نفس منطق admin-entry) ولو نجح بيفتح كوكي اللوحة + madmona_token مع بعض، وإلا business_employees (باسورد/PIN) عبر login_with_password. أي مسار دخول جديد يتبني على /api/login دي — ممنوع RPC دخول جديدة تتنادى من المتصفح مباشرة. issue_madmona_token_for_admin للـservice_role بس.

- (٢٥/٨/٢٠٢٦) قاعدة الحقول: أي سؤال عن مواصفات (ماركة/موديل/سنة/…) يتسأل من جدول attributes بس — ممنوع خانات مكررة hardcoded في الويزارد أو فورم التعديل. كل select فيه «أخرى…» = كتابة حرة (SelectWithOther في ListingForm، وAttributeFieldRenderer في الويزارد). كل تصنيفات المركبات (بيع + بحرية) عندها make/model/year. خانات brand/model_name في listings بتتملى أوتوماتيك من قيم make/model عند الحفظ (للعرض) — مش من إدخال منفصل.

- (٢٥/٨/٢٠٢٦) طابور البان الحقيقي هو whatsapp_campaign_messages (مش whatsapp_outbound_queue) — الرسايل المتأخرة بتنطلق دفعة واحدة أول ما البوت يتوصل. الحماية: pg_cron job «wa-campaign-stale-cancel» (كل ساعة، jobid 211) بيلغي أي queued عدّى معادها ٢٤ ساعة، وشاشة /admin/send فيها كارت الطابور بزرار «🧹 امسح الطابور» (DELETE /api/admin/send، ?session= اختياري). ملحوظة: cron.schedule جوه migration ممكن ميتسجلش بصمت — دايمًا اتأكد بـ select من cron.job بعدها.

- (٢٥/٨/٢٠٢٦) عمود «النشر» في /admin/listings = published_at (تاريخ أول نشر) و«— لسه» = مفيش تاريخ. تريجر trg_set_published_at بيسجله أوتوماتيك عند أي نشر من أي مسار. تنضيف الأصناف: أي إعلان-صنف (أكل/تجميل) محتواه موجود جوه منيو/كتالوج بيزنس اتنقل للـrecycle bin (١٢٤ يوم ٢٥/٨) — الاسترجاع بـ restore_listing(id). لسه فيه ~١٨٠ درافت صنف من غير بيزنس أم (بلا رقم) — مستنية قرار محمد: مسح ولا مراجعة.

- (٢٥/٨/٢٠٢٦) نبض الأداء: notify_employee_performance() بيقرا من daily_tasks الحقيقية ويبعت لكل موظف مضمونة پوش + إشعار داخلي (نسبة الإنجاز، المتأخر، أهم ٣ مهام مطلوبة). مجدول pg_cron مرتين يوميًا: employee-performance-pulse-midday (١ الضهر القاهرة) و-eod (٥:٣٠). العنوان فيه النسبة والتاريخ عشان dedupe الساعة ما يبلعوش.


## 📋 المهام في مكان واحد + لغز الكرونات المختفية (٢٥ أغسطس ٢٠٢٦)
- محمد: «عايز تاسكات تكون في مكان واحد وبتاب التفاصيل وعايز اعادة صياغة لتاب شغلي». `/chat/tasks` بقت ريدايركت على `/account/work` — أي شاشة مهام جديدة ممنوعة، «شغلي» هي الوحيدة. المهام متجمّعة (متأخر 🔥 / بمعاد / من غير معاد) وزرار «ضيف مهمة» بينادي `add_my_task`.
- 🕵️ **قاعدة حرجة**: الكرونات كانت بتختفي لأن `marid_cron_guard` (بيتنده من نبضة المارد) بيمسح أي جوب مش مسجّلة في `public.cron_allowlist` (عمود reason إجباري). **أي `cron.schedule` جديد لازم يتسبقه insert في `cron_allowlist`** — وبعدها اتأكد بتشغيل `select public.marid_cron_guard()` يدوي وشوف إن الجوب لسه عايشة في `cron.job`.


## 🧑‍💼 موظفين الأبليكيشن في شاشات /admin (٢٥ أغسطس ٢٠٢٦)
- محمد: «اعلانات شهد لسة مش بتنزل مع انها ضايفاها من تاب شغلي». السبب: middleware بيحرس /admin و/api/admin بكوكي اللوحة بس، وجلسة الأبليكيشن في localStorage (الـedge مايشوفهاش) — فأي موظف داخل من شغلي كان بياخد 401/ريدايركت وكل adminRpc بيفشل.
- **القاعدة**: أي شاشة أدمن المفروض موظفين الأبليكيشن يستخدموها لازم: (١) استثناء pass-through في middleware للصفحة، (٢) قبول توكن Supabase في مسارات الـAPI بتاعتها عبر `isListingsStaffRequest` (adminGate — edge-safe، بيتأكد بـ`is_listings_staff_uid`)، (٣) الفرونت يبعت التوكن بـ`staffAuthHeaders()` من adminRpc. الدوال المسموحة للـstaff في /api/admin/rpc محصورة في `LISTINGS_STAFF_ALLOWED` — متضفش دالة فلوس/صلاحيات هناك أبدًا.


## 🔌 النشر الأوتوماتيكي لدرافتات الويزارد (٢٥ أغسطس ٢٠٢٦)
- محمد: «الاعلان من ضيف ويزارد مش بيضاف عايز اعرف ليه مش بيتنشر». السبب: دالة `auto_publish_submitted_draft()` كانت موجودة من غير تريجر متركب على `listing_drafts` — اتركب `trg_auto_publish_submitted_draft` (AFTER INSERT OR UPDATE OF status). الدالة بترفع نفسها service جوّه الترانزاكشن قبل `publish_unclaimed_draft` عشان ترجرات النشر على listings ماترفضش لو الإدخال جه من سياق من غير هوية (كرون/ويبهوك).
- **قاعدة**: أي دالة تريجر بتتكتب لازم نتأكد إن التريجر نفسه متركب فعلًا (`select tgname from pg_trigger`) — دي تالت حاجة «مكتوبة بس مش موصّلة» نكتشفها (كرونات pg_cron مرتين + التريجر ده).
