# قرارات معمارية مقفولة — مضمونة

> القرار المقفول ده اتفقنا عليه ونفّذناه. أي شغل جديد يحترمه.
> آخر تحديث: ٢٣ يوليو ٢٠٢٦

---

## 🔒 القرار: توثيق الرقم = الوارد بس (مفيش رسايل باردة)

**السبب:** رقم المارد بيتوقف (يتحظر) من **الرسايل الباردة المتولّدة** — زي
كود الـOTP اللي مضمونة بتبعته للعميل. الرد على رسالة العميل **مش** رسالة باردة،
فمسموح. عشان كده وحّدنا كل التوثيق على **الطريقة العكسية**:

> **العميل هو اللي يبعت** كود `MADxxxxx` لواتساب المارد،
> والرقم يتأكّد من **مصدر الرسالة نفسها** (محدش يقدر يبعت برقم غيره).

### القواعد المقفولة
| القاعدة | الحالة |
|---|---|
| مضمونة **مابتبعتش** أي كود/رسالة متولّدة للعميل | 🔒 مقفول |
| كل توثيق رقم = العميل يبعت `MADxxxxx` للمارد (وارد) | 🔒 مقفول |
| الرد على رسالة العميل (ترحيب/تأكيد) = **مسموح** (مش بارد) | 🔒 مقفول |
| الرقم بيتأكّد من الـ JID/مصدر الرسالة — مش من رقم بيتكتب | 🔒 مقفول |
| الرقم المخفي (LID) يتحلّ من `wa_lid_map`، ولو مفيش تطابق يتوثّق كهوية واتساب | 🔒 مقفول |

### دخول جوجل — إيميل + اسم بس، **والتوثيق بالوارد**
- من جوجل بناخد **الإيميل والاسم فقط** (الإيميل ملوش استخدام عندنا دلوقتي).
- **لازم** يوثّق رقمه بعد الدخول — بنفس طريقة الوارد (يبعت كود للمارد).
- الرقم المُثبت **يترّبط بحساب جوجل الحالي** — مش حساب جديد.
- لو الرقم مربوط بحساب تاني → نقوله يدخل بالواتساب بالرقم ده (مفيش دمج تلقائي).

---

## ✅ اللي اتنفّذ فعلًا (Layer 1 — دخول جوجل بالوارد)

| الملف | التعديل |
|---|---|
| `src/app/api/auth/complete-phone/route.ts` | اتكتب من الأول: يقرا الكود المتأكّد من `wa_inbound_verifications`، يستخرج الرقم المُثبت (يحلّ LID)، يتأكد إنه مش مربوط بحساب تاني، يربطه بحساب جوجل الحالي، يستهلك الكود، يعلّم `wa_verified` في الميتاداتا، ويبعت رد ترحيب. **مفيش OTP بارد.** |
| `src/app/auth/complete-phone/page.tsx` | اتكتبت من الأول: واجهة الوارد (ابعت الكود للمارد) بنفس جذر إصلاح الـpopup اللي في `WhatsAppLogin`. |
| `src/app/auth/callback/page.tsx` | علامة `wa_verified` في الميتاداتا تمنع رجوع صاحب الرقم المخفي لصفحة التوثيق كل مرة (loop). حساب جوجل من غير رقم موثّق → صفحة التوثيق بالوارد. |
| `src/lib/wa-welcome.ts` | **جديد** — مصدر موحّد لرد ترحيب الدخول (اللينك اللي كان رايحه + شات المارد). |
| `src/app/api/auth/wa/route.ts` | اتنضّف: بيستخدم `sendLoginWelcome` من المصدر الموحّد بدل نسخة مكرّرة. |

**اتّحقّق:** `npm run build` نجح (EXIT=0) — كل المسارات اتجمّعت.

---

## ⏳ اللي فاضل (Layer 2 — لسه محتاج شغل في جلسة جديدة)

توثيق إنشاء الإعلان (add-listing) و`ListingForm` لسه على **OTP بارد**. ده مش
متنفّذ لأنه بيلمس مسار الإعلان الحرج (مصدر الدخل) ومحتاج اختبار لايف كامل.

### الحقائق المتأكّد منها (من قاعدة البيانات — ماتتخمّنش تاني)
- `whatsapp_outbound_queue` **مش مهجور** — بيقراه `process_whatsapp_outbound_queue()`
  وعليه ترايجرات سياسة (`trg_enforce_whatsapp_policy` وغيرها). **ممنوع حذفه** —
  ٢٠+ فنكشن بتستخدمه.
- بعض أكواد الـOTP الباردة **فعلًا بتتبعت** (status = `sent`) — خطر حظر حقيقي.
  آخر ١٤ يوم: ٩ محاولات OTP.
- ترايجر النشر `enforce_listing_publish_requirements()` بيرفض نشر أي إعلان
  (مش directory ومش مورّد داخلي) من غير `phone_verified_at` **و** `contact_phone`.
- `verify_phone_otp(p_phone, p_code, p_listing_id)` هو اللي بيسِت
  `listings.phone_verified_at + contact_phone` — بيقرا من `phone_verifications` (البارد).
- `profiles.phone` مخزّن بصيغ مختلطة: الغالبية `+20…` (١٩٩)، وشوية `01…` (٢٠)
  و`20…` (٢٣). الـcallback بيتأكد من `^\+20\d{10}$`. **خزّن `+20…`.**

### الخطة المقترحة للـLayer 2
1. RPC جديد `verify_phone_inbound(p_code, p_listing_id)` — نفس شغل
   `verify_phone_otp` بس مصدره `wa_inbound_verifications`.
2. كمبوننت موحّد `WhatsAppVerify` (نفس جذر الـpopup) — يرجّع الرقم المُثبت.
3. تحويل `AddListingClient.tsx` (سطر ~٣٦٣٨/٣٦٦٥) و`ListingForm.tsx` (~٦٦٤) للوارد.
4. بعد ما مفيش كود بيستخدم `/api/otp/send` و`/api/otp/verify` → stub بيرفض ويوجّه
   للوارد. **من غير حذف** الطابور المشترك.

---

## 🔗 مراجع
- مسار الوارد الأساسي: `src/components/WhatsAppLogin.tsx` + `src/app/api/auth/wa/route.ts`
- تأكيد الكود (ويبهوك): `src/app/api/whatsapp/baileys/route.ts` (بيسِت `verified_phone` = رقم المُرسِل)
- ضد الحظر: `MARID_REPLY_ONLY=1` في `src/lib/whatsapp.ts`
