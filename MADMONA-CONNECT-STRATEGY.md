# استراتيجية الربط والتحويل — شات مضمونة (Project Target)

> ٢١ يوليو ٢٠٢٦ · إحنا بنبني مشروع مش تاسكات. ده تحليل شامل للربط بين
> المستخدم/المورد/الطلبات/الاستفسارات، وإزاي نحوّل الناس على شات مضمونة.

## 1) الفكرة المحورية: الرقم = الهوية الموحّدة

كل كيان في الداتا مربوط بـ **`contact_phone`**:
- `profiles.phone` (الأكونت) · `marketplace_orders.guest_phone`/customer_id
- `whatsapp_conversations.contact_phone` (واتساب **و** الويب — session_id='web')
- `sales_leads.contact_phone` · `customer_demand_requests.contact_phone`
- `instant_listing_drafts.contact_phone`

**النتيجة:** أي شخص = رقم واحد بيجمع: محادثاته (واتساب+ويب) + ليدز + طلباته +
استفساراته + مسوّداته + أكونته. ده أساس **"عرض العميل 360°"**.

## 2) خريطة الربط بين الكيانات

```
profiles (customer|supplier|admin)
   └─ marketplace_suppliers (profile_id) ── listings (supplier_id) ── project_id (بورصة)
        └─ marketplace_orders (supplier_id, customer_id/guest_phone, primary_listing_id)
             └─ marketplace_order_items (order_id, listing_id)
   └─ whatsapp_conversations (contact_phone, session_id) ── whatsapp_messages
        ├─ sales_leads (تقييم النية)
        ├─ customer_demand_requests (طلب مش موجود → matched_supplier_id)
        └─ instant_listing_drafts (مسوّدة مورد → published_listing_id)
```

**الرابط الذهبي:** الشات (واتساب+ويب) هو نقطة الدخول اللي بتولّد ليد/طلب/مسوّدة،
والمارد بيحوّلها لأوردر أو إعلان، ويربط العميل بالمورد (جروب/تحويل).

## 3) القمع (Funnel): إزاي نحوّل الناس على شات مضمونة

كل نقطة احتكاك → استدعاء واضح للشات:
- **الهوم/التواصل** → كارت "رد فوري" (اتعمل ✅) + زر عائم للشات.
- **صفحة الإعلان (listing)** → "اسأل عن ده" يفتح الشات ومعاه سياق الإعلان (deep-link `/chat?listing=<id>`).
- **الأوردر/الحجز** → "محتاج مساعدة في الأوردر ده؟" → شات بسياق الأوردر.
- **استفسار مش لاقي** → الشات يسجّل `customer_demand_requests` تلقائي (المارد بيعمل ده).
- **المورد** → الشات يجهّز مسوّدة إعلان (`instant_listing_drafts`) ويحوّلها للبورصة/الماركت.
- **بعد أي محادثة** → إنشاء أكونت تلقائي بالرقم (اتعمل ✅) → إعادة استهداف بالإشعارات.

**القياس:** كل محادثة ليها `session_id` و`source`؛ نقيس تحويل: محادثة → ليد → أوردر.

## 4) استراتيجية القنوات: الشات المملوك + الواتساب (مش بديل — تكامل)

- **الشات المملوك = الأساس الموثوق** (مفيش بان، تسليم مضمون، هوية أكونت).
- **الواتساب = نقطة التقاط ثانوية**: العميل اللي بيكلّمنا على واتساب **بيتسجّل في الداتابيز**
  (`whatsapp_conversations`)، فلازم نسيب قناة واتساب ظاهرة **للالتقاط** —
  حتى لو الرد الآلي مش بيتسلّم (بسبب الحجب)، الأدمن يقدر يتابع من لوحة `wa-review`.
- **التوحيد:** الاتنين بيصبّوا في نفس الجدول بنفس الرقم → المارد يشوف تاريخ العميل كامل
  مهما كانت القناة. (ده متبني أصلاً — session_id بيفرّق القناة بس.)
- **القرار:** نعرض **الاتنين** (شات مباشر + واتساب) بس الشات هو الـ CTA الأساسي (primary)،
  والواتساب ثانوي (secondary) مع لوحة متابعة للأدمن.

## 5) تزبيط الشات باحترافية (قبل كل ده — الأولوية القصوى)

معايير "احترافي جدًا":
1. **هوية سلسة** — دخول تلقائي بالأكونت (✅)، وإنشاء أكونت للجديد (✅).
2. **استمرارية** — تحميل تاريخ المحادثة عند الفتح (دلوقتي بتبدأ فاضية — **لازم نحمّل الرسايل السابقة**).
3. **إشعارات** — لما ييجي رد والعميل بره (web push — البنية موجودة `push-subscription`).
4. **حالة التسليم/القراءة** — علامات (✓✓) في الشات المملوك (سهل، احنا مالكين القناة).
5. **سياق** — deep-link من الإعلان/الأوردر يفتح الشات عارف الموضوع.
6. **شكل ومايكرو-تفاعلات** — typing، تمرير سلس، ستيت فاضي مهذّب، سرعة.
7. **ميديا كاملة** (✅ صور/صوت/ملفات) + ضغط (✅).
8. **قابل للتثبيت كأيقونة** (PWA — تحت).

## 6) أيقونة الشات على التليفون (PWA install)

التطبيق **PWA بالفعل** (manifest + service worker + أيقونات 192/512). خطتنا:
- إضافة **shortcut** للشات في الـ manifest (اسمه "شات مضمونة" → `/chat`) → يظهر كاختصار.
- زر **"ثبّت الشات على موبايلك"** داخل `/chat` يستخدم `beforeinstallprompt` (أندرويد/كروم)
  ويعرض تعليمات "Add to Home Screen" على iOS.
- بديل أنضف طويل المدى: manifest مستقل بـ `start_url=/chat` لو عايزينه أيقونة منفصلة تمامًا.

## 7) خارطة التنفيذ بالأولوية

**المرحلة أ — احترافية الشات (قبل كل حاجة):**
1. تحميل تاريخ المحادثة عند الفتح (استمرارية).
2. إشعارات web push للردود.
3. زر تثبيت PWA + shortcut في الmanifest.
4. علامات تسليم/قراءة + تحسينات شكل.

**المرحلة ب — الربط والتحويل:**
5. deep-links: "اسأل عن ده" من الإعلان/الأوردر → شات بسياق.
6. عرض الشات (primary) + واتساب (secondary) في كل نقاط الاحتكاك + زر عائم.
7. لوحة أدمن موحّدة: عرض 360° للعميل بالرقم (محادثات+ليدز+أوردرات+طلبات).

**المرحلة ج — العمق:**
8. قياس القمع (محادثة→ليد→أوردر) + إعادة الاستهداف بالإشعارات.
9. ربط طلبات الاستفسار (`customer_demand_requests`) بالموردين تلقائيًا (matched_supplier_id).
