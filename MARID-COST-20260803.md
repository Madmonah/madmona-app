# تكلفة المارد — شغل ٣ أغسطس ٢٠٢٦

## الخلاصة

| | الرسالة | الشهر (بمتوسط ٣٥ رد/يوم) |
|---|---|---|
| قبل | ~$0.095 ≈ 4.9 ج | ~$100 ≈ 5,200 ج |
| بعد المتعمل | ~$0.088 | ~4,600 ج |
| + شيل `who_is_this` (لسه) | ~$0.069 | ~3,800 ج |
| + تقصير البرومبت (لسه · ⚠️ خطر جودة) | ~$0.055 | ~3,000 ج |

الموديل: `claude-sonnet-4-5-20250929` · $3 / $3.75 (كتابة 5د) / $6 (كتابة 1س) / $0.30 (قراءة) / $15 لكل مليون توكن.
سعر الصرف وقت الحساب: 51.93 ج/$.

## الأرقام اللي القياس اتبنى عليها

- البادئة المكاشة ≈ **22,000 توكن** = `CUSTOMER_CONCIERGE_PROMPT` (38 KB) + `MARID_TOOLS` (23 أداة / 402 سطر)
- سياق المحادثة: 24 رسالة ≈ 1,000 توكن
- **الرسالة الواحدة = ٢-٤ نداءات** (`MAX_TURNS` = 4، و6 للأدمن)، لأن البرومبت بيفرض `who_is_this` أول حاجة
- توزيع التكلفة: مدخلات غير مكاشة ~42٪ · cache write ~31٪ · cache reads ~19٪ · مخرجات ~8٪

## اللي اتعمل

### الداتابيز (مطبّق فعلاً)
- `ai_usage_log` — توكنز حقيقية لكل نداء + `latency_ms` + رقم اللفّة
- `ai_model_pricing` — الأسعار في صفوف مش في كود
- `v_ai_usage_cost` — تكلفة كل نداء
- `v_ai_cost_per_message` — ملخص يومي: تكلفة الرسالة + عدد النداءات للرسالة
- قفل ٢٠ جدول `_backup_*` / `_cron_*` كانوا مكشوفين لـ anon (RLS + REVOKE)

### الكود (محتاج deploy)
باك أب كل ملف موجود بامتداد `.bak_costwork_20260803`

| الملف | التعديل |
|---|---|
| `src/lib/ai-usage.ts` | **جديد** — `logAiUsage` fire-and-forget، مايقدرش يكسر الرد |
| `src/lib/marid-brain.ts` | `ttl: '1h'` + لوج كل لفّة + **إصلاح باج `[object Object]`** |
| `src/lib/anthropic.ts` | لوج في `callClaude` — نقطة العبور الوحيدة لكل الوكلاء الخلفيين |
| `src/app/api/whatsapp/baileys/route.ts` | تمرير `channel` و `conversationId` |

### باج `[object Object]` (الأهم)
النداء الأخير في `callMaridWithTools` كان `system: \`${system}\`` و`system` مصفوفة كائنات —
فكان بيروح لكلود كنص `"[object Object]"`. يعني لما المارد يستهلك كل اللفّات كان بيرد
**من غير برومبت خالص**: لا شخصية، لا روابط، ولا قاعدة «ماتخترعش سعر». بيحصل تحديدًا في
المحادثات الطويلة — أكتر وقت العميل محتاج فيه رد مضبوط.

## بعد الـ deploy

```sql
select * from v_ai_cost_per_message order by day desc;
```

`channel = 'whatsapp'` → المارد · `channel = 'backend'` → الوكلاء الخلفيين.

## قرارات مقصودة (ماتعملتش)

- **تقصير البرومبت** — الـ 38 KB مش حشو، دي قواعد اتكتبت من أخطاء حقيقية (مثال: منع
  الاعتذار عن التأخير، كان طالع في ١٩٪ من الردود). أي قص لازم يتقاس قبل/بعد.
- **نقل قسم «عندك أدوات» جوه الكاش** — بيوفّر ~10٪ بس بيغيّر **ترتيب** التعليمات.
- **تقليل الـ history لـ ١٤** — مرفوض. الكومنت في الكود بيقول ١٢ كانت بتقطع السياق،
  وعشان كده اترفعت لـ ٢٤. التوفير ٢٪ والخسارة أكبر.

## تصحيحات حصلت أثناء الشغل

- `orchestrator_job_runs` = 69,339 صف **مش** نداءات Claude — أغلبها سباكة
  (`_selftest_ping` لوحده 1,247/يوم).
- الكاش 1-hour **مش** بيوفّر 35-40٪ زي ما اتقال في الأول — الحسبة الدقيقة بتطلع
  قريبة من التعادل عند الحجم الحالي، وبتكسب لما الرسايل تزيد.

## متبقي

1. **deploy**
2. بعد يومين: اقرا الأرقام الحقيقية من `v_ai_cost_per_message`
3. الوكلاء الخلفيين: كل دوال الداتابيز السبعة اللي بتنادي Anthropic **من غير كاش خالص**
   (`cache_control` = false). لو طلعوا أغلى من المارد، إضافة الكاش هناك أكبر مكسب متبقي.
4. Batch API للشغل غير اللحظي = خصم ٥٠٪
