# جرد قدرات المارد — إيه اللي ضاع في الترحيل

> ٢٠ يوليو ٢٠٢٦ · مقارنة النظام القديم (Cloud API / Deno) بالجديد (Baileys / Next)
>
> **ليه الملف ده موجود:** اكتشفنا ٣ قدرات ضايعة بالصدفة أثناء تصليح حاجات
> تانية. الجرد المنهجي طلّع **٢٧**. أي شغل جديد قبل ما نقفل دول = بناء
> فوق نظام ناقص.

---

## 🔴 حرِج — بيسبب ضرر دلوقتي

### ١. المحادثات الموقوفة بيترد عليها
القديم: `whatsapp-webhook:1350, 1416` بيقرا `status` ولو `paused` بيسكت.
الجديد: **مابيقراش `status` خالص.** أي محادثة الأدمن أوقفها، المارد
هيرجع يرد فيها.

### ٢. مفيش حارس لوب
القديم: `:1543-1584` — لو ١٢ رسالة صادرة في ساعة، أو تكرار ردود، أو
تكرار وارد → يوقف المحادثة تلقائيًا وينبّه الأدمن.
الجديد: مفيش. الرقم معرّض إنه يتقفل.

### ٣. الميديا بتتحذف
القديم: `:401-474` — الصور تروح `content-images`، والـPDF والفيديو
`project-media`، وبيتسجّل `metadata.image_url` وغيره.
الجديد: بيبعت base64 لـClaude **وبيرميه**.
→ صفر ملفات محفوظة، والمسودات من غير صور.
→ **ده سبب فشل النشر في الماركتبليس** (الداتابيز بترفض إعلان من غير صورة).

### ٤. اللينكات الممغنطة راحت
القديم: `:295-323` — كل لينك بيتغلّف بـ`wa_login_tokens` → `/l/<token>`
يدخّل العميل تلقائي.
الجديد: لينكات عادية.
→ رجعنا لمشكلة «الأوردرات بتتلغي لأن الناس مش مسجّلة دخول».

### ٥. فلو المطاعم كامل
القديم: `:572-873` — قبول/تعديل/رفض الأوردر، تعديل الأسعار سطر-سطر
وإعادة حساب الإجمالي، إلغاء العميل. بيمس `marketplace_orders` و
`marketplace_order_items` و`whatsapp_outbound_queue`.
الجديد: **مفيش.** ده فلو فلوس.

### ٦. حجز وإلغاء المواعيد
القديم: `:927-945`, `:1622-1643` — `book_meeting` / `cancel_meeting` / `my_meeting`.
الجديد: مفيش → المارد هيرجع **يخترع مواعيد**.

---

## 🟡 مهم — بنخسر شغل وفلوس

| # | القدرة | القديم | الأثر |
|---|---|---|---|
| ٧ | تنبيه الليد السخن | `:1107-1155` | رقم كلّمناه قبل كده يرد → إشعار فوري. ضاع |
| ٨ | `unmet_demand` | `:1084-1105` | طلب مش في الكتالوج → `customer_demand_requests` + تنبيه. الجديد بيقول «مش متاح» وخلاص |
| ٩ | `sales_leads` + lead scoring | `:1740-1751` | ٨٠ للمورد، ٥٠ للعميل. **مفيش تقييم ليدز خالص** |
| ١٠ | قناة الأدمن | `:1219-1225, 1421-1459` | أوامرك بتتسجّل في `admin_directives` وتتحوّل. الجديد مش عارفك |
| ١١ | «شير واكسب» | `:1246-1264, 1385-1404` | كود الإحالة وإثبات الشير |
| ١٢ | تنبيه اهتمام ERP | `:1714-1732` | مورد مهتم بالـCRM → تنبيه |
| ١٣ | تسجيل المتقدمين للوظايف | `:1157-1170` | `job_applications` |
| ١٤ | رد صوتي (TTS) | `:325-382` | Groq عربي. `/send-voice` جاهز ومحدش بينده عليه |
| ١٥ | إعلانات ميتا (CTWA) | `:1210-1217` | `ad_id` / `ctwa_clid` على المحادثة |
| ١٦ | حالة التسليم | `:1761-1797` | delivered / read / failed |
| ١٧ | `reply-sweeper` كشبكة أمان | `sweeper:62-72` | وارد من غير رد >١٠ دقايق → اعتذار تلقائي |
| ١٨ | إعدادات من الداتابيز | `whatsapp_config` | العمولة، العنوان، رقم التنبيه. الجديد env ثابت |
| ١٩ | مذاكرة البورصة في البرومبت | `:88-137` | أسعار وخطط سداد ولينكات، كاش ١٠ دقايق |
| ٢٠ | `salvageReply` | `:1029-1052` | إنقاذ الرد لو الـJSON اتقطع. الجديد بيسكت |
| ٢١ | فلتر الضوضاء | `:67-72` | نقطة أو إيموچي لوحده مايستاهلش رد |
| ٢٢ | `media-ack` | `:875-913` | رد مهذب على ميديا مش مفهومة + throttle |
| ٢٣ | تصحيح اسم البراند والمسارات | `:190-206` | `/categories/x` → `/marketplace?category=x` |
| ٢٤ | استقبال المزيكا من الأدمن | `:503-570` | رفع MP3 + أمر «ركّب تراك» |
| ٢٥ | `wa_reply_claims` | `:1527-1530` | قفل توزيعي بين الويبهوك والـsweeper |

---

## 🔻 اتدهورت (شغّالة بس أضعف)

- **الرد الواحد للدفعة:** القديم كان **بيستنى ٨ ثواني ويجمّع لحد ١٢ رسالة
  ويرد عليهم كلهم**. الجديد بيقارن الوقت وبيرمي الرسالة المتخطّاة —
  **مابتدخلش في الرد أصلاً**.
- **التاريخ:** القديم ٢٤ رسالة بتمييز «مضمونة(AI)» عن البشري. الجديد ١٢ من غير تمييز.
- **الجروبات:** القديم بيعمل محادثة منفصلة `group:<id>`. الجديد بيسجّل الجروب
  على محادثة المشارك نفسه → **خلط سياق الخاص بالجروب**.
- **الصور:** القديم `imageRule` مفصّل بيطلب وصف بصري محدد. الجديد سطر واحد
  «شوفها ورد عليه» → ده سبب ضياع قراءة أكواد الوحدات.
- **المسودات:** القديم لحد ٨ منتجات من رسالة واحدة + صور. الجديد واحد بس بدون صور.
  ⚠️ وكمان `status:'pending'` بدل `'new'` — **اتأكد الأدمن بيفلتر على أنهي قيمة**.
- **تصنيف المحادثة:** القديم بيحدّث `contact_type`/`first_intent`/`first_category`.
  الجديد بيرجّعهم في الـresponse ومابيحفظهمش.

---

## ✅ اللي كسبناه في الجديد

أدوات حقيقية بدل حشو البرومبت · `who_is_this` بيقرا الملف الشخصي والمورد ·
جسر الـLID · جروبات الموردين والتحويل عليها · `create_project` بضوابط ·
منع التكرار بـ`wa_message_id` · أنواع رسايل أوسع · `/send-voice` و`/group-*`

---

## الترتيب المقترح

**الأول (ضرر شغّال):** ١ الموقوفة · ٢ حارس اللوب · ٣ حفظ الميديا · ٤ اللينكات الممغنطة
**بعدين (فلوس):** ٥ المطاعم · ٦ المواعيد · ٧ الليد السخن · ٨ unmet_demand · ٩ lead scoring
**بعدين:** الباقي

**قاعدة:** قبل أي قدرة جديدة، بص هنا الأول.
