# حالة المارد — ٢٠ يوليو ٢٠٢٦

> ده الملف اللي تبصّ فيه الأول لما تيجي تشتغل على المارد.

---

## الوضع دلوقتي

**شغّال ومتصل** على Railway بـ Baileys، الرقم `201002229982`،
والرقم لسه شغال على الموبايل عادي.

```bash
curl https://madmona-app-production.up.railway.app/health
```

بيرجّع `connected` و`version.commit` — **اتأكد من الكوميت قبل ما تقول
«اتصلح»**. النسخة اللي فيها الإصلاح لازم تكون هي الشغالة فعلاً.

---

## الأدوات اللي عنده

| الأداة | بتعمل إيه |
|---|---|
| `search_catalog` | بحث في ٣٤٤ إعلان حقيقي |
| `list_categories` | ٤٠٧ تصنيف |
| `who_is_this` | مورد؟ عميل؟ جديد؟ + تاريخه |
| `get_my_orders` | حجوزات العميل |
| `create_listing_draft` | إعلان جديد → `instant_listing_drafts` |
| `create_project` | مشروع عقاري → بورصة مضمونة |
| `create_supplier_group` | جروب متابعة للمورد |
| `forward_to_supplier` | تحويل طلب لجروب المورد |
| `search_projects` | بحث في ١١٤ مشروع — للمقارنة وسياق السوق |
| `manage_order` | قبول/رفض/إلغاء أوردر (بتحقق صلاحية) |
| `manage_meeting` | حجز/إلغاء ميعاد |
| `record_unmet_demand` | طلب مش عندنا → تنبيه |
| `get_referral_code` | «شير واكسب» |
| `record_job_application` | طلبات التوظيف |

وتلقائي من غير أدوات: تسجيل الليد وتقييمه (٨٠ مورد / ٥٠ عميل)،
وتنبيه الليد السخن.

---

## الحمايات

**المحادثة الموقوفة** — لو `status = paused` أو `blocked`، المارد بيسكت.
**حارس اللوب** — أكتر من ١٢ رسالة في ساعة → إيقاف تلقائي + تنبيه.
**منع التكرار** — `wa_message_id` بيتفحص قبل أي معالجة.
**الرد الواحد للدفعة** — بيسجّل، يستنى ٧ث، وبعدين يقارن بوقت
رسالته هو. آخر واحد بس هو اللي يرد.
**تنسيق واتساب** — `**عريض**` بتتحوّل لـ`*عريض*` قبل الإرسال.
**الميديا التالفة** — بيشيلها ويكمّل بالنص بدل ما يقع.
**إنقاذ الرد** — لو الـJSON اتقطع بيستخرج النص الخام.
**الرسايل الفايتة** — بيستقبل `append` كمان (الرسايل اللي وصلت والخدمة مقطوعة).
**تنبيه الانقطاع** — بعد دقيقتين تنبيه، ولما يرجع تنبيه تاني.

---

## القواعد اللي اتعلمناها (بالتجربة مش بالتنظير)

**١. الرد يروح على نفس الـJID اللي جت منه الرسالة.**
واتساب بيبعت مُعرّف مخفي بدل الرقم. لو أعدت تركيب رقم منه، بتبعت
لرقم مش موجود، وBaileys بيقبله ويدّيك `wa_message_id` والرسالة تضيع.

**٢. `wa_message_id` موجود ≠ الرسالة وصلت.**
أخطر إشارة كاذبة قابلناها.

**٣. اقرا الأعمدة والـenums الفعلية قبل ما تكتب.**
النهارده لوحده: `cover_url` مش موجود في `listings`، و`city` مش موجود
في `customer_demand_requests`، و`rejected` مش قيمة في `mp_order_status`،
و`title` اسمها `name_snapshot`. كل واحدة منهم كانت هتوقع الكود.

**٤. `GRANT` على مستوى الجدول بيتغلّب على `REVOKE` على مستوى العمود.**
لازم `revoke select on table` الأول وبعدين `grant select (cols)`.
غير كده هيقول Success وهو مش شغال.

**٥. الميديا لازم تتحفظ قبل ما تتبعت للتحليل.**
كنا بنبعتها لـClaude وبنرميها — والداتابيز بترفض إعلان من غير صورة.

**٦. أي تعديل على مسار الرد يتجرّب برسالة حقيقية بعد النشر.**
النهارده كسرت المارد مرتين وأنا بحسّنه، والاتنين اتكشفوا بالاختبار.

**٧. الحارس اللي بيمنع التكرار غير الحارس اللي بيمنع الرد.**
أول نسخة من حارس التكرار منعت رد المارد على عميل رد بعد ٣٥ ثانية.

**٨. الرسايل اللي بتوصل مع بعض بتفتح عمليات متوازية.**
Yuri Sushi بعت ٤ رسايل في ثانية → ٤ عمليات → ٤ ردود في ٢١ث،
وأولهم قال «أقدر أساعدك في إيه» والمنيو كان واصل قبله بنص ثانية.
مقارنة الأوقات لوحدها ماتحلّهاش — الفحص بيحصل قبل ما الإخوات
يتسجّلوا. الترتيب الوحيد اللي بيشتغل: **سجّل → استنى → قارن
بوقت رسالتك إنت**. ولو قارنت بنافذة زمنية، كلهم هيسكتوا.

**٩. الناس بتبلّغ عن الأرقام اللي بتحس إنها بوت.**
«أنا مساعد ذكي» · «تم استلام طلبك» · «كيف يمكنني مساعدتك» —
كلها ممنوعة. والرد العام اللي مالوش علاقة باللي اتبعت أخطر
علامة. الرد لازم يذكر تفاصيل من كلام العميل بالاسم والرقم.

**١٠. ماتقولش «سجّل من اللينك» — قول «هعملهالك».**
`create_listing_draft` → `instant-claim-builder` بيبني الصفحة
فعلاً ويبعتله لينك يستلمها بيه. الأداة كانت موجودة من زمان بس
المارد ماكانش بيعرض الخدمة دي على حد.

---

## 👑 قناة الأدمن

المارد بيفرّق بين محمد وأي حد تاني. الأرقام في `ADMIN_PHONES`.

| الأمر | مثال |
|---|---|
| `admin_stats` | «إيه الأخبار» · «عاملين إيه الأسبوع ده» |
| `admin_review_queue` | «فيه إيه مستني مراجعة» |
| `admin_conversation` | «وقّف المحادثة مع فلان» · «شوفلي محادثة الرقم ده» |
| `admin_publish` | «انشر مشروع كذا» · «ارفض المسودة الفلانية» |
| `admin_broadcast` | «ابعت للمطوّرين إن...» |

**كل أمر بيتسجّل في `admin_directives` قبل أي معالجة** — حتى لو
المارد فهم غلط أو التنفيذ وقع، الأمر محفوظ ومحمد يقدر يراجع.

⛔ **البث** بيعرض النص والعدد ويستنى موافقة صريحة. `dry_run` افتراضي.
⛔ أدوات الأدمن **مش موجودة أصلاً** في قايمة أدوات غير الأدمن.
✅ حارس اللوب مستثنى للأدمن.

---

## شخصية المارد

**خبرة ١٠٠ سنة بطاقة شاب في العشرين.**

الخبرة = ماتسألش عن حاجة تقدر تعرفها. دوّر الأول واسأل بس عن
اللي مستحيل تعرفه (الميزانية، التفضيل، الميعاد).

لما حد يبعت مشروع: **ذاكره وقارنه باللي عندنا**. «تمام استلمت»
رد موظف — الرد اللي بيفرق هو اللي بيوري إنك فاهم السوق.

المقارنة بمشاريع تانية **من الأدوات بس** — ممنوع من الذاكرة،
وممنوع نقلل من حد.

**لينك بورصة مضمونة العقارية إجباري** لكل مطوّر وكل حد بيسأل عن عقار.

---

## الجروبات

لكل مورد جروب: هو + `01004194133` + `01104496225`.
**كل الأعضاء أدمن** عشان المورد يضيف فريقه بنفسه.

```bash
node scripts/create-supplier-groups.mjs                    # معاينة
node scripts/create-supplier-groups.mjs --send --limit 5   # عقارات
node scripts/create-supplier-groups.mjs --all --send --limit 5  # كل التصنيفات
node scripts/fix-group-names.mjs --send                    # تصليح الأسماء الوهمية
```

🚨 **واتساب بيرجّع `rate-overlimit` لو أسرعنا.** حصل في ٢٠ يوليو بعد
~٤٥ جروب في ساعتين على فاصل ٦٠–١٥٠ث. الفاصل بقى ٣–٦ دقايق،
والسكريبت بيوقف لوحده أول تحذير. **ماتزوّدش الحد** — الرقم أغلى
من الجروبات الباقية.

⚠️ **الأسماء الوهمية.** التسجيل التلقائي بيحط «موردة جديدة» و«حساب
1060138703»، والاسم ده بيتحوّل لعنوان جروب بيشوفه المورّد. ١٢ جروب
اتعملوا كده. اتصلحوا بأسماء حقيقية من **اسم الواتساب** ومن **عنوان
إعلاناتهم** لما اسم الواتساب مالوش لازمة («.» · «None» · حروف مزخرفة)،
واتحدّث `business_name` كمان مش الجروب بس. السكريبت بيتخطّاهم دلوقتي.

🖼️ **اللوجو.** الداتابيز فيها `logo_url` لمورّد واحد من ٥٠ —
فالمصدر الحقيقي هو **صورة البروفايل بتاعت الرقم على الواتساب**
(`/profile-picture` → `profilePictureUrl`). لقينا ٣٤ من ٤٩.
بتتحفظ في `logo_url` كمان عشان الماركتبليس يستفيد ومانرجعش ندوّر.
`/group-picture` بياخد بافر مش لينك — لازم نجيب الصورة الأول.

⚠️ أول رسالة **لازم** تشرح إحنا مين وليه ضفناه وإزاي يخرج.
الإضافة من غير شرح بتتقري كسبام وبتوقف الرقم.

---

## 🚨 البلوك — ٢٠ يوليو ٢٠٢٦

واتساب حط بلوك على `201002229982` — **بدء محادثات جديدة بس**،
والرد شغّال عادي.

**السبب:** بدء محادثات جديدة كتير في وقت قصير (من التطبيق مباشرة).

**الحارس:** `MARID_REPLY_ONLY=1` في `sendText` — أي رسالة لحد
ماكلّمناش قبل كده بتترفض قبل ما توصل واتساب. الرد على اللي
بيكلّمنا مالوش قيد.

مكانه في نقطة الإرسال نفسها، فبيغطّي كل مسارات المشروع مش
الويبهوك بس. ⚠️ بس **مابيغطّيش** الرسايل اللي بتتبعت من
التطبيق على الموبايل يدوي — دي بره النظام.

**متشال؟** لأ. ده مش إجراء مؤقت — الرقم بيتوقف من الرسايل
الباردة مش من الردود، فالحارس يفضل شغّال دايمًا.

**درس:** `rate-overlimit` من واتساب مش تحذير عادي — هو آخر
إنذار. لما ظهر أثناء عمل الجروبات كان لازم الوقوف تمامًا
والرجوع لمحمد، مش الاستمرار بحد أقل.

## 📞 رقمين للمارد

الخدمة بتدعم أكتر من رقم: `SESSIONS=رقم:اسم,رقم:اسم`،
و`POST /sessions` لإضافة رقم، و`GET /qr?session=الرقم` للربط.

`whatsapp_conversations.session_id` بيربط كل محادثة بالرقم اللي
جت عليه. **الرد بيخرج من نفس الرقم دايمًا** — الرد من رقم غير
اللي العميل كلّمه بيوصله كرسالة من مجهول، وده نفس نمط البدء
البارد اللي بيوقّف الأرقام.

## 🍽️ المطاعم

**اللي كان موجود ومحدش شايفه:** ٦٢ مطعم · ١٧١٧ صنف منيو ·
صفحات المنيو والطلبات والحجوزات والتقييمات والفريق والحسابات
كلها مبنية. تاب «حسابي» كان بيعرض لينك واحد يتيم على الماركتبليس.

دلوقتي كل التابات مفتوحة من «حسابي»، وصاحب المطعم بيلاقي لينك
مباشر على المنيو — بنعرفه من وجود أصناف على إعلاناته
(`restaurant_menu_items.listing_id`).

**المارد بيقول لأي مطعم أربع حاجات + لينك:**
عندنا نظام طلبات شغّال · بنرشّح المطاعم المسجّلة · هتاخد لوحة
تحكم من تاب حسابي · ونربط نظامك بنظامنا.

⚠️ التعليمات لوحدها ماكفتش — أول تجربة اتكلم عن المنيو وبس ونسي
التلاتة الباقيين. **المثال الصريح في البرومبت هو اللي ثبّت الشكل.**

## اللي لسه ناقص

من `MARID-LOST-CAPABILITIES.md` (٢٧ قدرة ضاعت، اتصلح منهم ١٦):

**متبقي:** إثبات الشير للإحالات ·
تنبيه اهتمام ERP · الرد الصوتي (TTS) · حالة التسليم
(delivered/read) · `reply-sweeper` كشبكة أمان · إعدادات من
`whatsapp_config` بدل env · استقبال المزيكا من الأدمن ·
تصحيح المسارات الغلط · فلتر الضوضاء · `media-ack`.

**متعلّق:** رواق (يوسف) مستني رد على «هتحتاج انظمه السداد»،
واتوعد بجروب. وعبده مستني رد على صيدلية ١٥٤م المعروضة للبيع.


---

## 🔄 تحديث ٢٢ يوليو ٢٠٢٦ — هوية الجهاز + إصلاح الجلسة الميتة

**المشكلة:** رقم المارد `201002229982` كان بيبان متصل بس مش بيوصّل (shadowban)، والرقمين
الاتنين نفس الحكاية. اتلاحظ إن الجلسة كانت تقف `connected:false + waiting_for_qr:false`
(اتعلّقت في حالة logged-out ومش بتطلّع QR).

**اتعمل (كوميتات على main → Railway):**
- `e0cc06a` — غيّرنا هوية العميل في `wa-service/src/sessions.js`:
  `browser: ['Madmona','Chrome','1.0.0']` → `['Mac OS','Desktop','10.15.7']`
  عشان يبان كـ WhatsApp Desktop رسمي بدل اسم مخصص بيتفلّج.
- `10387c6` — إصلاح جذري: لما الجلسة تتعمل logged-out، دلوقتي **بتمسح نفسها
  (`logoutSession`) وتعيد التشغيل تلقائيًا** فتطلّع QR جديد لوحدها، بدل ما تقف
  مستنية مسح يدوي للمجلد. (كانت بتعمل `return` وتسكت.)

**النتيجة:** بعد الديبلوي، الجلسة داوت نفسها ورجعت `waiting_for_qr:true` وطلّعت QR جديد
بالهوية الجديدة على `/qr?session=201002229982`. متبقّي: **مسح الـQR من موبايل الرقم**
(الأجهزة المرتبطة) — دي الخطوة الوحيدة اليدوية.

**تنبيه مهم (صراحة):** تغيير الهوية بيشيل علامة كشف واحدة بس الـshadowban غالبًا سلوكي
(كمية/بلاغات). لو فضل مش بيوصّل بعد الربط، الحل الجذري = WhatsApp Business Cloud API
الرسمي، أو أرقام نظيفة تتسخّن بالتدريج، أو الاعتماد على الشات المملوك.

**ملاحظة جانبية:** `src/app/api/chat/route.ts` كان اتمسح محليًا (0 بايت) — اترجّع من git.

---

## 🖥️ تحديث ٢٢ يوليو ٢٠٢٦ — مراقبة المارد لايف + التدخّل اليدوي

**الفكرة:** شاشة زي واتساب بتوري محادثات المارد لايف، ومحمد يقدر يمسك غلطة ويصلّحها فورًا.

**اتعمل:**
- `/admin/marid-monitor` — قايمة المحادثات بشكل واتساب، بتتحدّث كل ١٠ث، بتبرز
  اللي «مستني رد» (خلفية صفرا + وسم + عدّاد في الهيدر)، وبتفرّق بين رد المارد 🧞
  والرد اليدوي 👤. الإيقاف/التشغيل للمارد على كل محادثة موجود.
- **شريط تدخّل يدوي** جوه المحادثة: محمد يكتب التصحيح ويبعت (Enter يبعت)،
  وخانة «وقّف المارد بعد ما أبعت» (مفعّلة افتراضيًا) عشان مايردّش فوق التصحيح.
- endpoint جديد `POST /api/admin/marid-monitor/send` — بيمرّ من **نفس نقطة
  الإرسال الموحّدة** (`sendText`)، فبيحترم: الرد يخرج من نفس الرقم (session_id)،
  وحلّ الـLID للـJID المحفوظ، والتسجيل التلقائي كـ«رد يدوي» (`ai_generated=false`)،
  وحماية اسم البراند والروابط. محمي بكوكي الأدمن.
- اللينك اتربط في ناف الأدمن (مجموعة «الليدز والتواصل») عشان الشاشة تبقى واضحة.

⚠️ محادثات الويب لسه الرد اليدوي منها متوقف (الإرسال لواتساب بس دلوقتي).

---

## 🔢 تحديث ٢٢ يوليو ٢٠٢٦ — سياق/إعداد مستقل لكل رقم (Task B)

**الفكرة:** فيه أكتر من رقم مربوط بالمارد (`201002229982` الأساسي + `201114621551`).
دلوقتي كل رقم ممكن يكون ليه **سياق/شخصية مستقلة** و**تشغيل/إيقاف مستقل**.

**اتعمل:**
- جدول `wa_number_configs` في Supabase (`session_id` PK · `label` · `persona` ·
  `enabled` · `updated_at`) — RLS مفعّل، الوصول من السيرفر بمفتاح الخدمة بس.
- `src/lib/wa-number-config.ts` — `getNumberConfig(session_id)` بيقرا الإعداد بأمان
  (أي عطل → الافتراضي: شغّال بلا سياق، عشان مايوقفش الرد)، و`numberPromptSection`
  بيبني قسم السياق اللي يتلزّق فوق البرومبت الأساسي.
- `baileys/route.ts`: بيحمّل إعداد الرقم حسب `session_id`، ولو `enabled=false`
  يسجّل ويسكت (الأدمن مستثنى)، وبيحقن سياق الرقم في `systemPrompt` عند الرد.
  **السياق إضافي** — القواعد الأساسية كلها بتفضل زي ما هي.
- `POST/GET /api/admin/wa-numbers/config` — قراءة/حفظ الإعداد (محمي بكوكي الأدمن).
- صفحة `/admin/wa-numbers`: زرار «إعداد» لكل رقم → محرّر فيه تبديل التشغيل
  وسياق/شخصية الرقم، وشارات «المارد موقوف» / «سياق مخصص» على الكارت.

**ليه سياق *إضافي* مش استبدال للبرومبت:** الاستبدال الكامل كان هيضيّع كل
الحمايات المكتسبة بالتجربة (who_is_this الأول، منع جُمَل البوت، اسم البراند…).
فالسياق بيتلزّق فوق الأساس، والأساس محمي.

---

## 🐞 تحديث ٢٢ يوليو ٢٠٢٦ — باجات ظهور الداتا (الماركتبليس + الصور) + إصلاحها من الجذر

محمد: «الداتا اللي بتتبعت للمارد (واتساب + شات مضمونة) مش بتظهر في الماركتبليس /
بورصة مضمونة». التتبّع طلّع الباجات دي، كلها اتصلحت من الجذر:

**١) إعلانات المارد ماكانتش بتوصل الماركتبليس أبدًا (الأخطر).**
- المارد (`create_listing_draft`) كان بيعمل مسودة بحالة `pending` **ومن غير ما
  يحفظ صورة العميل** (الأداة أصلًا مالهاش `image_urls`).
- كرون `publish-drafts` (اللي بيرقّي المسودات لإعلانات حية) بيدوّر على `status='new'`
  **وبصورة**. فمسودات المارد (`pending` + بلا صورة) ماكانش بيلمسها حد → عالقة للأبد.
  دي **نفس** الغلطة اللي الكرون اتعمل عشانها (١٧ يوليو)، رجعت لما الحالة اتغيّرت
  من `new` لـ`pending`.
- **الإصلاح الجذري:** (أ) الأداة بقت تحفظ صورة العميل في `image_urls` + حقن سيرفر-سايد
  للصورة المحفوظة في حلقتَي الأدوات (واتساب + شات) كضمان. (ب) الأداة بتكتب `status='new'`.
  (ج) الكرون بقى يقبل `new`+`pending` ونافذته ١٥٠ (منع الازدحام) — عشان **الغلطة
  دي متتكررش** حتى لو الحالة اتلخبطت تاني. (د) الأداة بتقول للعميل **الناقص فورًا**
  (صورة/سعر/تصنيف) عشان يكمّله في اللحظة.
- الباك-فيل: شغّلنا الكرون بوضع `?silent=1` (نشر من غير دفعة واتساب تكسر الرقم).

**٢) بورصة مضمونة العقارية سليمة.** ٤٨ مشروع من المارد ظاهرين فعلًا
(`status='published'` + `embargoed=false`). المشكلة كانت في الماركتبليس بس.

**٣) تاب «الكل» في الماركتبليس كان بيعرض ٦٠ إعلان بس.**
`MarketplaceClient.tsx` كان فيه `.limit(60)` ثابت. اتحل بـ**بيجينيشن «حمّل المزيد»**
(`visibleLimit` بيكبر ٦٠ كل ضغطة، بيرجع ٦٠ عند تغيير الفلتر، و`loadSeqRef` بيضمن الصحة).

**٤) الصور اللي عليها أسعار (منيو/قوايم).** المارد بيشوف الصورة (vision) بس ماكانش
متعلّم يفرّغها. دلوقتي البرومبت بيقوله صراحة: اقرا كل صنف وسعره من الصورة وسجّلهم
صنف صنف بـ`create_listing_draft` — مش صورة واحدة بلا تفاصيل، والأسعار في الصورة هي المصدر.

**✅ باج `message_count` — اتصلح من الجذر (٢٢ يوليو):** كان عالق (بيتحط 1 يدويًا في
`logOutboundMessage` مع كل صادر ومابيتزودش)، فـ`who_is_this` مايعرفش العميل الراجع
(بيفلتر `> 2`). الجذر: تريجر `tg_wa_sync_conversation_counters` كان بيسيّنك حقول «آخر
رسالة» بس من غير `message_count`. الحل: (١) التريجر بقى يزوّد `message_count = COALESCE+1`
على كل إدخال رسالة (كل المسارات: واتساب/شات/كرون)، (٢) شيلنا التصفير اليدوي من الكود،
(٣) باك-فيل بإعادة الحساب من الرسايل الفعلية. النتيجة: ٩٣٠ محادثة راجعة بقى `> 2`
(كانت صفر)، والمارد هيعرف العميل الراجع.

**نتيجة الباك-فيل (`?silent=1`):** اتأكد إن **صفر** رسايل واتساب اتبعتت (الرقم آمن).
نُشر الإعلان الحقيقي القابل للنشر. الباقي (٢٠ مسودة بصور) عالقين لأن الـ`contact_phone`
بتاعهم **LID مش رقم حقيقي** (Rino's Kitchen ×١٩ + واحد) ومش موجودين في `wa_lid_map`،
فمش ممكن نعملهم حساب مورّد. و٣١ مسودة `new` بلا صور (محتاجة العميل يبعت صورة —
المارد بقى بيطلبها فورًا). و١٥٠ `needs_review` محتاجة تصنيف.

**✅ باج الـLID في المسودات — اتصلح من الجذر (٢٢ يوليو):** كان الـLID بيتخزّن كـ
`contact_phone` في المحادثة والمسودات لو الربط ماكانش اتعمل. الحل: مسار الواتساب
(`baileys/route.ts`) بقى **يحلّ الـLID → الرقم الحقيقي من `wa_lid_map` أول ما الرسالة
تدخل** (لو فيه ربط)، فالمحادثة والمسودات والأدوات كلها تشتغل بالرقم الحقيقي والرد لسه
بيروح للـJID الصح. (العالقين الحاليين لسه محتاجين ربط/صور — الإصلاح بيمنع التكرار مستقبلًا.)

---

## فحص باجات استباقي (٢٢ يوليو) — عيلة `.maybeSingle()` وسباقات مسار الرد

**✅ اتصلح (٧ — كلهم منشورين):** استعلامات بتعمل `.maybeSingle()` على رقم/محادثة —
والرقم الواحد ليه صفوف كتير، فـ`.maybeSingle()` بيرمي `PGRST116` ويترجّع `null`:
- `whoIsThis` / `getMyOrders`: العميل المعروف كان يتقري «جديد». → `.limit(1)`.
- `manageOrder`: **صاحب الأوردر كان يتعامل كغريب فيتقفل قبول/رفض الأوردر (تدفّق فلوس).**
  → بنجيب كل بروفايلاته ونفحصهم ضد مورّد الأوردر.
- `recordLead`: كراش بيضاعف الليدز. → `.limit(1)`.
- `sendText` (fallback الـLID): إرسال بيفشل بصمت. → بنختار أحدث صف فيه `wa_jid`.
- **سباق الـ`metadata`:** read-modify-write المتزامن كان يمسح `wa_jid` (العميل غير
  قابل للوصول) ويبعت تنبيه ليد سخن مكرّر. → RPC `wa_meta_merge` (دمج ذرّي
  `metadata || patch`) + `wa_claim_hot_lead` (compare-and-swap فواحد بس يبعت التنبيه).
- **خصوصية fallback الاسم:** `findHistoryByName` كان بيرجّع رقم وآخر ٨ رسايل لأي شخص
  بنفس الاسم المعروض. → الربط بالاسم بيحصل بس لو فيه **رقم واحد** بالاسم ده؛ لو أكتر
  من شخص بنفس الاسم بنتعامل معاه كجديد من غير كشف رقم/رسايل حد تاني.

**⚠️ محدّدة ومحتاجة إصلاح بحرص (توقيت/تزامن في قلب مسار الرد الحي — مش اتلمست عشان
الإصلاح المتسرّع أخطر من الحالة النادرة):**
1. **debounce بيقارن ساعتين مختلفتين** (`body.timestamp` من واتساب vs `created_at` من
   السيرفر) بهامش ٢ث — ممكن يسقّط رسالة عميل جديدة في نافذة ضيّقة. الإصلاح الصح:
   قارن `created_at` بتاع الرسالة الواردة (ساعة السيرفر) بآخر صادر.
2. **dedup check-then-act (TOCTOU)** — إعادة إرسال متزامنة من Baileys ممكن ترد مرتين.
   الإصلاح: `insert` ذرّي والتعامل مع unique-violation كـ«اتعالجت».

> سباق الـ`metadata` وخصوصية الاسم كانوا هنا — **اتصلحوا** واتنقلوا لقايمة «اتصلح» فوق.
> فاضل الاتنين دول بس (توقيت/تزامن حسّاس في مسار الرد — محتاجين جلسة إصلاح مع اختبار).


---

## ✅ تصحيح ٢٤ يوليو ٢٠٢٦ — السباقين اللي فوق **اتصلحوا** (الملف كان قديم)

قسم «محدّدة ومحتاجة إصلاح بحرص» فوق بيقول إن فاضل سباقين في مسار الرد.
**الاتنين اتصلحوا فعلًا** — الملف ماتحدّثش وقتها فضلّل الجلسات اللي بعده:

| السباق | اتصلح في |
|---|---|
| debounce بيقارن ساعتين مختلفتين | `562141b` (٢٢/٧) + ضبط النافذة في `4718565` (٢٤/٧) |
| dedup check-then-act (TOCTOU) → رد مكرر | `562141b` (٢٢/٧) + كسر التعادل في `1c616b4` (٢٤/٧) |

**التأكيد بالداتا (٢٤/٧):** ٣ أيام ورا بعض بصفر رد مكرر (<٢٠ث) وصفر رسالة
عميل من غير رد خلال ١٥ دقيقة. التفاصيل في `SESSION-STATUS.md`.

## 🚨 عدّاد التسليم (✓✓) كان مكسور — والقاعدة اللي اتعلمناها منه

الشاشة كانت بتوري ٩٠٪ من الرسايل واقفة على `sent`، واللي بيقرا ده بيستنتج
إن الرقم متحظور. القياس المستقل (العميل رد خلال ١٠ دقايق = وصلت أكيد) طلّع:
**١٣٤ رسالة مثبت وصولها، ٢ بس ليهم ✓✓ = ١.٥٪ التقاط.**

السبب: كنا سامعين على `messages.update` بس — Baileys بيوصّل إيصالات الشات
الفردي في `message-receipt.update`. اتصلح في `be3e9e9` (ليسنر تاني، إضافي،
مش على مسار الإرسال).

**القاعدة ١١: «مفيش ✓✓» ≠ «الرسالة ماوصلتش».**
ده الوجه التاني لقاعدة ٢ («`wa_message_id` موجود ≠ الرسالة وصلت»).
العدّاد المكسور ده كلّفنا ترقية Baileys ٦.٧→٧.٠.٠-rc13 واترجّعت بعد ١٣ دقيقة
في نفس اليوم — كانت بتطارد شبح. **قبل أي استنتاج عن الحظر، قيس التسليم
بردود العملاء مش بالـACK.**

⏳ الفيكس لايف بس **لسه محتاج ترافيك يثبته** — شوف اختبار القبول في
`SESSION-STATUS.md`.
