# تحليل الأسباب الجذرية

> ٢٠ يوليو ٢٠٢٦ · ليه كل حاجة اتكسرت مرة واحدة، وإزاي منعنا التكرار

---

## الجذر ١ — مفيش نقطة إرسال واحدة

### العَرَض
لما الرقم اتنقل من Cloud API للمارد، **كل حاجة واتساب وقعت مع بعض**:
الردود، الـ OTP، دخول المالك، الإرسال الجماعي، إشعارات الحجز، تهاني أعياد الميلاد.

### السبب الحقيقي
**١٨ ملف** كان بينفّذ الإرسال بنفسه — كل واحد بينادي `graph.facebook.com`
مباشرة بنسخته الخاصة، وبيقرا التوكن بنفسه من `whatsapp_config`.

مفيش نقطة تحكم. القناة كانت متكررة ١٨ مرة.

### ليه الترقيع مايكفيش
إصلاح كل ملف لوحده = ١٨ إصلاح، وتاني مرة القناة تتغيّر يحصل نفس الشيء بالظبط.

### الإصلاح الجذري

```
مسارات Next.js  ──┐
                   ├──▶  lib/whatsapp.ts → sendText()  ──▶  المارد
دوال Deno Edge  ──┘         (نقطة واحدة)
   عن طريق /api/internal/wa-send
```

**اتعمل:**
- `src/app/api/internal/wa-send/route.ts` — البوابة الموحّدة (متختبرة، بعتت فعليًا)
- `supabase/functions/_shared/wa-send.ts` — مساعد لدوال Deno
- `src/app/api/admin/wa-review/send` — النسخة المكررة اتشالت
- `src/app/api/cron/publish-drafts` — النسخة المكررة اتشالت

**النتيجة:** القناة بتتغيّر في ملف واحد، والكل بيمشي وراها.

### الحارس

`scripts/check-wa-channel.mjs` — بيفشل لو أي ملف حي نادى Graph للإرسال.

```bash
node scripts/check-wa-channel.mjs
```

يتحط في CI عشان يمنع التكرار من غير ما حد يفتكر.

**الحالة:** ٨ ملفات حية → ٧ (كل مسارات Next.js اتحوّلت).
الباقي دوال Deno — تتحوّل بـ `_shared/wa-send.ts` واحدة واحدة.

---

## الجذر ٢ — أنماط `.vercelignore` غير مثبّتة

### العَرَض
`/api/whatsapp/*` كان بيرجع **404 لشهور**. الردود التلقائية مكانتش بتشتغل
ومحدش عرف السبب. اتشخّصت غلط على إنها مشكلة فوترة/توكن.

### السبب الحقيقي
سطر `WHATSAPP` — المقصود بيه فولدر التوثيق في الجذر — كان بيطابق
`src/app/api/whatsapp/` كمان، لأن النمط من غير `/` بيطابق أي عمق.

### ليه ده خطير
**بيفشل بصمت.** مفيش خطأ بناء، مفيش تحذير. المسار بيختفي وخلاص.

### الإصلاح الجذري
كل الأنماط اتثبّتت على الجذر (`/scripts`, `/docs`, `/WHATSAPP`...)
مع تعليق في أول الملف بيشرح ليه.

فحصت باقي الأنماط: **مفيش واحد تاني بيطابق حاجة جوه `src`** — بس دلوقتي
الفئة كلها ممنوعة، حتى لو حد ضاف `src/app/api/docs/` بكرة.

---

## الجذر ٣ — كتابة كود من غير قراءة الواجهات

### العَرَض
ويبهوك المارد كان بيرمي 500 على كل رسالة. رسالة أميرة وصلت واتسجّلت،
بس الرد وقع.

### السبب الحقيقي
اتكتب بالتخمين من ملف تاني. **٤ توقيعات غلط:**

| النداء | المكتوب | الصح |
|---|---|---|
| `upsertConversation` | `{contactPhone, contactName, direction}` | `{phone, name, contactType}` |
| `logInboundMessage` | `{from, ...}` | `{conversationId, wa_message_id, body}` |
| `getConversationHistory` | عاملها `{direction, body}` | بترجع `{role, content}` |
| `callClaude` | `{system, messages}` | `{systemPrompt, userMessage}` |

### الإصلاح الجذري
مش بس تصحيح التوقيعات — **تغيير طريقة الشغل**:
اتوثّقت في `CLAUDE.md` كقاعدة، و`npx tsc --noEmit` بقى خطوة إجبارية
قبل أي رفع. الأخطاء دي كانت هتتمسك في ثانية.

---

## الجذر ٤ — بيانات حساسة مكشوفة

### العَرَض
`suppliers` و `marketplace_suppliers` بيردوا على المفتاح العام بأعمدة
فيها `password_hash` و `payout_details` و `national_id`.

### السبب الحقيقي
سياسات RLS اتعملت على مستوى **الصفوف** بس. الفكرة كانت "اعرض الموردين
النشطين للعامة" — بس ده فتح **كل الأعمدة** مش الآمنة بس.

### الإصلاح الجذري
`REVOKE SELECT (أعمدة محددة) FROM anon` — حماية على مستوى العمود.
أدق من تعديل RLS وعكسه أسهل.

`migrations/20260720_secure_suppliers_columns.sql` ⏳ **مستني تنفيذ**

**القاعدة للمستقبل:** لما تفتح جدول للعامة، اسأل «أنهي أعمدة؟» مش «أنهي صفوف؟».

---

## اللي لسه مفتوح

| البند | ليه لسه |
|---|---|
| الإصلاح الأمني | 🔴 محتاج تنفيذ SQL — مفيش عندي `DATABASE_URL` |
| ٧ دوال Deno | تتحوّل بـ `_shared/wa-send.ts` واحدة واحدة |
| تنضيف جداول backup | موثّق في `DB-AUDIT.md` — محتاج موافقة |
| الحارس في CI | يتحط في GitHub Actions |
