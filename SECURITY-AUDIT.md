# مراجعة أمنية — مضمونة

> ٢٠ يوليو ٢٠٢٦ · **مفيش تعديل اتنفّذ — الإصلاحات جاهزة ومستنية موافقتك**

---

## 🔴 حرج — بيانات مكشوفة للعامة

المفتاح العام (`NEXT_PUBLIC_SUPABASE_ANON_KEY`) موجود في كود الموقع — يعني **أي زائر**
يقدر يستخرجه من المتصفح ويستعمله. والجداول دي بترد عليه:

### ١. `suppliers`

| العمود المكشوف | الخطورة |
|---|---|
| `password_hash` | 🔴 هاشات كلمات سر |
| `payout_details` | 🔴 بيانات تحويل بنكي |
| `payout_method` | 🔴 وسيلة الصرف |
| `commission_rate` · `commission_extra_rate` | 🔴 نسب عمولة سرية |
| `contact_phone` · `contact_email` | 🟠 بيانات تواصل |

**مؤكد بالاختبار** — استعلام بالمفتاح العام رجّع اسم مورد + تليفون + نسبة عمولة.

### ٢. `marketplace_suppliers` ← الأخطر

| | |
|---|---|
| صفوف مكشوفة | **146** |
| فيها **رقم قومي** فعلي | **4** 🔴 |
| `commission_rate` | مكشوف |

الرقم القومي مستحيل يكون كشفه مقصود.

### ٣. `listings` — على الأغلب مقصود

١٦٤ إعلان بتليفون مكشوف. ده منطقي (الإعلان محتاج وسيلة تواصل).
**سايبه زي ما هو** — لو عايز تغيّره، الأنضف view عام من غير التليفون.

---

## ✅ الإصلاح جاهز

`migrations/20260720_secure_suppliers_columns.sql`

الطريقة: **منع أعمدة محددة عن `anon` بس**. مش تغيير على الـ RLS ولا حذف بيانات.

**ليه آمن؟** فحصت كل مستهلكي `suppliers` في الكود — **كلهم صفحات أدمن ومالك**
وراء تسجيل دخول (`business-finance/*`, `owner/*`). مفيش صفحة عامة بتقراه.
فالمسجّلين مش هيتأثروا.

**عكسه:**
```sql
grant select (contact_phone, commission_rate, ...) on public.suppliers to anon;
```

**للتنفيذ:** Supabase Dashboard ← SQL Editor ← الصق الملف ← Run

---

## ✅ نضيف ومتحقق منه

| الفحص | النتيجة |
|---|---|
| أسرار مكتوبة في الكود | ✅ مفيش |
| ملفات `.env` في git | ✅ `.env.example` بس (قيم وهمية) |
| `SERVICE_ROLE_KEY` في كود العميل | ✅ في `/api/` بس |
| جداول حساسة تانية | ✅ محمية (`profiles`, `whatsapp_*`, `supplier_payouts`, `supplier_documents`, `bookings`, `cold_leads`) |
| الكرونات الـ ٨ | ✅ كل مسار موجود |
| الاستيرادات | ✅ مفيش مسار مكسور |

---

## ⏭️ بعد التنفيذ

١. نفّذ الـ SQL
٢. اتأكد:
```bash
curl "$SUPABASE_URL/rest/v1/suppliers?select=commission_rate&limit=1" -H "apikey: $ANON_KEY"
# المتوقع: خطأ صلاحية بدل البيانات
```
٣. افتح صفحة أدمن (`/admin/business-finance/...`) واتأكد إنها لسه شغالة

⚠️ **ولو `password_hash` فيه هاشات حقيقية لموردين** — يُفضّل تطلب منهم
تغيير كلمات السر، لأن الهاشات كانت مكشوفة لفترة مش معروفة.
