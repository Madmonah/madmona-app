# 📌 حالة المهام — مضمونة (آخر تحديث: 2026-07-22)

> الهدف من الملف ده: أي جلسة جديدة تبدأ من هنا، مش من الأول. الأرقام كلها متأكد
> منها من الداتابيز مباشرة في الجلسة دي.

---

## ✅ اتحل خلاص في الجلسة دي

### 1) الماركت بليس كان بيبان فاضي للزوار (أهم مشكلة) — **اتحل واتأكد live**
- السبب الجذري الحقيقي: دور `anon` معندوش صلاحية قراءة على عمود `kyc_status` في
  `marketplace_suppliers`. الكويريهات كانت بتطلبه → 401 → الماركت فاضي **للزوار بس**
  (المسجّلين كانوا بيشوفوا عادي، عشان كده المشكلة كانت متخفية).
- الإصلاح: شِلنا `kyc_status` من 3 كويريهات anon-facing:
  `marketplace/page.tsx` (SSR + fallback أمان)، `MarketplaceClient.tsx`،
  و`marketplace/[slug]/page.tsx` (كان `select('*')` بيكسر لينكات الإعلانات
  المتبعوتة للزوار → اتقسّم لكويري آمن + تواصل best-effort).
- التأكيد: البرودكشن كزائر موبايل رجّع 26 كارت في الـHTML الخام، ولينك إعلان رجّع 200.
- كوميت: `578f0ce`. التفاصيل الكاملة في BUGS-LOG (entry 2026-07-22 ب).

---

## 🟢 اتأكدنا إنها تمام (مش محتاجة شغل)

- **تصنيف الأراضي:** موجود فعلاً — «أرض زراعية» بيع (`sale-properties-agricultural-land`)
  وإيجار (`properties-agricultural-land`). مفيش أراضي بدون تصنيف.
- **داتا الواتساب الجديدة (الدرافتات):** فحصنا `listing_drafts` (381 صف):
  - `submitted` (7): كلها اتحوّلت لإعلانات خلاص.
  - `claimed` (113): 72 اتحوّلت. الباقي ناقص (71 من غير صور).
  - `draft` (90): **كلها من غير صور** = فورمات نص مليانة/متسابة، مش جاهزة تتنشر.
  - يعني **مفيش كومة داتا جاهزة متنساش تتنشر** — اللي جاهز اتنشر، والباقي ناقص صور/بيانات.
  - استثناء صغير: 4 درafتات عربيات إيجار من «أبيكس» (رقم +201034996165) جاهزة بصورة
    بس عالقة claimed. ممكن ننشرهم يدوي لو حبيت (بس بحذر — تجنّبًا لتكرار مشكلة الكرون).

---

## 🟡 محتاجة قرارك (مقدرش أقرره لوحدي — قابل للتنفيذ فورًا لو قلت تمام)

1. **شارة «موثّق» الخضرا:** شِلناها مؤقتًا مع إصلاح الماركت. علشان ترجع للزوار محتاجين
   نضيف صلاحية `GRANT SELECT (kyc_status) ON marketplace_suppliers TO anon` + نرجّع
   العمود في الكود. `kyc_status` بيتعرض أصلاً كشارة عامة، فده آمن — بس قرار ليك لأنه
   بيخلي حالة التوثيق (approved/pending) تبقى قابلة للقراءة العامة.

2. **أرقام إعلانات الدليل للزوار:** حاليًا الزائر غير المسجّل مايشوفش رقم إعلانات الدليل
   (بيسجّل علشان يتواصل). لو عايزها تبان للكل: `GRANT SELECT (contact_phone)`. ده قرار
   نموذج عمل (تسجيل مقابل تواصل مباشر).

3. **مضمونة كـ«صاحب الإعلان»:** فيه 66 إعلان منشور تحت «مضمونة». الأغلبية رقم تواصلهم
   هو رقم مضمونة نفسه (01002229982 = 57 إعلان، 01067122107 = 35) — يعني مش «متسجّلين
   غلط لصاحب تاني»، ده فعلاً رقم المنصّة. لو البراند إن مضمونة سوق مش بايع، ممكن نخفي
   اسم «مضمونة» كصاحب إعلان أو نحطه محايد. محتاج توجيهك للسلوك المطلوب.

---

## 🔴 متوقفة على البوت / الريبو التاني (مش موجود على الجهاز)

1. **مطاعم مضمونة الـ32 (paused):** أرقام تواصلها LID (زي `16295508590600`) أو لاندلاين
   (`+20 2 24800009`). جرّبنا نحلّها من `wa_lid_map` (فيه 1849 صف) → **0 منهم موجود**.
   يعني محتاجين بوت الواتساب (Baileys/رايلواي) يلتقط الـLID دول تاني. حاليًا موقوفين
   (مش ظاهرين للعامة) وده صح لحد ما يتحلّوا.
2. **ميديا شات المارد الشخصي:** مربوطة بفلو `whatsapp_messages`/البوت.
3. **بوت الواتساب على 3 أرقام:** ريبو منفصل (رايلواي) مش موجود على E:\ — الجلسة دي
   مقدرتش توصله. لازم يتفتح من مكانه.

> ملاحظة مهمة للجلسة الجاية: `wa_lid_map` فيه 1849 mapping شغّال — لو البوت لقط الـLID
> بتاعة المطاعم دي، هيبقى ممكن نحلّها بـSQL join من غير ما نستنى البوت يشتغل realtime.

---

## 🧰 معلومات تقنية سريعة (للرجوع)
- البرودكشن: `https://www.madmonacairo.com` (مش vercel.app ولا madmona.com).
- Supabase project: `mjhflxpxunwycbiquoig`. مضمونة supplier: `7310f6ef-...`.
- درس الجلسة: لما تفلتر anon، افحص **صلاحيات الأعمدة** مش بس RLS. اختبر بالـanon key
  الخام (curl REST) مش بحسابك — عمود واحد ممنوع يفشّل الكويري كله بصمت.

---

## 🔄 تحديث 2026-07-22 (مساءً) — بعد قرارات المالك

### ✅ اتنشرت 4 إعلانات أبيكس (كانت عالقة claimed بصور)
نشرناهم تحت صاحبهم الحقيقي: profile `36fb0b3a...` / supplier `a5c404dc...`
(صحّحنا اسمه من «موردة جديدة» → «أبيكس لتأجير السيارات»). إيجار عربيات يومي،
صور + تسعير. Slugs: `apex-car-*`. Backup: `_backup_apex_drafts_20260722`.

### ⚠️ «كمّل الدرافتات اللي ناقصها صورة بس»: مفيش منها ولا واحدة
فحصنا: الـ90 درافت `draft` كلهم **ناقصين السعر (90) والتصنيف (86) والمدينة (79)** —
مش ناقصين صورة بس. الـ41 `claimed` غير محوّلين: 0 كامل-إلا-الصورة، 4 بس كاملين
بصورة (دول أبيكس اللي اتنشروا). يعني مفيش داتا «تمام وناقصها صورة» نعملها.

### 🔴 نسبة إعلانات مضمونة لأصحابها: متوقفة على تسجيل الملاك (مش ممكن دلوقتي)
- 139 إعلان تحت مضمونة. التفصيل:
  - **57** رقم تواصلهم = رقم مضمونة الرسمي (01002229982) → دي فعلاً بتاعة المنصّة (برoker/دليل).
  - **35** رقمهم 01067122107 (رقم داخلي/محمد، والـsupplier المطابق «مطعم رقاق المدق») →
    نسبهم لمطعم غلط، فمسبناهم.
  - **~12** أرقام خارجية حقيقية (6 ملاك) بس **معندهمش حساب في النظام خالص**.
  - **32** أرقام LID/لاندلاين → متوقفة على البوت (موقوفين).
- **السبب إني مقدرش أنسبهم:** تريجر النشر (`enforce_listing_publish_requirements`)
  بيفرض إن رقم الإعلان يكون **متحقّق عبر واتساب OTP**، والنسبة محتاجة صاحب عنده
  **حساب متحقّق**. الملاك دول مش مسجّلين، وأنا مش مسموح أعمل حسابات ولا أتحقق من أرقام
  (ده شغل بوت الواتساب). كل الـ252 بروفايل في النظام مربوطين بحساب auth حقيقي.
- **الحل الحقيقي:** الملاك يسجّلوا عبر لوجين الواتساب (البوت) → وقتها الإعلانات تتنسب
  ليهم تلقائي بالمطابقة بالرقم. أو المالك يقرّر نعمل «سجلّات نسبة» بدون حساب (بس من
  غير أسماء/بيزنس نيم، فقيمتها محدودة) — قرار ليك.


---

# 🗓️ سجل جلسة ٢٢ يوليو ٢٠٢٦ (مساءً) — Claude/Cowork — اقرا ده الأول

> الهدف: أي جلسة جاية تبدأ من هنا وماتعيدش أي كلام. كل اللي تحت اتعمل واتنشر على
> البرودكشن (`main` → Vercel/Railway) واتأكد **لايف** إلا المذكور صراحةً.

## ✅ اتعمل واتنشر واتأكد لايف (بالكوميتات)
1. **المارد رجع اتصل + هويته اتغيّرت + بيداوي نفسه** — `e0cc06a` (browser: `['Mac OS','Desktop','10.15.7']` بدل `['Madmona',...]` في `wa-service/src/sessions.js`) و`10387c6` (لما الجلسة تتعمل logged-out بتمسح نفسها وتطلّع QR جديد تلقائي بدل ما تقف).
2. **استرجاع ١٣ ملف كانوا متصفّرين (0 بايت) في الشجرة المحلية** — اترجّعوا من git. (تفاصيل تحت في البجات).
3. **batching شات الموقع** — `225838e` (`/api/chat/route.ts`: يستنى `MARID_BATCH_WAIT_MS`=7000ث، لو جت رسالة أحدث يسكت، لو مفيش يرد رد واحد للدفعة — مطابق للواتساب).
4. **أيقونة المارد** — `554d8ac` (سكربت `scripts/_make_marid_icons.py` من `reel-engine/genie.png` → `public/marid-icon-{192,512,maskable-512}.png` + `marid-apple-180.png`، الجني في النص بمنطقة أمان).
5. **ذاكرة المارد عبر القنوات** — `d7551c0` (`/api/chat`: التاريخ بيتجمّع من كل محادثات نفس الرقم مش المحادثة الحالية بس).
6. **تحسين شكل شات المارد** — `f6b07fa` (خط Cairo، هيدر/خلفية متدرّجين، فقاعات أنعم، مدخل أنضف).
7. **أدوات الشغل في المارد (شات + شغل زي ClickUp)** — `4932a9f` (`create_task`) و`eda0dab` (`list_tasks` + `complete_task`) في `src/lib/marid-tools.ts` → جدول `flow_tasks` (source='chat'). **متأكد لايف:** المارد بيفهم بالعامية، يطلّع العنوان/المكلّف/الأولوية، يسجّل، يعرض، يقفل.
8. **المارد يسمع اسمه في رومات الفريق** — `da41d82` (`/team` sendMsg: أي رسالة فيها «مارد» بتنادي `/api/team/marid` تلقائي، مش بس بالزرار).
9. **تزويق شات الروم + مؤشّر «المارد بيفكر…»** — `282b6a8` (Cairo/هيدر/فقاعات + `maridThinking` state). **متأكد لايف** إن المارد بيرد في الروم ويستخدم أدوات الشغل هناك كمان.

## 🐞 بجات وملاحظات مهمة (متتكررش)
- **تصفير ملفات:** يوم ٢٢/٧ لقينا ١٣ ملف أساسي 0 بايت في الشجرة المحلية (page.tsx, layout.tsx, marketplace×2, account, add-listing, TopNav, whatsapp.ts, chat/marid, api/chat, cron/publish-drafts, CLAUDE.md, BUGS-LOG.md). **البرودكشن كان سليم** (بينشر من git). **قاعدة:** قبل أي commit اعمل `git status` — لو فيه ملفات اتصفّرت `git restore` قبل ما تكمّل. السبب المرجّح sync/كراش على الجهاز.
- **الـshadowban:** رقم المارد بيتصل ويستقبل بس الإرسال ممكن مايتسلّمش. الوارد شغّال دايماً — عشان كده التسجيل بالـOTP المعكوس (العميل يبعت الكود للمارد).
- **الـwebhook للأتمتة محتاج secret:** `WA_SERVICE_SECRET`/`SHARED_SECRET` موجودين في Railway/Vercel بس، مش في ملفات البيئة المحلية.

## 🔨 تاسكات باقية (بتفاصيلها — متتشرحش تاني)
- **#6 حساب موثّق + #2 حفظ دخول:** يتربط أونبوردينج الشات بـ`/api/auth/wa` (OTP معكوس جاهز ومقاوم للحظر) → يعمل Supabase session ثابتة (حل التوثيق والحفظ مع بعض). الفلو: start→wa_url→poll→finish→verifyOtp.
- **#7 يوزر نيم بدل الرقم:** عرض الاسم بدل رقم الموبايل في الشات.
- **#9 تأكيد الفويس:** الكود متطبّق (Whisper عبر Groq/OpenAI في `marid-media.ts`)؛ محتاج تأكيد إن `GROQ_API_KEY`/`OPENAI_API_KEY` متظبّط على Vercel + تجربة رسالة صوتية.
- **#5 الترويج لشات الموقع:** مداخل/CTAs على الموقع + رسالة جاهزة.
- **#10 باقي تكامل ClickUp:** اربط الرسالة بأوردر/ليد/مشروع + أزرار UX (حوّل لمهمة بضغطة) + posts + مكالمات بتلخيص. الـERP/CRM كله داخلي (جداول `erp_*`, `bz_*`, `inventory_*`, `customers`, `leads`, `marketplace_orders`, `flow_tasks`...).

## 🆕 مطالب جديدة (جلسة ٢٢/٧ مساءً) — لسه متعملتش
- **A) الواتساب بيضيّع رسايل لما تتبعت دفعة:** «لما نبعت أكتر من رسالة مش كلها بتوصل». محتاج فحص مسار الوارد في `wa-service/src/index.js` + `/api/whatsapp/baileys/route.ts` (منطق `alreadyCovered` 2ث + batch 7ث + فلتر العمر MAX_MESSAGE_AGE_MIN + append/notify). التأكد إن **كل** رسالة بتتسجّل حتى لو الرد واحد.
- **B) مسار مستقل لكل رقم واتساب للمارد:** كل رقم يتضاف يكون ليه مسار/سياق (وممكن شخصية/إعداد) لوحده. الأساس موجود (multi-session في wa-service + الرد يخرج من نفس الرقم) — محتاج توسعة per-number config.
- **C) لوحة أدمن لمراقبة محادثات المارد لايف (داخل شات مضمونة):** الأدمن يشوف المحادثات اللي المارد بيرد عليها عشان يلحق أي غلط. موجود `/admin/wa-review` كنقطة بداية — محتاج عرض حي/أوضح مدمج في تجربة الشات.
