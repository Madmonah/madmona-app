# 📌 حالة المهام — مضمونة (آخر تحديث: 2026-07-22)

> الهدف من الملف ده: أي جلسة جديدة تبدأ من هنا، مش من الأول. الأرقام كلها متأكد
> منها من الداتابيز مباشرة في الجلسة دي.

---

## ✅ اتحل خلاص في الجلسة دي

### 1) الماركت بليس كان بيبان فاضي للزوار (أهم مشكلة) — **اتحل واتأكد live**
- السبب الجذري الحقيقي: دور `anon` معندوش صلاحية قراءة على عمود `kyc_status` في
  `marketplace_suppliers`. الكويريهات كانت بتطلبه → 401 → الماركت فاضي **للزوار بس**
  (المسجّلين كانوا بيشوفوا عادي، عشان كده المشكلة كانت متخفية).
- الإصلاح: شِلنا `kyc_status` من 3 كويريهات anon-facing:
  `marketplace/page.tsx` (SSR + fallback أمان)، `MarketplaceClient.tsx`،
  و`marketplace/[slug]/page.tsx` (كان `select('*')` بيكسر لينكات الإعلانات
  المتبعوتة للزوار → اتقسّم لكويري آمن + تواصل best-effort).
- التأكيد: البرودكشن كزائر موبايل رجّع 26 كارت في الـHTML الخام، ولينك إعلان رجّع 200.
- كوميت: `578f0ce`. التفاصيل الكاملة في BUGS-LOG (entry 2026-07-22 ب).

---

## 🟢 اتأكدنا إنها تمام (مش محتاجة شغل)

- **تصنيف الأراضي:** موجود فعلاً — «أرض زراعية» بيع (`sale-properties-agricultural-land`)
  وإيجار (`properties-agricultural-land`). مفيش أراضي بدون تصنيف.
- **داتا الواتساب الجديدة (الدرافتات):** فحصنا `listing_drafts` (381 صف):
  - `submitted` (7): كلها اتحوّلت لإعلانات خلاص.
  - `claimed` (113): 72 اتحوّلت. الباقي ناقص (71 من غير صور).
  - `draft` (90): **كلها من غير صور** = فورمات نص مليانة/متسابة، مش جاهزة تتنشر.
  - يعني **مفيش كومة داتا جاهزة متنساش تتنشر** — اللي جاهز اتنشر، والباقي ناقص صور/بيانات.
  - استثناء صغير: 4 درafتات عربيات إيجار من «أبيكس» (رقم +201034996165) جاهزة بصورة
    بس عالقة claimed. ممكن ننشرهم يدوي لو حبيت (بس بحذر — تجنّبًا لتكرار مشكلة الكرون).

---

## 🟡 محتاجة قرارك (مقدرش أقرره لوحدي — قابل للتنفيذ فورًا لو قلت تمام)

1. **شارة «موثّق» الخضرا:** شِلناها مؤقتًا مع إصلاح الماركت. علشان ترجع للزوار محتاجين
   نضيف صلاحية `GRANT SELECT (kyc_status) ON marketplace_suppliers TO anon` + نرجّع
   العمود في الكود. `kyc_status` بيتعرض أصلاً كشارة عامة، فده آمن — بس قرار ليك لأنه
   بيخلي حالة التوثيق (approved/pending) تبقى قابلة للقراءة العامة.

2. **أرقام إعلانات الدليل للزوار:** حاليًا الزائر غير المسجّل مايشوفش رقم إعلانات الدليل
   (بيسجّل علشان يتواصل). لو عايزها تبان للكل: `GRANT SELECT (contact_phone)`. ده قرار
   نموذج عمل (تسجيل مقابل تواصل مباشر).

3. **مضمونة كـ«صاحب الإعلان»:** فيه 66 إعلان منشور تحت «مضمونة». الأغلبية رقم تواصلهم
   هو رقم مضمونة نفسه (01002229982 = 57 إعلان، 01067122107 = 35) — يعني مش «متسجّلين
   غلط لصاحب تاني»، ده فعلاً رقم المنصّة. لو البراند إن مضمونة سوق مش بايع، ممكن نخفي
   اسم «مضمونة» كصاحب إعلان أو نحطه محايد. محتاج توجيهك للسلوك المطلوب.

---

## 🔴 متوقفة على البوت / الريبو التاني (مش موجود على الجهاز)

1. **مطاعم مضمونة الـ32 (paused):** أرقام تواصلها LID (زي `16295508590600`) أو لاندلاين
   (`+20 2 24800009`). جرّبنا نحلّها من `wa_lid_map` (فيه 1849 صف) → **0 منهم موجود**.
   يعني محتاجين بوت الواتساب (Baileys/رايلواي) يلتقط الـLID دول تاني. حاليًا موقوفين
   (مش ظاهرين للعامة) وده صح لحد ما يتحلّوا.
2. **ميديا شات المارد الشخصي:** مربوطة بفلو `whatsapp_messages`/البوت.
3. **بوت الواتساب على 3 أرقام:** ريبو منفصل (رايلواي) مش موجود على E:\ — الجلسة دي
   مقدرتش توصله. لازم يتفتح من مكانه.

> ملاحظة مهمة للجلسة الجاية: `wa_lid_map` فيه 1849 mapping شغّال — لو البوت لقط الـLID
> بتاعة المطاعم دي، هيبقى ممكن نحلّها بـSQL join من غير ما نستنى البوت يشتغل realtime.

---

## 🧰 معلومات تقنية سريعة (للرجوع)
- البرودكشن: `https://www.madmonacairo.com` (مش vercel.app ولا madmona.com).
- Supabase project: `mjhflxpxunwycbiquoig`. مضمونة supplier: `7310f6ef-...`.
- درس الجلسة: لما تفلتر anon، افحص **صلاحيات الأعمدة** مش بس RLS. اختبر بالـanon key
  الخام (curl REST) مش بحسابك — عمود واحد ممنوع يفشّل الكويري كله بصمت.
