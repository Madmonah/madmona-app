// src/app/admin/overview/page.tsx
// ============================================================================
// 🗂️ (٢٨ أغسطس ٢٠٢٦) محمد: «عايزها نسخة واحدة علشان تبقى لمّة كل حاجة».
//
// الأوفرفيو كانت فهرس ٧٩ أداة بـاستعلام واحد، والداشبورد فيها الأرقام
// الحية — نسختين من نفس الفكرة (الكومبوننت جوّه dashboard اسمه
// AdminOverview أصلاً، دليل إنهم كانوا حاجة واحدة اتفصلت).
//
// دلوقتي: الأرقام + كل الأدوات في `/admin/dashboard`.
// الصفحة دي بقت تحويل — بتحمي أي لينك أو بوكمارك قديم.
// ============================================================================
import { redirect } from 'next/navigation'

export default function AdminOverviewRedirect() {
  redirect('/admin/dashboard')
}
