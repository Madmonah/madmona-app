// src/app/admin/prompt-versions/page.tsx
// ============================================================================
// ١٩ أغسطس ٢٠٢٦ — تكملة لملاحظة محمد ١٥ أغسطس ٢٠٢٦ ("مش عايز نسخ قديمة
// تلخبطني"): الصفحة كانت أصلا موصوفة في الكود نفسه إنها "صندوق اقتراحات
// مش مصدر تشغيل" — مفيش أي أجينت بيقرا من prompt_versions، والأجينت اللي
// كان المفروض يولّدها (prompt-optimizer) عمره ما اشتغل. دلوقتي agent_registry
// اتمسح خالص، فالصفحة بقت redirect بدل ما تفضل تورّي مقارنة مع جدول ممسوح.
// ============================================================================

import { redirect } from 'next/navigation'

export default function PromptVersionsRemoved() {
  redirect('/admin/hq')
}
